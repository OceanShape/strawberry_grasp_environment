# G — 면접용: 쿼드트리 스캔 설계 근거와 적응 분할 시나리오

> **성격**: 제출 산출물이 **아니다.** F 와 같은 부류로, 면접에서 "왜 쿼드트리인가",
> "그냥 한 번 보고 가면 되지 않나", "실기에서도 그렇게 했나"가 나왔을 때 여는 자료다.
>
> **기준일** 2026-09-14. **§6-1 가지치기는 09-10 에 코드로 반영되어 10:13 런으로 검증됐다**
> (`log/m3/20260910T101326-43213d77/`). **§6-2 적응 분할은 09-11 에 구현, 09-12 런 10 에서 실행 검증됐다**
> (`log/m3/20260912T013357-ea2ae427/`): nw·sw 분할, ne 잎, se 가지치기, 세부 자세 5칸 중 4 수락·1 거부(부모 퇴화). **단, 시뮬에서 분할의 이득은 측정되지 않는다** — §6-2 마지막 문단과 §6-3 을 그대로 말한다.
>
> **09-11 보강**: §2 머리(근거의 두 층 구조·부를 이름) · §2 ② 기구학 쉬운 설명 · §8 예상 질문 2개(출발 자세와 특이점, 세부 자세 보장 여부) ·
> §6-2 B 안 적용 범위 정정 · §9 금지 표현 2개.
>
> **왜 이 문서인가**: 부트캠프 최종본은 쿼드트리 1차 스캔과 가지치기를 빼고
> 4분면 전수 순회로 축소됐다. 면접에서 쿼드트리를 어필하려면 그 사실을 먼저 알고,
> 근거를 코드와 수치로 말해야 한다.

---

## 1. 한 줄 정의

**보드를 4분면으로 나눈 트리 위에서, 원거리 스캔으로 내려갈 분면을 고르고(가지치기),
분면마다 티칭된 근거리 스캔 겸 파지 시작 자세로 이동해 수확하며,
밀집한 분면만 한 단계 더 쪼개는(적응 분할) 탐색 구조.**

레벨은 세 개다.

| 레벨 | 노드 이름 | 자세 | 하는 일 |
|---|---|---|---|
| 0 | `root` (home) | overview `[87.98, -94.92, 129.89, 175.94, -31.34, 93.42]` | 보드 전체를 멀리서 봄. **익은 과실이 있는 분면**만 고른다 |
| 1 | `root/nw` `ne` `se` `sw` | v12 티칭 자세 4개 (YAML) | 분면을 가까이서 봄. **줄기 파지점**을 뽑고 그 자세에서 pick 을 시작한다 |
| 2 | `root/nw/nw` … | (실기: NW 4개만 티칭됨) | 밀집·가림이 심한 분면만 더 가까이 |

---

## 2. 왜 쿼드트리인가 — 근거 세 가지 (이 순서로 말한다)

> **구조부터 잡는다 (09-11).** 세 근거는 두 층이다.
> - **①·② = 왜 공간을 나눠서 여러 번 보나.** 칸이 고정된 그리드여도 성립한다. **"트리라서 가능한 것"으로 말하지 않는다.**
> - **③ = 왜 하필 트리인가.** 트리 전용은 가지치기와 적응 분할뿐이다. "4칸 고정이면 그리드 아니냐"(§8)에는 ③으로만 답할 수 있다.
>
> 말하는 순서도 이 층을 따른다: ①(정보) → ②(관절 자세) → ③(가지치기·적응 분할).
> ①·②로 "나눠야 한다"를 세우고, ③으로 "트리여야 한다"를 닫는다.

### 근거 ① 인식은 두 단계가 필요하다 — 레벨마다 얻는 정보가 다르다

실기 `strawberry_fusion_node` 는 인식을 **네 단계**로 쌓는다 (`_log_perception_summary` 주석 그대로):

> ripe segmentation → ripe with 3D depth → stable stem/keypoint pick candidate → published target

토픽도 둘로 갈린다.

| 채널 | 무엇 | 어떻게 | 어디서 얻나 |
|---|---|---|---|
| `/strawberry/detection/scene_positions` | 익은 과실의 **중심 좌표** | 세그 마스크 중심 픽셀 + 그 픽셀의 깊이 | **멀리서도** 된다 (과실은 ~30mm 덩어리) |
| `/strawberry/detection/pick_pose` | **줄기 파지점** (KP0) | 키포인트 픽셀 + 그 픽셀의 깊이, 안정화 후 발행 | **가까이서만** 된다 (줄기는 1~2mm) |

카메라는 그리퍼 밑동에 붙은 eye-in-hand(D455) 다. 그러니 **자세가 곧 해상도**다.

| 레벨 | 그리퍼 밑동에서 보드까지 (보드 y=810 기준) | 픽셀 1개의 크기 (근사) | 1~2mm 줄기 |
|---|---|---|---|
| 0 overview | 약 810mm | 약 1.9mm | 1px 이하 — 키포인트·깊이 불가 |
| 1 분면 (v12) | 약 470~530mm | 약 1.2mm | 경계 |
| 2 세부 (실기 NW 티칭, ee y=433) | 약 380mm | 약 0.9mm | 가능 |

> 픽셀 크기는 D455 깊이 화각 약 87° / 640px 로 잡은 근사다. 카메라 광학 중심과 그리퍼 밑동의
> 오프셋은 빼지 않았다. **경향만 말하고 숫자를 단정하지 않는다.**

정리하면: **상위 레벨은 "있는가"에 답하고 하위 레벨은 "어디를 잡는가"에 답한다.**
한 자세로 둘 다 얻을 수 없으니 두 번 봐야 하고, 두 번째 보기는 보드 전체를 한 자세로 못 담으니
공간을 나눠야 한다. (여기까지는 그리드도 같다 — 트리여야 하는 이유는 ③.)

**부를 이름**: "정확도"라고 하지 않는다. "정확도가 몇 % 올랐나"라는 수치 질문을 부르는데, 위 픽셀 크기는 근사라
숫자를 단정할 수 없다. **"레벨마다 얻는 정보가 다르다(해상도)"**로 말한다.

**깊이 1 을 "파지점 확정"으로 단정하지 않는다.** 위 표에서 깊이 1(약 1.2mm/px)은 "경계"이고, 확실히 되는 것은 깊이 2(약 0.9mm/px)다.
그래서 "깊이 0 은 있는가, 더 가까운 레벨은 어디를 잡는가"로 말하고, **"깊이 1 로 부족한 곳만 깊이 2 로 내려간다"**로 ③ 적응 분할에 잇는다.

### 근거 ② 분면 자세는 카메라 시점이면서 **파지 시작 관절 자세**다

`PLANNER_CHANGES.md` 09-09 항목: *"쿼드트리 스캔의 전제(모든 pick 은 scan pose 에서 시작)"*.
`curobo_planner_node._abort_pick_with_complete` 가 중단 경로에서도 scan pose 로 복귀하고 나서
`pick_complete` 를 발행하도록 고친 이유가 이것이다.

6축 팔은 같은 그리퍼 위치를 여러 관절 조합(브랜치)으로 만든다. 어느 브랜치에서 출발하느냐가
그 영역의 과실에 닿을 수 있는지를 결정한다. 실기 기록 [`compute_nw_pick_ready_pose.py`](../_baseline/A_strawberry_motion/scripts/compute_nw_pick_ready_pose.py) 도큐스트링:

| 출발 자세 | IK 결과 | 보드까지 계획 | 세부 4칸까지 관절 변화 |
|---|---|---|---|
| 분면 중심 자세 (옛 `root/nw`) | 팔꿈치 거의 편 특이점 근처 해만 (\|J3\| ≤ 1.2°) | y=702mm 에서 막힘 (보드 672 못 닿음, `IK_FAIL`) | 174~205° |
| 세부 4칸 중심 자세 (J3 ≈ 63°) | 건강한 브랜치 | 보드면까지 cuRobo 단독으로 도달 | 18~36° |

즉 **공간 분할이 곧 관절 브랜치를 영역별로 고정하는 장치**다. 분면 자세가 없으면 모든 pick 을
overview 한 자세에서 출발해야 하고, 멀리 있는 과실은 다른 브랜치 해가 나와 (a) `IK_FAIL`/스윙 가드에
걸려 못 집거나 (b) 보드 옆에서 팔 전체를 뒤집는 재배치가 나온다. (b)가 시뮬 브릿지에서 실제로 났던
elbow-flip 다이브다 (F §4). 09-08 에 `ripe_01`·`ripe_03` 위치를 옮긴 이유도 한 자세에서 `IK_FAIL`
7회·5회였기 때문이다 (`layout_layer.usd` 주석).

**기구학을 쉬운 말로 (되물으면).** 위 표를 "같은 목표인데 출발 자세에 따라 실패한다"로 요약하면 반만 맞다.
특이점 해는 목표 때문이 아니라 **출발 자세 자체**에서 나왔다.

- 6축 팔은 같은 손끝 위치·방향을 여러 관절 조합(브랜치)으로 만든다(§4). 팔은 연속으로 움직이므로, 출발한 브랜치를 벗어나려면 팔을 크게 뒤집어야 한다.
- 특이점은 팔이 어떤 방향으로 움직이지 못하는 자세다. 대표가 **팔꿈치를 다 편 상태** — 더 뻗을 수 없고, 근처에서는 손끝을 조금 옮기는 데도 관절이 크게 돈다.
  팔을 쭉 편 채로는 선반 안쪽 컵을 더 밀어 넣을 수 없는 것과 같다.
- 실기 기록(`compute_nw_pick_ready_pose.py` 도큐스트링): 옛 NW 분면 자세를 **시드 128개**로 IK 하니 모든 해가 |J3| ≤ 1.2° — 그 자세에는 다른 브랜치가 아예 없었다.
  거기서 출발하면 뻗을 여유가 없어 y=702mm 에서 막히고, 보드면(672mm)을 30mm 남기고 `IK_FAIL`.
  **같은 실패 목표**(06-18, x=-255, z=828mm)를 세부 4칸 중심 자세(J3≈63°, 팔꿈치 굽힘)에서 출발시키면 cuRobo 만으로 보드면까지 계획된다.

한 문장: **"같은 목표라도 출발 자세가 팔꿈치를 다 편 특이점 근처면 더 뻗을 여유가 없어 목표 직전에 막힌다.
다른 브랜치 해를 찾더라도 팔을 통째로 뒤집는 이동(elbow-flip)이 된다."**

### 근거 ③ 가지치기와 적응 분할 — 트리라서 할 수 있는 두 가지

- **가지치기(pruning)**: 부모에서 한 판정으로 자식 서브트리를 통째로 건너뛴다. 여기서는 레벨 0 의
  `scene_positions` 를 분면별로 세어 **0개인 분면은 이동·근거리 스캔·pick 을 전부 생략**한다.
  전수 순회는 이 판정 없이 네 자식을 무조건 펼치는 것이다.
- **적응 분할(adaptive subdivision)**: 모든 칸을 같은 깊이로 쪼개지 않고 **필요한 칸만** 한 단계 더 쪼갠다.
  실기 팀은 가림이 심한 NW 에만 깊이 2 자세 4개를 티칭했다. "필요한 곳만 내려갔다"가 적응 분할의 정의다.

**"시간 절약" 은 첫 근거로 쓰지 않는다.** 깊이 1·4칸에서 가지치기 하나만으로는 "한 번 보고 있는 데만 간다"와
구별이 안 된다는 반론이 바로 온다. 시간은 ①·② 의 부산물로만 말한다.
**금지어는 아니다** — 먼저 꺼내지 않을 뿐이다. 물으면 §8 "시간이 절약되나?"의 답(가지치기 효과는 크지 않고, 전체 단축의 대부분은
시뮬 dwell 단축)으로 수치와 함께 답한다. 피하면 오히려 감추는 것처럼 보인다.

---

## 3. 원안 · 실기 최종본 · 시뮬 — 무엇이 빠졌었나

| 단계 | 원안 | 실기 최종본 (`_baseline`) | 시뮬 02:02 런 (수정 전) | 시뮬 10:13 런 (수정 후) |
|---|---|---|---|---|
| L0 home 1차 스캔 | 있음 | **없음** (overview 는 시작 게이트·복귀점) | 없음 | ✅ 09-10 반영 (`overview_prescan`, `scene_positions` 기준) |
| 타겟 있는 분면만 순회 | 있음 | **없음** — 4분면 무조건. 데모용 순서 sw → nw_flat → ne → se (원 팀은 nw 시작 4셀 순회도 실기 검증, 민1 STEP 6). 리포 YAML(`nw_flat` 없음)과 돌리면 NW 가 빠지는 별칭 결함은 09-09 시뮬에서 고침(`cell_traversal.py`) | 없음 — se 0개인데 방문 | ✅ 09-10 반영 — 10:13 런에서 se 미방문 |
| L1 분면 자세 이동 후 근거리 스캔 | 있음 | 있음 (v12) | 있음 (분면 필터가 카메라 시야 대체) | 유지 |
| 분면 전부 수확, 재스캔 | 있음 | 있음 | 있음 | 유지 |
| L2 적응 분할 | 있음 (NW) | 티칭 자세만 있고 **런타임 판정 없음** | 논리 분할 퇴화 → 09-10 중심선 교정 | 규칙 기반 판정 **09-11 구현**(§6-2, 후보 밀도 ≥3 → 세부 자세 유도), 런 10 대기 |
| overview 복귀 | 있음 | 있음 + `HARVEST_PRIORITY_ORDER` (초기 설계 잔재) | 동일 | 유지 |

면접에서 경위는 이 한 문장으로 끝낸다:

> 실기 최종본은 KPI 발표를 위해 1차 스캔과 가지치기를 빼고 전수 순회로 축소됐다.
> 시뮬에서 원안 구조를 복원했고, 파라미터로 분리해 실기 기본 동작은 그대로 보존했다.

---

## 4. 용어를 물어보면

**세그멘테이션 vs 키포인트.** 익음은 과실 **덩어리의 색·모양** 속성이라 마스크 단위 분류가 맞다.
파지점은 **가는 줄기 위의 한 점**이라 마스크로는 못 찍고 키포인트 모델이 픽셀 좌표를 내야 한다.
이 프로젝트의 키포인트는 KP0 줄기 밑동(파지 목표) · KP1 줄기 중간 · KP2 줄기 끝(방향 기준) 이다.

**깊이(depth).** RealSense 깊이 프레임은 픽셀마다 "카메라 광축 방향으로 표면까지의 거리"를 준다.
거리이긴 하지만 픽셀별·축 방향 값이고, 내부 파라미터로 역투영해야 3D 점이 된다 (`_px_to_3d`).
줄기가 가늘어 키포인트 픽셀의 깊이가 배경(보드)에 찍히는 일이 잦고, 그래서 fusion 노드는
반경 6px 중앙값을 쓰고 `stem_keypoint_depth_invalid` 로 거부하며, 여러 프레임 안정화(`stable_hits`,
`position_min_samples`, `position_max_spread`)를 거친 뒤에야 발행한다. **이 안정화가 실기 dwell 12초의 이유다.**

**IK 브랜치 / elbow-flip.** 같은 손끝 위치를 팔꿈치 위/아래, 어깨 앞/뒤, 손목 ±180° 로 만들 수 있다
(최대 8개 해). 수치 IK 는 어느 브랜치 해를 줄지 보장하지 않는다. 브릿지 MoveLine 에서 45mm 이동에
J1 이 176° 도는 해가 20% 나온 것이 그 증상이었다 (F §4). 분면 자세는 pick 을 "이미 맞는 브랜치에서"
시작하게 만든다.

---

## 5. 적응 분할 — 실기 코드에 있는 것과 없는 것

**있는 것 (기계장치):**

| 기존 장치 | 위치 | 동작 |
|---|---|---|
| 계층 이름 | `cell_id` = `root/nw/se` 식 | 트리 경로가 그대로 셀 이름 |
| YAML 세부 자세 탐색 | `scan_executor_node._compute_scan_order` | `target_cell:=root/nw` 로 띄우면 YAML 에 `root/nw/nw..sw` 가 있을 때 그 4자세를 순서대로 방문 (`SUBCELL_SCAN_STARTED`) |
| collect-then-pick | `collect_then_pick`, `collect_pick_ready_cell` 파라미터 | 세부 4자세를 돌며 후보를 모은 뒤 부모의 pick-ready 자세로 가서 순위대로 pick |
| 논리 분할 | `_group_poses_by_subcell` | 한 시야의 탐지를 2×2 로 나눠 **수확 순서**만 정함 (이동 없음). 순서는 아래쪽 먼저 sw → se → nw → ne (§8 순서 Q) |
| 실기 깊이 2 자세 | `compute_nw_pick_ready_pose.py` `SUBCELLS_DEG` | NW 4칸, 물리 검증됨. 현재 YAML 에는 없음 |

**없는 것 (판정):** "언제 쪼갤지"를 정하는 런타임 규칙이 없다. 쪼갤지 여부는 사람이 오프라인에서
정해 YAML 에 자세를 넣는 방식이었다. 즉 실기의 적응 분할은 **정적**이다.

**논리 분할의 결함 — 09-10 수정 완료:** `_group_poses_by_subcell` 이 보드 전체 중심선(`BOARD_SUBCELL_*_MID_M`)으로
분면 안을 나눠서, 로그가 매 분면 `root/nw/nw:2 · root/ne/ne:1 · root/sw/sw:3` 처럼 부모와 같은 이름의
구석으로 몰렸다. 09-08 에 bbox 중점을 보드 고정값으로 바꾸면서 생긴 부작용이다.
중심선을 **부모 분면 경계**로 바꿨고, 10:13 런에서 실제로 분산됐다:

| 분면 | 02:02 (보드 중심선) | 10:13 (부모 분면 중심선) |
|---|---|---|
| nw | `nw:2` 한 칸 | `sw:1  se:1` |
| ne | `ne:1` 한 칸 | `sw:1` |
| sw | `sw:3` 한 칸 | `se:1  nw:1  ne:1` |

같은 딸기 배치인데 2차 분할이 살아났다. **면접관이 로그를 보면 바로 보이는 지점이라 먼저 고쳤다.**

**실기 NW 세부 자세의 성질 (FK 로 확인, 2026-09-10):**

| 자세 | ee (mm) | 보드 810 기준 최소 여유 | v12 `root/nw` 대비 최대 스윙 |
|---|---|---|---|
| `root/nw/nw` | (-370, 433, 826) | 129mm | 194° (J1 180°) |
| `root/nw/ne` | (-265, 433, 842) | 129mm | 189° |
| `root/nw/sw` | (-464, 433, 665) | 129mm | 201° |
| `root/nw/se` | (-141, 433, 665) | 129mm | 205° |

네 자세 모두 ee y=433mm 한 평면 위에 있고(실기 티칭 평면), 보드 여유는 충분하다. 그러나
**v12 `root/nw`(J1=144°) 와 어깨 브랜치가 반대**(J1≈-36\~-61°)라 분면 자세에서 세부 자세로 직행하면
약 200° 재배치가 된다. overview 에서 가면 124\~153° 로 분면 자세와 같은 급이다.
→ 실기 자세를 쓰려면 **overview 경유**가 맞고, 분면 자세에서 내려가는 구조를 만들려면 세부 자세를
**부모 자세와 같은 브랜치에서 유도**해야 한다 (§6 시나리오 B).

---

## 6. 시뮬 시나리오 — 6-1 완료 / 6-2 구현 완료(런 10 대기)

### 6-1. 가지치기 — ✅ 2026-09-10 반영·검증 완료

아래 1~5 를 넣고 10:13 런으로 확인했다 (`log/m3/README.md` §런 2 판정표 9/9).
로그 증거: `OVERVIEW_SCAN nw:2 ne:1 se:0 sw:3` · `TRAVERSAL_PRUNED skip=['root/se']` ·
`TRANSIT_VIA_OVERVIEW root/ne -> root/sw` · `root/sw/se:1 nw:1 ne:1` · `PICK COMPLETE` 6건 · 364.9→240.6초.

1. `scan_executor_node` 에 `overview_prescan` 파라미터 (기본 false = 실기 보존, 시뮬은 true).
   트리거 직후 `root=SCANNING` 셀 상태를 발행하고(fake_vision 필터 해제, 2회차 런의 잔존 필터 문제도 해결)
   overview 에서 `scene_positions` 를 모아 분면별로 센다. 0개 분면은 순회에서 뺀다.
   상태 문자열: `OVERVIEW_SCAN nw:2 ne:1 se:0 sw:3`, `TRAVERSAL_PRUNED skip=[root/se]`.
   `pick_pose` 가 아니라 `scene_positions` 를 쓰는 이유가 근거 ① 그 자체다.
2. 순회 순서는 실기 그대로 nw, ne, se, sw(`_ALL_CELLS_CLOCKWISE_ORDER`). 가지치기는 0개 분면을 **빼기만** 하고 남은 순서는 그대로다. 개수순 재정렬은 하지 않는다(`HARVEST_PRIORITY_ORDER` 는 상태 로그일 뿐). 기준은 §8 순서 Q.
3. 가지치기가 만드는 **비인접 이동 쌍**의 안전. 관절공간 직선(MoveJoint 폴백) 여유를 20개 순서쌍 전부 FK 로 계산했다 (보드 810, v12 자세):

| 이동 쌍 | 최대 스윙 | 보드 여유 |
|---|---|---|
| 순차 5쌍 | 95~165° | 181mm 이상 |
| nw ↔ se | 143.5° | **37mm** (50mm 기준 미달) |
| ne → sw | **233.9°** | 94mm |
| overview ↔ 어느 분면이든 | 95~151° | 202mm 이상 |

   **MoveJoint 는 비인접 쌍에서 overview 를 경유**하도록 분기한다. (cuRobo 경유 `plan_scan_transit` 는
   MotionGen 이 만들어지지 않아 한 번도 실행된 적 없는 죽은 경로였고, 09-10 에 기본 false 로 바꿨다 — §8 마지막 Q.)
4. `_group_poses_by_subcell` 중심선을 부모 분면 경계로 (§5 결함).
5. dwell: `-p scan_dwell_sec:=3.0` (§7).

### 6-2. 적응 분할 — ✅ 2026-09-11 구현 (계획서 T4b), ✅ 2026-09-12 런 10 실행 검증

**런 10 결과 (09-12, 익은 8/4 배치, `subdivide_min_candidates:=3`)**

| 분면 | 1차 스캔 후보 | 판정 | 세부 자세 | 결과 |
|---|---|---|---|---|
| se | 0 | 가지치기 (`TRAVERSAL_PRUNED`) | — | 방문 없음 |
| nw | 3 | **분할** (`SUBDIVIDE root/nw cells=['sw','se']`) | sw 칸 수락(Δ32.1°) / **se 칸 거부(Δ66.3° > 60, `SUBDIVIDE_REJECTED`)** → 부모 자세로 복귀해 pick | 3/3 파지 |
| ne | 2 | 잎 (`SUBDIVIDE_SKIP 2 < 3`) | — | 부모 자세에서 2/2 파지 |
| sw | 3 | **분할** (`cells=['se','nw','ne']`, sw 칸은 후보 0 → 2단 가지치기) | 3칸 전부 수락(Δ25.4 / 33.6 / 26.2°) | 세부 자세에서 3/3 파지 |

전체 283.0초(런 9 는 6과실 217.2초). 세부 칸 4곳 방문(유도·이동 ~0.6초 + dwell 3초)과 부모 복귀·재스캔을 합친 분할 오버헤드는 약 22초, 전체의 8%.
런 11(09-12 02:13)도 같은 트리로 돌았다. 오프라인 예측(nw·ne 위쪽 칸은 IK 밖, sw 는 4/4)과 런 결과가 일치한다.

**시뮬에서 분할의 이득은 측정되지 않는다 — 이걸 먼저 말한다.** 시뮬의 좌표는 USD 정답값이라 가까이 가도 인식이 좋아질 것이 없다.
실제로 같은 과실의 접근 계획 시도 수는 분할 전후가 같다:

| 과실 | 런 9 (부모 자세에서, 분할 끔) 접근 계획 실패/성공 | 런 10 (세부 자세에서) | 런 11 |
|---|---|---|---|
| ripe_01 (nw) | 2 / 2 | 2 / 2 | 2 / 2 |
| ripe_02 (nw) | 4 / 3 | 4 / 3 (부모 퇴화) | 4 / 3 |
| ripe_04·05·06 (sw) | 0 / 2 씩 | 0 / 2 씩 | 0 / 2 씩 |

파지 판정 결과도 전부 같다(런 11 의 ripe_08 미파지는 접근 IK 편차, 분할과 무관). 그래서 시뮬이 검증한 것은 **이득이 아니라 메커니즘과 비용**이다 —
"후보 밀도로 쪼갤지 정하고, 부모 자세에서 세부 자세를 유도하고, 유도가 안 되면 부모 자세로 퇴화하는 코드가 실기 노드 위에서 끝까지 돈다", 그리고 "그 값은 런당 약 22초와 거부 1건".
이득이 생기는 자리는 실기의 인식(가림·군집에서 재스캔)이고, 그건 H §1 판정 기준상 시뮬 범위 밖이다.

**09-14 뒤에는 측정할 것이 하나 생겼다.** ① 자세에서는 시야가 세부 칸으로 좁아져 플래너에 등록되는 이웃 장애물이 줄고 파지 시작 거리가 짧아진다.
이건 "가까이 가서 좁게 본다"는 물리 모델의 결과이지 인식 개선이 아니다. 효과는 런 12 와 T4d 배치 변경 런(배치별 기록, 비율 아님)에서 본다 —
**재기 전에는 "분할로 좋아졌다"고 말하지 않는다.**

**구현 결과 (09-11, 09-14 보강)**: 아래 B 안 — `execution/subcell_pose.py`(계산부) + `scan_executor_node.py`
(`subdivide_min_candidates`, 기본 0 = 끔 = 실기). 계획기(MotionGen)가 아니라 **IKSolver 만** 만들고 세부 이동은 실기와 같은 **MoveJoint** 다
(관절공간 직선 보드 여유를 FK 로 사전 계산).

**09-14 정정 — 깊이 2 는 깊이 1 보다 보드에 가깝다.** 실기 NW 깊이 2 티칭 자세 4개는 ee y=433mm 평면(보드에서 377mm)에 있고,
v12 분면 자세는 y 317\~339(보드에서 470\~490mm)다. 즉 실기는 깊이 2 에서 약 100mm 더 가까이 갔다. 09-11 유도 규칙은 "y 는 부모와 같게"
두어 이 성질을 빠뜨렸고, 09-14 에 유도를 **사다리**로 바꿨다: ① 실기 평면 433 으로 x·y·z 이동(`lab_plane`) → ② 안 되면 부모 y 유지·x·z 만(`parent_y`)
→ ③ 안 되면 부모 자세 pick. 시뮬 비전은 **①에서만** 그 세부 칸으로 시야를 좁힌다(`VIEWING` 상태, `subcell_view_enabled`) — 거리가 같은 ②·③에서
시야를 좁히면 물리적으로 거짓이기 때문이다. 깊이 3 은 만들지 않는다(실기 데이터가 깊이 2 까지).

오프라인(`log/m3/offline_checks/subcell_poses_tiered_20260914.txt`, 한도 60°): 16칸 중 **① 9칸**(nw sw·se, ne sw, se 4칸, sw sw·se·nw),
**② 2칸**(ne/se, sw/ne), **IK 밖 4칸**(nw·ne 의 위쪽 — 두 단계 모두 팔 길이 밖), 보드 여유 최소 68mm. 카메라(D455, USD 오프셋 툴 축 앞 63mm)와 보드의
거리는 분면 자세 412\~472mm, ① 자세 318\~319mm. **09-15 확인(`docs/d455_min_range.md`)**: 실기 카메라 `rs-enumerate-devices` 로그의
베이스라인 95.03mm·초점거리로 인텔 산식(MinZ = Fx×B/126)을 풀면 MinZ 는 1280×720 496mm / 848×480 328mm / 640×480 297mm / 640×360 248mm —
① 자세의 카메라-과실 약 292mm 는 848×480 이면 **밖**, 640×480 이하면 경계\~안. 로그에 실제 사용 해상도·disparity shift 가 없어
**실기 설정은 미확인**이고, `fake_vision` 엔 거리 모델이 없어 이 축에서 시뮬이 원본보다 관대하다(한계로 기록, 규칙 0) —
면접에서 "실기가 그 거리에서 봤다"고 말하지 않는다.
오프라인 검사 `scripts/check_subcell_scan_poses.py [--ee-y 0 | --lab-fk]`, 근거 `PLANNER_CHANGES.md` 09-11·09-14.

**규칙**: 분면 근거리 스캔의 중복 제거 후보가 `subdivide_min_candidates` (기본 0 = 끔, 시뮬 3) 이상이면
그 분면을 4칸으로 쪼개고, **후보가 있는 칸만** 세부 자세로 내려가 다시 스캔·pick 한다.

**왜 밀도인가**: 실기가 NW 를 쪼갠 이유는 가림·군집이었고, 시뮬에는 가림 모델이 없다. 군집의 관찰 가능한
대리 지표가 후보 수다. "가림"이라고 말하지 않고 "밀집"이라고 말한다.

**세부 자세를 어디서 얻나 — 두 안:**

| 안 | 방법 | 장점 | 단점 |
|---|---|---|---|
| A. 실기 NW 티칭 자세 | `SUBCELLS_DEG` 4개를 YAML 에 복원, 군집을 NW 에 배치 | 실기 데이터 그대로 (sim2real 1차 출처 규칙) | NW 한정. v12 분면 자세와 브랜치가 반대라 overview 경유 필요 |
| **B. 부모 자세에서 유도** (권장) | 부모 관절의 FK 로 ee 자세를 얻고, x/z 만 세부 칸 중심으로 평행이동(같은 y·같은 자세)한 목표를 `_plan`(MotionGen) 으로 계획. 부모 대비 관절 변화 60° 초과면 `SUBDIVIDE_REJECTED` 로 포기하고 부모 자세에서 pick | 같은 브랜치라 작은 이동, 새 좌표 하드코딩 아님 (계산값). 적용 범위는 **아래 분면(se·sw) 4/4, 위 분면(nw·ne)은 아래 칸 1개씩 — 16칸 중 10칸**, 안 되는 칸은 부모 자세로 퇴화 (09-11 검사. 계획 당시 "어느 분면이든 됨"으로 적었던 것을 정정) | 실기 티칭값이 아니라는 점을 문서에 적어야 함 |

부수 변경: `quadrant_filter.quadrant_from_cell_id` 가 3단 이름(`root/sw/nw`)을 부모로 뭉개는 현재 동작을
파라미터로 풀어 세부 칸 단위로 좁힌다 (카메라가 가까워지면 시야가 좁아지는 것과 같다).
등록되는 이웃 장애물도 줄어드니 그 점은 한계로 적는다.

**시연 레이아웃**: 09-10 02:02 런의 배치(nw 2 / ne 1 / se 0 / sw 3)가 이미 세 동작을 한 런에 보여준다.
se 는 **가지치기**, nw·ne 는 **잎(leaf)**, sw 는 **분할**. 레이아웃을 바꿀 필요가 없다.
HUD 트리 패널(09-11 구현, 화면 확인은 런 10)이 가지치기·잎·분할을 실행 중에 그린다 — 제외된 분면은 흐리게, 분할한 분면 아래로 세부 칸 줄이 열리고, 로봇이 있는 노드가 하늘색으로 켜진다(09-16 까지 주황; 보드 테두리와 같은 색).

### 6-3. 면접에서 적응 분할을 말하는 순서

1. "쿼드트리의 적응성은 모든 칸을 같은 깊이로 쪼개지 않는 데 있다. 실기 팀은 가림이 심한 NW 한 곳에만
   깊이 2 자세를 티칭했고, 나머지는 깊이 1 로 충분했다. 쪼갤지는 사람이 오프라인에서 정했다."
2. "시뮬에서는 그 판정을 후보 밀도 규칙으로 런타임에 올렸고, 세부 자세는 부모 자세와 같은 관절 브랜치에서 유도한다.
   실기 NW 티칭 자세를 FK 로 확인하니 분면 자세와 어깨 브랜치가 반대여서 그대로 쓰면 200° 재배치가 나오기 때문이다.
   런 10 에서 nw·sw 가 분할되고 ne 는 잎, se 는 가지치기로 남았다. 세부 자세 5칸 중 4칸은 됐고 1칸은 관절 변화 66° 로 거부돼 부모 자세로 퇴화했다."
3. **먼저 꺼내는 한계**: "시뮬에서 분할의 이득은 측정되지 않았다. 좌표가 정답값이라 가까이 가도 인식이 좋아질 게 없고,
   실제로 접근 계획 시도 수가 분할 전후 같다. 시뮬이 보인 건 이 메커니즘이 실기 노드 위에서 끝까지 돈다는 것과 그 비용, 런당 약 22초와 거부 1건이다.
   이득은 실기의 인식 단계에서 나고, 그건 시뮬 범위 밖이다."
4. "깊이 2 자세는 실기 티칭 평면(보드에서 377mm)으로 내려간다. 실기 자세 4개를 FK 해서 얻은 값이라 새 좌표가 아니다. 안 되면 부모 거리에서
   평행이동, 그것도 안 되면 부모 자세다. 16칸 중 9칸이 실기 평면, 2칸이 부모 거리, 위쪽 4칸은 팔 길이 밖이다. 시야는 실제로 가까워진 자세에서만
   좁힌다. 임계값 3은 시연 배치에서 정했지만, 배치를 바꿔 다섯 번 돌리니 분할이 se / ne / nw·se / sw / ne·sw 로 매번 다른 분면에서 났다. 세부 칸 18개는 실기 평면 12 · 부모 거리 3 · IK 해 없음 퇴화 3 이었다. 후보가 몰린 분면이면 어디든 쪼갠다." (09-15 T4d, `log/m3/random/README.md`)

되물으면 — **"그럼 왜 넣었나?"** → 실기 원안(PROJECT_GOAL §3)이 1차 스캔 → 세부 영역 2차 스캔의 두 단계였고, 실기 최종본은 그걸 티칭 자세 하나로 대신했다.
시뮬은 그 설계를 런타임 판정으로 되돌려 실기 노드가 그 구조를 감당하는지 본 것이다. 이득 측정은 인식이 있는 실기에서 할 일이고, 그 자리(세부 자세 재스캔)가 어디인지는 코드로 보여준다.

**순서 복원과 분할이 실기 노드 모순을 드러낸 사례 (09-17)** — 순회 순서를 원 팀 기록(민1 STEP 6, nw → ne → se → sw)으로 되돌리고 적응 분할을 얹은 시퀀스가, 실행기와 플래너의 J6 창 불일치를 픽 시작 때 플랜지 한 바퀴 회전으로 꺼냈다. 순서·분할을 물으면 이 문구 그대로 답한다:

> "원 팀 코드는 시연용으로 sw 부터 돌았고, 그 순서에선 손목이 창 안에 머물고 한 분면만 수확했습니다. 시뮬에서 순서를 원 팀 기록대로 nw 부터 되돌리고 분할을 더하자, 실행기와 플래너가 J6 한계를 다르게 보는 모순이 동작으로 나왔습니다."

"손목이 창 안"은 티칭 관절값으로 한 계산, "한 분면만 수확"은 PROJECT_GOAL §1-2 구술 관찰이다. 원인·수정·되물음은 H §11.

---

## 7. "시간이 오래 걸린다" 에 대한 사실

구간별 시간 (`log/m3/*/scan_executor.log` 타임스탬프). **왼쪽이 가지치기 전(02:02), 오른쪽이 후(10:13)** 다:

| 구간 | 02:02 | 10:13 | 비고 |
|---|---|---|---|
| overview 1차 스캔 | — | 3.0초 | 이동 없음 (이미 overview) |
| 분면 간 이동 | 4.5초 (4회) | 4.4초 (3회, sw 는 overview 경유 2.0초) | 실기와 같은 급. 느리지 않다 |
| 재스캔 이동 | 0.1초 × 3 | 0.1초 × 3 | pick 이 scan pose 로 복귀해 끝나므로 거의 0 |
| 스캔 자세 dwell | **12.0초 × 7 = 84초** | 3.0초 × 6 = 18초 | `scan_dwell_sec` |
| pick 6건 | 214초 | 214.3초 | 건당 35.7초. **두 런이 같다** |
| overview 복귀 | 0.8초 | 0.8초 | |
| **전체** | **364.9초** | **240.6초** | |

"영역 이동이 느리다"는 체감은 **도착 후 12초 정지**다. 실기 dwell 12초는 fusion 노드의 다중 프레임
안정화(§4) 때문이고, fake_vision 은 2Hz 결정적 좌표라 그 시간이 필요 없다.

| 대책 | 비용 | 효과 |
|---|---|---|
| `run_nodes.sh` 에 `-p scan_dwell_sec:=3.0` | 0줄 (실기 기본 12 유지) | ✅ 적용 — 84초 → 18초 |
| 조기 종료 조건 일반화 — 중복 제거 후보 집합이 K 회 연속 같으면 종료, `scan_dwell_sec` 은 상한 | 약 15줄, 새 파라미터 기본 0=끔 | 실기에도 적용 가능한 형태 |

---

## 8. 예상 질문

**Q. 그냥 한 번 다 보고 딸기 있는 데로 가면 되지 않나?**
→ 멀리서는 "있다"만 알고 "어디를 잡는가"는 모른다 (근거 ①). 가까이 가서 다시 봐야 하고, 가까이서는 한 자세로
보드를 못 담으니 나눠야 한다. 나눈 칸의 자세가 파지 시작 브랜치까지 정한다 (근거 ②).

**Q. 1차 스캔에서 놓친 분면은 영영 안 가는 것 아닌가?**
→ 맞다. 모든 가지치기의 recall 트레이드오프다. 대응은 상위 판정을 보수적으로 잡는 것 — 줄기 탐지가 아니라
낮은 임계의 세그 존재 여부로 자른다. 시뮬은 비전을 완벽하게 두므로 이 문제를 모델링하지 않는다고 명시한다.

**Q. 4칸 고정이면 그리드지 쿼드트리가 아니지 않나?**
→ 깊이 1 만 보면 맞다. 트리 성질은 가지치기와 적응 분할에서 드러난다 (근거 ③). 실기는 NW 만 깊이 2 로 내려갔다.

**Q. 같은 목표점인데 출발 자세에 따라 왜 실패하나?**
→ 출발 자세가 팔꿈치를 거의 다 편 특이점 근처면 더 뻗을 여유가 없다. 실기 NW 분면 자세는 시드 128개로 IK 해도 |J3| ≤ 1.2° 해뿐이었고,
거기서 출발하면 보드면 30mm 앞에서 `IK_FAIL` 이었다. 같은 목표를 팔꿈치를 굽힌 세부 칸 중심 자세(J3≈63°)에서 출발시키면 보드면까지 계획됐다 (§2 ②).

**Q. 세부 칸 자세에서 시작하면 반드시 해가 나오나?**
→ 보장은 없다. 도달 여부는 팔 길이·관절 한계·손끝 방향 제약·보드 충돌이 정한다. 칸을 나누면 출발점과 목표가 가까워져 관절 변화가 작아지니
같은 브랜치에 머물 가능성이 올라갈 뿐이다(실기 NW 174\~205° → 18\~36°). 09-11 검사에서도 16칸 중 10칸만 됐다 — 위 분면(nw·ne)의 위쪽 칸 4개는
"부모와 같은 거리·방향" 규칙으로 IK 해가 없고, 2개는 관절 변화가 한도 60° 를 넘었다(66.3°, 70.4°). 실기 NW 세부 자세도 분면 자세와 어깨 브랜치가
반대라 바로 가면 약 200° 재배치다(§5). 그래서 **보장 대신 검사와 퇴화**로 설계했다: 세부 자세마다 IK 해·관절 변화 ≤60°·이동 중 보드 여유를
확인하고, 하나라도 안 되면 `SUBDIVIDE_REJECTED` 로 그 칸만 부모 자세에서 pick 한다. 최악의 경우가 "분할하지 않은 것"과 같다.

**Q. 분면·세부 칸은 어떤 순서로 방문하나? 기준은?**
→ 깊이마다 기준이 다르고, 둘 다 원 팀 것을 그대로 쓴다. 이 프로젝트가 새로 정한 순서는 없다.

| | 깊이 1 (분면) | 깊이 2 (세부 칸) |
|---|---|---|
| 순서 | nw → ne → se → sw | sw → se → nw → ne |
| 코드 | `scan_executor_node._ALL_CELLS_CLOCKWISE_ORDER` | `_group_poses_by_subcell` 의 고정 목록 → `_subdivide_and_pick` 이 그 순서로 돈다 |
| 출처 | 원 팀 실기 기록 `~/민1` STEP 6 · `~/민2` §7 — 분면 간 직접 이동(`INTER_CELL_DIRECT`)으로 4셀 순회 실기 검증. 그 전 세대 스냅샷도 같은 순서, 최종 세대 `_baseline` 만 데모용 sw 시작 | 원 팀 최종 세대 실행기 docstring (`_baseline/.../scan_executor_node.py` 같은 함수). 그 전 세대 스냅샷(`~/바탕화면/strawberry_grasp_isaac/docs/scan_executor_node.py`, 리포 밖)은 깊이 2 도 nw → ne → se → sw 였다 — 최종 세대에서 lower-first 로 바뀌었고 이유는 docstring 에만 있다 |
| 기록된 이유 | **없다.** 모양은 왼쪽 위에서 시작하는 시계 방향이라 이동이 모두 옆 분면이다 — 이건 관찰이지 원 팀 근거가 아니다 | "아래쪽 먼저(lower-first)": 아래 탐지는 꼭지/KP1 높이에 가깝고, 위 탐지는 잎·꽃받침·가지가 걸린 어려운 경우가 많다 |
| 건너뛰기 | overview 1차 스캔에서 익은 과실 0개 분면(`TRAVERSAL_PRUNED`, 시뮬 `overview_prescan:=true`) | 후보 0개 칸(`SUBCELL_EMPTY … 2단 가지치기`). 세부 자세 유도 실패 칸은 순서는 그대로 두고 부모 자세에서 pick |
| 칸 안의 과실 | — | `(x, z)` 오름차순 — x 가 작은 것 먼저, 같으면 낮은 것 먼저 |

동적 순서(이동 거리 최소·개수순)는 없다. 가지치기는 빼기만 하고 남은 순서를 바꾸지 않는다.
09-09 에 깊이 1 을 원본 데모 순서(sw → nw_flat → ne → se, 이것도 시계 방향)에서 원 팀 기록 순서로 되돌렸다.
원본 상수 이름 `_ALL_CELLS_ZORDER` 는 틀린 이름이라(Z-order 면 nw → ne → sw → se) 09-17 에 `_ALL_CELLS_CLOCKWISE_ORDER` 로 바꿨다. 동작은 같다.

**Q. 실기에서 가지치기를 했나?**
→ 안 했다. §3 의 한 문장. 감추지 않는다.

**Q. 왜 `scene_positions` 로 자르고 `pick_pose` 로 안 자르나?**
→ 실기에서 `pick_pose` 는 줄기 키포인트가 안정화돼야 나오고, 그건 먼 거리에서 안 된다. 그걸로 자르면 전부 잘린다.

**Q. 분면 이동에 cuRobo 를 안 쓰나? 보드 충돌은?**
→ 안 쓴다. `plan_scan_transit` 파라미터가 true 로 켜져 있었지만 계획기 객체가 미리보기 옵션 경로에서만 생성돼
한 번도 실행된 적이 없었다 — 두 런 로그에 `cuRobo 경유` 0건. 분면 자세를 실기 v12 티칭값으로 되돌린 뒤
순차 이동의 관절공간 직선 보드여유를 FK 로 20쌍 전부 훑으니 최소 181mm 였고, 가지치기로 생기는 비인접 쌍만
overview 경유로 막았다. 켜져 있지만 죽어 있는 안전망을 로그 대조로 찾아 기본값을 false 로 정직하게 돌렸다.

**Q. 시간이 절약되나?**
→ 가지치기 자체로 준 것은 분면 하나(se) 의 이동 1.1초 + dwell 13초다. 크지 않다.
전체 364.9→240.6초 중 대부분은 시뮬 dwell 단축분이고, 그건 쿼드트리와 무관하다.
시간은 근거가 아니라 부산물이다 (§2 끝).

---

## 9. 쓰지 말아야 할 표현

- "실기에서 쿼드트리 가지치기가 동작했다" — 안 했다.
- "적응 분할이 실기 런타임에 있었다" — 자세만 있었고 판정은 오프라인.
- "적응 분할이 런에서 검증됐다" — 런 10 뒤에는 **"메커니즘이 런에서 돌았다"**까지만. "분할로 파지·인식이 좋아졌다"는 금지 — 시뮬에서 이득은 측정되지 않았다(§6-2 표).
- "적응 분할로 성공률이 올랐다" / "분할 덕에 잡았다" — 접근 계획 시도 수·파지 결과가 분할 전후 동일하다.
- "무작위 배치 N 회에서 분할 발생률 X%" — 09-15 개정으로 비율·신뢰구간은 보고하지 않는다. 배치별 표로 "배치 k 에서 분면 Y 분할, 단계 Z"까지만.
- "실기가 깊이 2 자세(카메라-보드 319mm)에서 인식했다" — 티칭 자세가 그 평면에 있다는 것까지만. D455 MinZ 는 해상도에 따라 248~496mm
  (`docs/d455_min_range.md`), 실기 사용 해상도·disparity shift 미확인 (§6-2).
- "세부 자세는 부모와 같은 거리" — 09-14 부터 아니다. ① 실기 평면 ② 부모 거리 ③ 부모 자세의 사다리다.
- "세부 자세 유도가 어느 분면이든 된다" — nw·ne 위쪽 칸은 IK 밖이다. "아래 분면에서 되고, 안 되는 칸은 부모 자세로 퇴화한다"가 맞다.
- "가림(occlusion) 을 시뮬에서 재현했다" — 밀도 규칙이지 가림 모델이 아니다.
- "픽셀 크기 1.9mm" 같은 근사치를 단정 — 경향으로만.
- ①(인식)·②(관절 자세)를 "트리라서 가능한 것"으로 — 그리드여도 성립한다. 트리 전용은 ③(가지치기·적응 분할)뿐이다 (§2 머리).
- 근거 ①을 "정확도"라는 이름으로 — "레벨마다 얻는 정보가 다르다(해상도)"로 (§2 ①).
- "Z-order 로 순회한다" — 시계 방향이다. 원본 상수 이름 `_ALL_CELLS_ZORDER` 가 틀렸던 것이다 (§8 순서 Q, 09-17 이름 변경).
- "이동 거리가 최소가 되도록 순서를 정했다" / "원 팀이 인접 이동이라서 시계 방향을 골랐다" — 계산한 적 없고 기록에도 이유가 없다. **"원 팀 실기 기록의 순서를 그대로 썼다"**까지만.
- "깊이 2 도 같은 순서" — 깊이 2 는 아래쪽 먼저 sw → se → nw → ne 다.
- 기존 금지 목록(`SUBMISSION_PLAN.md` §7) 은 그대로 유효.

---

## 근거 파일

- 실기 시퀀스 원본: [`_baseline/A_strawberry_motion/execution/scan_executor_node.py`](../_baseline/A_strawberry_motion/execution/scan_executor_node.py)
- 현재 시퀀스: [`src/strawberry_motion/strawberry_motion/execution/scan_executor_node.py`](../src/strawberry_motion/strawberry_motion/execution/scan_executor_node.py) (`_scan_sequence`, `_compute_scan_order`, `_group_poses_by_subcell`)
- 분면 자세 YAML: [`src/strawberry_motion/config/scan_pose_candidates_refit_candidate.yaml`](../src/strawberry_motion/config/scan_pose_candidates_refit_candidate.yaml)
- 실기 NW 세부 자세: [`_baseline/A_strawberry_motion/scripts/compute_nw_pick_ready_pose.py`](../_baseline/A_strawberry_motion/scripts/compute_nw_pick_ready_pose.py)
- 인식 4단계·두 채널: [`src/strawberry_motion/scripts/strawberry_fusion_node.py`](../src/strawberry_motion/scripts/strawberry_fusion_node.py) (`_publish_scene_positions`, `_log_perception_summary`, `_px_to_3d`)
- 시뮬 분면 필터: [`src/strawberry_sim_core/strawberry_sim_core/quadrant_filter.py`](../src/strawberry_sim_core/strawberry_sim_core/quadrant_filter.py)
- 원안 시퀀스: [`PROJECT_GOAL.md`](../PROJECT_GOAL.md) §1-2
- 런 로그·판정표: [`log/m3/README.md`](../log/m3/README.md) — 02:02(전수 순회) / 10:13(가지치기) 2회분
