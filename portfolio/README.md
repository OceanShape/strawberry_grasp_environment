# portfolio/ — 제출용 산출물

[`SUBMISSION_PLAN.md`](../SUBMISSION_PLAN.md) §3 의 산출물 A(영상)와 `PORTFOLIO_SPRINT.md` §1 의 B~E.
**B·C·D·E는 완료**, A(영상)만 남았다. A 의 명세·순서는 SUBMISSION_PLAN 이 기준이다.
현재 위치(09-15): T1~T4d 완료(T4c 는 런 14, T4d 는 서로 다른 배치 5개 — 화면 육안은 사용자 확인) → **T5 녹화** → T6.

| # | 파일 | 상태 |
|---|---|---|
| A | 시연 영상 — 클립 4종 (A-1~A-4) | ☐ **T5 녹화** 차례 — 아래 "A 녹화 절차" |
| B | `B_architecture.svg` / `.png` | ✅ |
| C | `C_before_after.svg` / `.png` | ✅ (2026-09-10 패널 ① 문구 정정 — "0.0mm" 삭제) |
| D | `D_usd_layers.svg` / `.png` | ✅ |
| E | `E_metrics.md` | ✅ (2026-09-10 수치 갱신, 09-15 런 기록·미해결·표현 갱신. 규모 수치·결함 최종 집계는 T6) |
| F | `F_code_walkthrough.md` — **면접용 코드 워크스루** (제출물 아님) | ✅ (2026-09-10 작성) |
| G | `G_quadtree_interview.md` — **면접용 쿼드트리 설계 근거·적응 분할 시나리오** (제출물 아님) | ✅ (2026-09-10 작성; §6-1 가지치기 09-10 검증, §6-2 적응 분할 09-11 구현·**09-12 런 10 실행 검증(nw·sw 분할, 세부 자세 4/5 수락; 시뮬에서 이득은 측정 안 됨 — §6-2 표)**·09-14 깊이 2 실기 평면·09-15 런 12) |
| H | `H_scope_decisions.md` — **면접용 범위 판정: 무엇을 재현하지 않았나** (제출물 아님) | ✅ (2026-09-11 작성, `SUBMISSION_PLAN.md` §5 를 옮기고 원본 삭제; §9 실기 플래너 한계 기록 09-12·가드 원복 09-14; **09-15 §4 익은 8/미숙 4 재측정(겹침 쌍 7번째 파지), 낙하 과실은 부딪히고 그리퍼는 통과하는 이유, §9 T4c 낙하 표현**) |

- `.svg`가 원본이다. 텍스트를 고칠 일이 생기면 SVG를 열어 고친다.
- `.png`는 PPT에 바로 붙이는 용도 (3200×1800, 2배 해상도).
- PNG를 다시 만들려면:
  ```bash
  google-chrome --headless --disable-gpu --hide-scrollbars --force-device-scale-factor=2 \
    --window-size=1600,900 --screenshot=portfolio/B_architecture.png \
    --default-background-color=FFFFFFFF file://$PWD/portfolio/B_architecture.svg
  ```
- **영상 파일은 리포에 커밋하지 않는다** (용량). 링크로 참조하고, 런 로그만 `log/m3/` 에 보존한다.
- **F 는 제출 산출물이 아니다.** A~E 와 달리 PPT 에 넣지 않는다. 면접에서 "코드를 보여 달라 /
  구조를 설명해 달라"가 나왔을 때 그 자리에서 여는 자료다. T1~T6 순서를 바꾸지 않는다.
  → **면접 준비 자료를 모을 때는 [`F_code_walkthrough.md`](F_code_walkthrough.md), [`G_quadtree_interview.md`](G_quadtree_interview.md), [`H_scope_decisions.md`](H_scope_decisions.md) 를 반드시 포함한다.**

---

## 슬라이드 캡션 (그대로 써도 되는 한 줄)

**A — 시연 영상 (실물 ↔ 시뮬 대조)**
> 실기 제어 스택을 하드웨어 없이 구동하고, 같은 사건(분리·영역 이동·배치)을 실물 클립과 나란히 놓았다.
> 시뮬 쪽 접근 오차는 감추지 않았다 — 접근이 빗나가면 과실도 빗나간 채 매달린다.
> 과실 배치를 바꾼 런(A-5)도 같은 코드가 끝까지 돈다. 닿지 않는 과실은 건너뛰고, 이송이 거부되면 떨어진다 — 그대로 보인다.

**B — 아키텍처**
> 실기 파이프라인과 시뮬 파이프라인은 **양 끝단(비전 입력·로봇 제어 출력)만 다르다.**
> 경계를 ROS 2 인터페이스 계약으로 잡았기 때문에, 가운데 모션 플래너 2노드는 상대가
> 실물인지 시뮬인지 알지 못한 채 동작한다.

**C — 문제 해결 Before/After**
> 통합 테스트가 실제로 잡아낸 결함 4건. 모든 수치는 실행 로그 또는 USD·URDF 실측값이며
> 출처를 패널마다 적었다. ④는 기하 검증까지 끝났고 실행 검증은 남아 있다.

**D — USD 레이어 구조**
> 임포트·순정 애셋은 읽기 전용으로 두고 커스터마이징은 전부 상위 레이어의 오버라이드로 처리했다.
> URDF를 재빌드해 임포트 산출물이 통째로 교체돼도 수정 내역이 살아남는다.

**E — 정량 지표**
> 숫자마다 출처와 재생성 명령을 붙였다. 면접에서 되짚어 물어도 그 자리에서 열어 보일 수 있다.

---

## 권장 슬라이드 순서

1. 프로젝트 한 줄 정의 + 기술 스택 (`PROJECT_SUMMARY.md` §1·§2)
2. **B** 아키텍처 — "무엇을 만들었나"
3. **A** 시연 영상 — "실제로 돈다" (A-4 전체 수확 → A-1/A-2/A-3 실물 대조 쌍)
4. **C** Before/After — "어떻게 디버깅했나" (이 프로젝트에서 가장 강한 슬라이드)
5. **D** USD 레이어 — "협업 가능한 구조로 만들었나"
6. **E** 정량 지표 + 잔여 이슈 — "정직하게 안다"
7. (부록) 발표 영상 링크 — 전체를 대조하지 않는다. 발표 영상에는 시뮬에 없는 병든 딸기 분기가 있다

> 4번이 핵심이다. 시뮬레이션 포트폴리오에서 변별력은 "돌아간다"가 아니라
> **"안 돌아갈 때 원인을 어떻게 좁혔나"** 에서 나온다.

---

## A 클립 체크리스트 (SUBMISSION_PLAN §3)

| # | 클립 | 실물 소스 | 시뮬 소스 (카메라) | 우선순위 | 확보 |
|---|---|---|---|---|---|
| A-1 | **딸기 분리** 쌍 (5~10초) | 부트캠프 단계별 클립(직선 접근·분리) | **클로즈업**, 45mm 직선 진입 구간 | **필수** | ☐ 실물 / ☐ 시뮬 |
| A-2 | **영역 이동** 쌍 (5~10초) | 발표 영상 구간 컷 | **롱샷**, 분면 전환(nw→ne) | 높음 | ☐ 실물 / ☐ 시뮬 |
| A-3 | **딸기 배치** 쌍 (5~10초) | 발표 영상 구간 컷 | **롱샷**, release 구간 | **높음** | ☐ 실물 / ☐ 시뮬 |
| A-4 | **시뮬 전체 수확** (30~60초) | — | 롱샷 메인 + 파지 순간 클로즈업 인서트 | **필수** | ☐ |
| A-5 | **배치 변경 런** (20~40초) | — | **롱샷**, T4d 파일럿 시드(기본 4)를 적용한 배치로 한 런 전체 | **높음** (09-15 추가) | ☐ |

- 실물 쪽은 **발표 영상 전체를 기준 삼지 않는다.** 구간만 잘라 쓴다. 발표 영상은 부록에 링크만.
- A-4 는 **시연 배치**(메커니즘 4가지가 한 런에 다 나오고 계란판이 그 착지에 맞춰져 있다), A-5 는 **배치 변경 런**이다 — "배치가 달라도 같은 코드가 끝까지 돈다"의 영상 증거. 건너뛴 과실·낙하·계란판 착지 어긋남을 자르지 않는다(SUBMISSION_PLAN T5).
- A-3 는 시연 배치 T5 런(실기 원본 값, 익은 8, 슬롯 `0,1,3,4,6,7,9,10`)의 트레이 안 배치 구간으로 찍는다. 원본 값 기준 배치는 8개 중 4개이고, A-4 에서는 이송 거부로 떨어지는 과실도 자르지 않는다(SUBMISSION_PLAN §4-0 T5).

---

## A 녹화 절차 — 카메라 2대, 런 3회 (시연 배치 2 + 배치 변경 1)

**선행 조건**: [`SUBMISSION_PLAN.md`](../SUBMISSION_PLAN.md) §4-0 의 **T4c·T4d 는 09-15 에 끝났다.** 배치 런 뒤라 Isaac 은 마지막 무작위 배치를 들고 있다 — **녹화 전 씬 재로드 필수**(파일은 시연 배치로 복원돼 있다).
T1(재완주)·T2(딸기 부착)·T3(덩굴)·T4(계란판)·T4b(적응 분할)는 09-10~12 에 끝났다(`log/m3/` 런 1~11).
T4c 는 런 13 결함(바닥 관통·노드 무재시작)을 고친 뒤 런 14 로 완료 기준을 채웠다. **녹화 런은 매번 `run_nodes.sh --kill` 뒤 새로 띄운다**(재트리거하면 트레이 슬롯 포인터가 이어진다).

클로즈업은 별도 내용이 아니라 **같은 사건의 다른 카메라**다. 같은 씬·같은 파라미터로 런을 두 번 돌리고,
카메라만 바꿔 찍은 뒤 편집에서 합친다.

| 런 | 뷰포트 카메라 | 담을 것 | 되는 클립 |
|---|---|---|---|
| 1 | **롱샷 고정** — 로봇 전신 + 보드 4분면 + 계란판이 한 프레임에 | 분면 전환, 이송·release, 전체 흐름 | A-2, A-3, A-4 메인 |
| 2 | **클로즈업** — 그리퍼·타겟에 붙여서 | 접근 정밀도, 줄기가 물리는 순간, 분리, HUD 판정 | A-1, A-4 인서트 |
| 3 | **롱샷 고정**, 배치는 T4d 시드 4 (`gen_random_layout.py --seed 4 --apply`) | 다른 배치에서 가지치기·분할·파지·배치·낙하가 도는 전체 흐름, 건너뛴 과실 포함 | A-5 |

런 1·2 는 시연 배치, 런 3 은 배치 변경이다. 런 3 앞뒤로 배치 적용·복원과 씬 재로드가 한 번씩 더 들어간다(아래 2번).

### 런마다 하는 일

1. **남은 노드부터 확인한다** — 옛 노드가 살아 있으면 코드로 안 보이는 오동작이 난다:
   ```bash
   ps -ef | grep -E "fake_vision|sim_executor_bridge|curobo_planner|scan_executor|status_monitor" | grep -v grep
   ```
   하나라도 나오면 [`docs/run_guide.md`](../docs/run_guide.md) "전체 종료 / 재실행 전 초기화" 를 먼저 한다.
2. **씬을 다시 로드한다** — USD 를 고쳤으면 노드 재시작만으로는 반영되지 않는다.
   **런 3(배치 변경)**: 재로드 전에 `python3 strawberry_harvest/scripts/scene_tools/gen_random_layout.py --seed 4 --apply`. 런 3 이 끝나면 `--restore` 하고 `git diff --stat strawberry_harvest/scenes/layers/` 가 비었는지 확인한다. 런 순서를 3 → 1 → 2 로 해도 되지만, 어느 쪽이든 배치가 바뀔 때마다 재로드다.
3. [`docs/run_guide.md`](../docs/run_guide.md) 순서대로 **T1(Isaac: 브릿지 + HUD) → T2 → T3 → T4** 를 띄운다.
   시뮬 전용 파라미터는 run_guide 의 T3 명령에 이미 들어 있다 (`ee_to_tcp_offset_m:=0.236`, `enable_open_stem_descent:=true`).
   T2(브릿지)에는 `tool_tcp_offset_m` 을 **지정하지 않는다** (기본값 0.236 이 T3 와 한 쌍).
4. **기동 로그 두 줄을 눈으로 대조한다** (안 보이면 파라미터가 안 먹은 것):
   - T3 플래너: `EE_TO_TCP_OFFSET_OVERRIDE: 160mm -> 236mm`
   - T2 브릿지: `GRASP_JUDGE_MODEL: tool_tcp_offset=236mm ...`
   두 값이 **같아야** 한다. 다르면 딸기를 제대로 잡아도 전부 빈손 판정이 난다. 나머지 대조 줄은 run_guide "기동 직후 눈으로 대조할 5줄".
5. 뷰포트 카메라를 이 런의 앵글로 맞추고, 화면 녹화를 시작한 뒤 **T5 트리거** (`/strawberry/scan/start`).
6. 확인할 것: `clamped` WARN 없음, `JOINT_COMMAND_REJECTED` 없음, 화면상 관통 없음, HUD 단계 표시가 진행에 맞게 바뀜, HUD 트리 노드가 순회 순서대로 켜짐(제외·분할·세부 칸).
7. 끝나면 로그를 `log/m3/` 로 보존한다 (`~/.ros/log/` 의 최신 파일을 노드별로 이름 붙여 복사).
   `log/m2_5/README.md` 와 같은 형식으로 런 요약 한 장을 같이 남기면 C·E 갱신(T6)이 쉽다.
   롱샷 런과 클로즈업 런은 하위 폴더로 나눈다 (`log/m3/run1_long/`, `log/m3/run2_close/`, 배치 변경 런은 `log/m3/run3_layout_seed4/`).

### 편집

- A-4: 롱샷 런을 메인으로 30~60초. 파지 순간마다 클로즈업 런의 같은 구간을 인서트. 자막 불필요, HUD 로 단계가 보인다.
- A-1/A-2/A-3: 시뮬 구간(5~10초)과 실물 클립을 나란히. 같은 사건, 같은 길이로 맞춘다.
- A-5: 런 3 롱샷을 20~40초로. 건너뛴 과실(`ABORT` 뒤 다음 과실로 넘어가는 장면), 낙하, 계란판 착지 어긋남을 자르지 않는다. 자막 없이 HUD 트리로 분할 분면이 시연 배치와 다른 것이 보이면 된다.
- 영상은 리포 밖에 두고 링크만 문서에 적는다.
