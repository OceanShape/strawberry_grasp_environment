# E — 정량 지표 (이력서·PPT용)

> **원칙**: 숫자마다 출처를 적는다. 출처를 댈 수 없는 숫자는 쓰지 않는다.
> 재생성 방법이 있는 항목은 명령어를 함께 적었다.
> 기준일 **2026-09-10** (규모 수치는 2026-09-09 재계측, 1차 출처 `PROGRESS_REPORT.md` §5).
> **END-TO-END 완주 횟수와 결함 최종 집계는 09-09 수정본 재완주(`SUBMISSION_PLAN.md` T1) 뒤 T6 에서 확정한다.**

---

## 1. 규모

| 지표 | 값 | 출처 / 재생성 |
|---|---|---|
| 빌드되는 ROS 2 패키지 | **5개** | `ls build/` — `strawberry_motion`, `strawberry_sim_core`, `e0509_gripper_description`, `dsr_gripper_tcp_interfaces`, `dsr_msgs2` |
| 이식한 실기 플래너 | **파이썬 64파일 / 15,975줄** (09-07 집계 61/15,256 에서 신설 모듈 3개 증가) | `find src/strawberry_motion -name '*.py' \| wc -l` / `-exec cat {} + \| wc -l` |
| 직접 구현한 가상 제어기 (ROS 측) | **10파일 / 1,532줄** | `src/strawberry_sim_core/` |
| 직접 구현한 Isaac Sim 스크립트 | **8파일 / 1,736줄** (HUD 5파일 912줄 포함) | `strawberry_harvest/scripts/` |
| USD 애셋·씬 파일 | **19개** | `find strawberry_harvest -name '*.usd' \| wc -l` |
| 씬의 딸기 prim | **12개** (익은 8 / 안 익은 4 — 2026-09-11 unripe 2개 전환, 위치 불변; 런 1~9 는 6/6) | `scenes/layers/layout_layer.usd` |
| 프로젝트 문서 | **16종** (`docs/` 8 + 루트 8) | `ls docs/*.md *.md` |

> ⚠️ **혼동 주의**: 로그의 `TARGET_FOUND ... 72 pick candidate(s)`는 **딸기 개수가 아니라**
> 스캔 프레임마다 누적된 후보 검출 건수다. 딸기는 12개다.

## 2. 이식 변경 추적

| 지표 | 값 | 출처 |
|---|---|---|
| 원본 스냅샷 대비 diff | **변경 592줄** (경로 치환 제외 **492줄**, diff 전체 1,169줄) — 2026-09-10 재생성 | `bash scripts/check_planner.sh` → `evidence/planner_diff.txt` |
| 수정 로그 항목 | **101건** | `PLANNER_CHANGES.md` (한 줄 형식, `PLANNER_POLICY_v2.md` §4) |
| 감사 대상 == 실행 대상 | **src ↔ install 사본 2개 일치** | `check_planner.sh`의 정합 검사 |
| 파라미터 정합 검사 | 보드 y 등 **6개 출처** 기계 대조 (실제로 2곳 누락을 잡음) | `python3 check_params.py` |

> 이식 변경은 **원본 스냅샷(`_baseline/`) 대비 diff로 언제든 재생성**된다.
> "무엇을 왜 바꿨는지"를 면접에서 즉시 열어 보일 수 있다.
> 실기 기본 동작은 파라미터 기본값으로 보존했다 (`ee_to_tcp_offset_m`, `enable_open_stem_descent` 는 시뮬에서만 켠다).

## 3. 정확도 · 검증

| 지표 | 값 | 출처 |
|---|---|---|
| MoveLine IK 해의 FK 검증 오차 | **0.003 mm** | `docs/next_steps.md` (cuRobo 실연산 검증) |
| FK→IK 왕복 오차 | **~1 µm**, 근방 5포즈 배치 IK **5/5** | `docs/next_steps.md` |
| elbow-flip 가드 IK 검증 (1차 방어) | **60회** 통과, 실전 다이브 0건 | `docs/next_steps.md`, `log/m1/` |
| elbow-flip **근본 원인** 수정 효과 (`seed_config` 오용) | 45mm 직선 진입 **6/6 실패 → 6/6 통과**, 스텝당 관절 이동 **1°** | `PLANNER_CHANGES.md` 2026-09-08 |
| 씬 기하 ↔ 플래너 벽 모델 | **131.7 mm 불일치 규명·정합** → `clamped` WARN **0건** (09-07 런) | `log/m1/curobo_planner.log:14` → `log/m2_5/curobo_planner.log` |
| 보드 평면 | 672~729 mm(3° 기울기) → **균일 평면**. 기준값은 09-09 에 원본 씬 실측 **810.0 mm** 로 복원, 6개 출처 정합 | `docs/parameters.md`, `check_params.py` |
| 과실 뒷면~보드 간격 | 매립 −22.2 mm → **+5.0 mm 돌출(균일)** | `docs/parameters.md` |
| 툴 길이 모델 | 160 mm 가정 → 실측 파지부 **+233.3 mm** → `ee_to_tcp_offset_m` **236 mm** (기하 검증 완료, 실행 검증 대기) | `PLANNER_CHANGES.md` 2026-09-07·09-08 |

> ⛔ **"접근 오차 131.7 → 0.0 mm" 는 쓰지 않는다.** 09-07 까지의 문서에 있던 표현이지만, "672 mm = 실기 실측값" 판정에 1차 출처가 없어
> 09-09 에 보드 기준을 810 mm 로 되돌렸다. 사실로 남는 것은 **불일치를 규명해 정합했고 clamp WARN 이 사라졌다**는 것이다.

## 4. 시퀀스 성적

| 지표 | 값 | 출처 |
|---|---|---|
| END-TO-END 시퀀스 완주 | **5회** (2026-07-10 / 07-16 / 08-29 ×2 / 09-07) | `log/m1/`, `log/m2_5/` |
| 2026-09-07 런 결과 (입증된 최고 성적) | 익은 딸기 **6개 시도 → 4개** 파지·이송·release 완주 | `log/m2_5/README.md` |
| 실패 2건의 사유 | 직선 진입 IK 실패 1 / 도달 범위 밖(`ripe_03`, 당시 x=+350mm) 1 | `log/m2_5/curobo_planner.log` |
| 09-09 정합 수정본 재완주 | **❌ 미확인** — 09-09 17:23 런은 nw 스캔 이동 `ARM_ARRIVAL_TIMEOUT`(관절 오차 130.9°) | `SUBMISSION_PLAN.md` T1 |
| 파지 판정 거리 (`d_tcp`, 당시 기준) | **13.5 ~ 28.9 mm** (판정 반경 35 mm). 09-09 부터는 조우 물림 구간(along/lateral) 기준 | `log/m2_5/sim_executor_bridge.log` |
| MoveLine 관절 이동량 | 실행된 해 **5.1 ~ 34.6°** (상한 45°), **176.1°** 해는 거부 | 〃 |
| 배치 슬롯 | 4회 모두 **slot 0** (`taught_slot_sequence=[0,…]`), slot≥1 대상 계획은 J6 spline jump 359° 로 거부 | `log/m2_5/curobo_planner.log:10,55,175~181` |
| 안전 게이트 | fail-closed **6단계** 거부 조건을 전부 정상 경로로 통과 | `scan_executor_node._start_cb` + `scan_safety.motion_start_allowed` |

## 5. 결함 처리

**수정 완료 — 09-07 집계 17건** (분류는 "무엇을 고쳤나"가 아니라 "어디를 고쳤나" 기준)

| # | 결함 | 고친 곳 |
|---|---|---|
| 1 | MoveLine 텐서 flatten 누락 → Exception | 가상 제어기 |
| 2 | MoveLine fail-open (실패해도 success=True) | 가상 제어기 |
| 3 | MoveLine TOOL(ref=1) 좌표계 미지원 | 가상 제어기 |
| 4 | IK elbow-flip 다이브 (1차: 최근접 해 + 45° 가드) | 가상 제어기 |
| 5 | 파지 판정이 항상 빈손 | 가상 제어기 |
| 6 | 브릿지/플래너 툴 오프셋 불일치 → 전 타겟 빈손 | 가상 제어기 |
| 7 | D455 중첩 강체 → articulation 초기화 실패 | USD 오버라이드 |
| 8 | 드라이브 게인 damping≈0 → 관절 출렁임·떨림 | USD 오버라이드 |
| 9 | 씬 기하 불일치 → 접근 오차 131.7 mm | USD 오버라이드 |
| 10 | 딸기가 보드에 매립 (파지 공간 없음) | USD 오버라이드 |
| 11 | 줄기 파단력 2 N (자중의 10.2배) → 스치면 끊김 | USD 오버라이드 |
| 12 | 스캔 재실행 불가 (`_started` 래치 미해제) | 플래너 |
| 13 | place 실패가 런 전체를 잠그는 fail-closed 래치 | 플래너 |
| 14 | 툴 길이 모델 73 mm 부족 → 과실·보드 관통 | 플래너 파라미터 |
| 15 | 커스텀 3D 프린팅 파츠가 충돌 모델에 없음 | cuRobo 설정 |
| 16 | cuRobo 충돌 월드가 사실상 비어 있음 | cuRobo 설정 |
| 17 | place 경로가 파라미터로 꺼져 있음 (증상: 파지 후 복귀만) | 실행 파라미터 |

**추가 규명·수정 — 2026-09-08~09 (대표 11건, 재완주 전이라 상태를 표기. 최종 집계는 T6)**

| # | 결함 | 고친 곳 | 검증 |
|---|---|---|---|
| 18 | 시뮬 그리퍼가 한 번도 움직이지 않음 (`set_position` 이 숫자만 저장, Isaac 스크립트는 위치 인덱싱) | 가상 제어기 + Isaac 스크립트 | ✅ 헤드리스 |
| 19 | open-stem descent 가 로드 불가 프로파일(`grasp_tcp_link` 부재)에 묶여 사문화 | 플래너 파라미터 분리 (`enable_open_stem_descent`) | ✅ 헤드리스 6/6 |
| 20 | IK elbow-flip **근본 원인** — `solve_single` 에 현재 관절을 `seed_config` 가 아닌 `retract_config` 자리로 | 가상 제어기 | ✅ 6/6 |
| 21 | 그리퍼 "가위 모양" — 평행사변형 관절쌍 상한 불일치 (1.1 vs 1.0 rad) | URDF + USD 오버라이드 | ✅ |
| 22 | J6 wrap 263° 점프 | 가상 제어기 | ✅ |
| 23 | MoveSpline deg/rad 혼용 → 모든 관절 57.3배 명령 (로그에 흔적 없음) + 한계 초과 발행 차단 | 가상 제어기 | ✅ |
| 24 | 분면 순회에서 nw 가 조용히 빠짐 (`root/nw_flat` 별칭이 YAML 에 없음) | 플래너 (`cell_traversal.py`) | 🔶 |
| 25 | 보드 y 가 6개 출처에 분산, 2곳 누락 → 딸기 앞을 집음 | 설정 + `check_params.py` | 🔶 |
| 26 | 목표 딸기가 자기 자신을 장애물로 등록 (부동소수점 경계) | 플래너 | 🔶 |
| 27 | 브릿지가 모션 종료 전 즉시 응답 → 잔차가 다음 상대 MoveLine 에 전파 | 가상 제어기 (도착 대기, 실행시간 하한) | 🔶 |
| 28 | 측정값을 명령으로 되실어 복귀 모션을 얼림 (J6 누적 오차) | 가상 제어기 | 🔶 |

**미해결 · 범위 결정** (전부 회피책 또는 결정 근거와 함께 문서화)

| 이슈 | 회피책 / 결정 |
|---|---|
| 09-09 정합 수정본 END-TO-END 재완주 미확인 | `SUBMISSION_PLAN.md` T1 (블로커) |
| 트레이 slot 1 이상에서 J6 한계 초과 | `taught_slot_sequence:="0,0,…"`로 slot 0 고정. T4 에서 URDF·cuRobo J6 한계 vs 실기 사양 대조 |
| 둘째 타겟 J1/J2 swing reject | 2026-09-07 런에서는 재현 안 됨 (미규명, 제출 후). 09-12 같은 계열(스윙 가드)을 아래 행에서 규명 |
| 트레이 이송 계획이 스윙 가드에 거부 → 그 자리 릴리스 (런 10 ripe_08 J3 186°, 런 11 ripe_04 J3 198°, 간헐적) | **실기 플래너 한계로 기록, 수정 안 함** (09-12 결정). 목표 IK 분기를 시작 자세와 무관하게 고르고 거부 뒤 재시도가 없다. 오프라인 재현, 실기 티칭 격자에서도 동일. 09-14 스윙 가드 3값 원복(최근 3런 결과 불변). 근거·판정 `H_scope_decisions.md` §9 |
| 실기 요청 가속도가 두산 공식 MoveIt 설정을 넘는다 — 스캔 MoveJ 180 °/s² (두산 120), cuRobo 계획 한계 573 °/s² (두산 120~225) | **실기 노드 기록 사항, 수정 안 함** (09-14 D3). 시뮬 브릿지는 자르지 않고 그대로 실행하며 `DOOSAN_MOVEIT_REF`·`MOVEJ_OVER_DOOSAN_MOVEIT` 로그로 초과만 남긴다. 두산 하드웨어 가속도 상한은 비공개 (`docs/e0509_spec_audit.md` §6·D3) |
| 그리퍼 l2/r2 링크에 cuRobo 충돌 구체 없음 | 커스텀 파츠 구체로 부분 대체 |
| 커스텀 파츠에 물리 콜라이더 없음 | 계획 단계에서 차단 (물리 최후 안전망은 부재) |
| 딸기가 그리퍼를 따라 이동하지 않음 (화면상 분리·배치 안 보임) | `SUBMISSION_PLAN.md` T2 키네마틱 부착 |
| 병든 과실 폐기 분기 | **검증 범위에서 제외 — 수확 핵심 경로 아님** (범위 결정, `H_scope_decisions.md` §2) |
| ~~`ripe_03`(x=+350 mm) 도달 불가~~ | 09-08 에 (200, 740) 으로 이동, IK 재검증 통과 |

**환경·의존성 문제 4건** — cuRobo 0.7.8 ↔ warp-lang 1.10+ 비호환, colcon ↔ setuptools 80+ 비호환,
rosidl의 비ASCII(한글) 경로 버그, cuRobo 가 설정 yml 을 ASCII 로 읽는 경로(한글 주석으로 기동 실패).
각각 원인 규명 후 버전 핀·경로 이전·주석 영어화로 해결하고 문서화.

## 6. 기술 스택 (버전 고정값)

Isaac Sim **5.1.0** · ROS 2 **Humble** · cuRobo **v0.7.8**(소스 빌드) · PyTorch **2.6.0+cu124** ·
CUDA **12.4** · warp-lang **1.9.1**(핀) · Python **3.10**(ROS) / **3.11**(Isaac Sim) · Ubuntu **22.04** · RTX 4060 Ti

---

## 쓰면 안 되는 표현 (자기 검열용 — `PLANNER_POLICY_v2.md` §5.1·§5.4)

- ❌ "실기 플래너 0줄 수정" / "무수정 구동" / "원본 노드를 그대로 사용" → **"이식(port)"** 을 쓴다
- ❌ "딸기 24개 / 72개" → 씬의 딸기는 **12개**, 나머지는 후보 검출 건수
- ❌ "파지 물리 검증" / "파지 성공" / "수확 성공" → 검증한 것은 **판정 로직과 시퀀스**다 (접촉 물리는 범위 밖)
- ❌ "전체 타겟 수확" → 익은 딸기 6개 중 **4개** 완주
- ❌ "접근 오차 131.7 → 0.0 mm" → **"131.7 mm 불일치 규명 및 정합(clamp WARN 소멸)"**
- ❌ "전체 시퀀스 재현" → **"수확 핵심 경로 완주"** (병든 딸기 분기는 범위 밖)
- ❌ "병든 과실 선별 / 솎아내기" → 쓰지 않는다. 잔여 이슈에 범위 결정으로만
- ⚠️ "관통 없이" → **T1 재완주 성공 후에만**
- ⚠️ "파지 후 과실이 그리퍼를 따라 이동·배치" → **T2 완료 후에만**
