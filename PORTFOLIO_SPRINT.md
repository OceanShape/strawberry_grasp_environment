# PORTFOLIO_SPRINT.md — 포트폴리오 제출용 마무리 스프린트

> **담당: 마일스톤 이력과 판정 기준.**
> **제출 범위·작업 순서는 2026-09-10 부터 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md), 착수 금지 목록과 근거는 2026-09-11 부터 [`portfolio/H_scope_decisions.md`](portfolio/H_scope_decisions.md) §2 가 기준이다.**
> 이 문서와 충돌하면 그쪽이 우선한다. 수치는 [`docs/parameters.md`](docs/parameters.md), 실행은 [`docs/run_guide.md`](docs/run_guide.md),
> 변경 근거는 [`PLANNER_CHANGES.md`](PLANNER_CHANGES.md) 가 기준이다.
>
> **지금 할 일 (SUBMISSION_PLAN §4 순서)**: **T1** 09-09 수정본 END-TO-END 재완주(블로커, Step 0 = 09-09 17:23 런 로그부터 읽기)
> → T2 딸기 키네마틱 부착 → T3 정적 덩굴 메쉬 → T4 계란판 + slot≥1 조사 → T5 녹화(카메라 2대) → T6 수치 갱신·푸시 → (선택) T7.

> **이 문서의 목적**: 이력서 제출용 포트폴리오 PPT를 만드는 데 필요한 최소 작업만 정의한다.
> 프로젝트 완결이 목적이 아니다. **제출 가능한 상태**가 목적이다.
>
> **참조 문서**: `PROJECT_SUMMARY.md` (현재 상태 스냅샷, 2026-08-29 기준)
> **마지막 개발 세션**: 2026-07-16 (약 6주 공백 후 재개)
> **작성일**: 2026-08-29

---

## 0. 작업 원칙 (반드시 먼저 읽을 것)

1. **범위를 늘리지 않는다.** 아래 "범위 밖" 목록에 있는 것은 아이디어가 떠올라도 하지 않는다. 별도 파일에 적어두고 넘어간다.
2. **완료 조건이 명시되지 않은 작업은 시작하지 않는다.** 각 마일스톤에는 판정 기준이 붙어 있다. 기준을 만족하면 더 손대지 말고 다음으로 간다.
3. **완료 기준을 채우기 전에는 다음으로 넘어가지 않는다.** 시간 한도로 중단하거나 대체물로 넘기지 않는다 (2026-09-11 계획 변경).
4. **M3가 끝나면 제출한다.** M4는 제출 이후의 일이다.
5. 각 세션 종료 시 이 문서 맨 아래 `## 진행 기록`에 한 줄 추가한다.
6. **플래너 수정은 [`PLANNER_POLICY_v2.md`](PLANNER_POLICY_v2.md)를 따른다 — 전면 허용, 사전 승인 불필요.**
   하드코딩·안전검사 완화·실패 분기 우회 모두 허용된다. 이번 스프린트에서 하드코딩은 기술 부채가 아니라 정당한 도구다.
   대신 `PLANNER_CHANGES.md`에 **한 줄** 남기고, **밖으로 나가는 문서에 "0줄 수정/무수정"을 쓰지 않는다**(v2 §5).
7. **판단 기준은 "이 작업이 영상 완성을 앞당기는가?" 하나다.** 아니면 하지 않는다.

---

## 0.5 스프린트 단일 목표 (v2 §1)

> **딸기를 탐지하고, 접근하고, 파지하고, 지정 위치에 배치하는 수확 핵심 경로가
> 끊김·오류 없이 완주하는 Isaac Sim 영상 1편을 확보한다.**

이 목표에 기여하지 않는 작업은 범위 밖이다. 애매하면 **"이 작업이 영상 완성을 앞당기는가?"**만 묻는다.

> **2026-09-10 확정** ([`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) §0·§1): 제출본이 주장하는 것은
> *"실기 제어 스택을 하드웨어 없이 구동하고, 실기와 시뮬의 불일치를 찾아 정합했다"* 한 문장이다.
> 이 문장을 강화하지 않는 작업은 전부 범위 밖이며, 새 작업 여부는 §2 **sim2real 게이트**로 판정한다.
> **종료 조건**: 09-09 수정본 END-TO-END 재완주 로그 1건 + 시뮬 영상 1편 + 실물 대조 클립쌍 3종 + 산출물 수치 갱신 + 원격 푸시.
> 원안의 병든 딸기 분기(C·D)는 **범위 밖으로 확정**했다(§3 참고). "전체 시퀀스"가 아니라 **"수확 핵심 경로"**라고 쓴다.

**v2 완료 판정** (`PLANNER_POLICY_v2.md` §6):
- [x] 스캔 → 탐지 → 계획 → 접근 → 파지 → **배치** → 복귀 전 구간 무오류 완주 —
      2026-09-07 16:44 런 (`log/m2_5/`). ⚠️ 단 툴 길이 모델 수정 **전** 상태라 화면상 관통이 남아 있다
- [x] **최소 2개 타겟 연속 처리** — 같은 런에서 **4개** 연속 파지·배치 완주
- [ ] 영상 녹화본 확보 — **카메라 2대(롱샷/클로즈업) × 런 1회씩**, 클립 명세는 SUBMISSION_PLAN §3 ← 🙋 **T1 재완주가 선행 조건**
- [x] `PLANNER_CHANGES.md` 한 줄 로그 최신화 — 101건 (2026-09-09, `bash scripts/check_planner.sh`로 diff 재생성)
- [x] 포트폴리오/이력서/경력기술서에서 §5.1 금지 표현 전수 검색 후 제거 — **2026-09-07 완료**

> 시퀀스·판정·배치는 09-07 런으로 입증됐다. 그 뒤 09-08~09 정합 수정본(변경 로그 약 60건)은 **아직 재완주 로그가 없다** —
> 09-09 17:23 런이 첫 분면(nw) 스캔 이동에서 `ARM_ARRIVAL_TIMEOUT`(관절 오차 130.9°)으로 중단됐다.
> 따라서 지금의 첫 과제는 녹화가 아니라 **T1 재완주**다 (SUBMISSION_PLAN §4 T1 Step 0·1).

---

## 1. 최종 산출물 정의 (여기서 역산한다)

포트폴리오 PPT에 들어갈 것. **이것들이 확보되면 스프린트 종료.**

| # | 산출물 | 용도 | 상태 |
|---|---|---|---|
| A | END-TO-END 시연 영상 (30~60초) | PPT 핵심. 스캔→탐지→접근→파지→**배치**→복귀 | ☐ 🙋 사용자 녹화 필요 |
| B | 아키텍처 다이어그램 (실기 vs 시뮬 대조) | 이식 경계·가상 제어기 설계를 한 장으로 설명 | ✅ `portfolio/B_architecture.svg` |
| C | 문제 해결 Before/After 시각자료 **4건** | 접근 오차 / elbow-flip 다이브 / 파지 판정 / 툴 길이 모델 | ✅ `portfolio/C_before_after.svg` |
| D | USD 레이어 구조도 | 순정 애셋을 고치지 않고 오버라이드 레이어로만 수정하는 원칙 설명 | ✅ `portfolio/D_usd_layers.svg` |
| E | 정량 지표 정리 | 패키지·버그·FK 오차·시퀀스 성적 — 전부 로그/실측 출처 표기 | ✅ `portfolio/E_metrics.md` |

> B~E는 `portfolio/`에 파일로 있다. 캡션과 PPT 배치 순서는 [`portfolio/README.md`](portfolio/README.md) 참고.

> A는 대체 불가다. 영상 없는 시뮬레이션 포트폴리오는 설득력이 절반이다.
> **A 의 명세는 2026-09-10 에 확정했다** ([`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) §3): 실물 대조 클립쌍 3종(A-1 분리 / A-2 영역 이동 / A-3 배치)
> + 시뮬 전체 수확 30~60초(A-4). 실물 쪽은 발표 영상 전체가 아니라 **구간만** 잘라 쓴다(발표 영상에는 시뮬에 없는 병든 딸기 분기가 있다).
> C는 스크린샷·로그 캡처로 충분하다. 영상 편집까지 갈 필요 없다.
> **A에 배치(place)까지 포함하는 것이 M2.5의 목적이다.** "수확"이라는 단어를 쓰는 이상
> 파지 후 놓는 데까지 가야 설득력이 산다. M2.5가 막히면 A는 파지·복귀까지로 후퇴한다.

---

## 2. 마일스톤

### M1 — 3차 통합 테스트 (예상: 반나절)

**목적**: elbow-flip 가드와 fail-closed 재설계가 반영된 상태에서 파이프라인이 여전히 완주하는지 확인.

**작업**
- 브릿지 재시작 후 기존 문서화된 테스트 절차 그대로 실행
- 4개 노드 + Isaac Sim 동시 기동, 6단계 안전 게이트 정상 통과 확인
- 실행 로그 전체 보존 (산출물 C·E의 근거가 된다)

**완료 판정 — ✅ M1 완료 (2026-08-29, 2회 실행)**
- [x] scan/start 트리거로 시퀀스가 중단 없이 복귀까지 도달 — 2회 모두 `SCAN_COMPLETE` (런 A: 관찰용, 런 B: 화면 녹화)
- [x] elbow-flip 다이브 재발 없음 — 가드가 근접 해를 찾으면 실행(관절 이동 5.8~33.7°, 상한 45° 이내), 먼 브랜치 해만 나오면 거부하고 `success=False` → planner 정상 중단. 두 경로 모두 실전 확인
- [x] 로그 파일 저장 완료 — `log/m1/` 6종 (노드 4종 + 트리거 응답, 런 2회분)

**신규 관찰 (원인 기록만, 조치는 M2 이후):** cuRobo 계획의 무작위성으로 런마다 pre-approach 종료 자세가 달라짐(J4=272° vs 92°) → 45mm 접근 성공 여부가 런마다 다름 (런 A: 타겟1 거부·타겟2 성공 / 런 B: 타겟1 성공·타겟2는 J1/J2 swing으로 전 후보 reject — 7/10 known issue 재현, M4 항목). 런 B 녹화본에 타겟1 전체 픽 사이클(접근→45mm 직선 진입→파지→당겨빼기) 포함 — **산출물 A 소재 확보**.

**실패 시**: 새 버그가 나오면 원인만 기록하고 M2로 넘어간다. 완주 자체는 이미 2회 입증됐으므로 3차가 막혀도 포트폴리오는 만들 수 있다.

---

### M2 — 접근 오차 해소 (예상: 1~2일)

> **⚠️ 2026-09-10 주의 — 아래는 09-05~07 당시 기록이다.** "672mm = 실기 실측값" 판정에는 1차 출처가 없었음이 09-08 에 확인돼,
> 09-09 에 보드 기준 y 를 **원본 씬 실측값 810.0mm 로 복원**하고 6개 출처를 `check_params.py` 로 정합했다 ([`docs/parameters.md`](docs/parameters.md)).
> 따라서 아래의 **"오차 0.0mm" 는 더 이상 쓰지 않는다** — 밖으로 나가는 표현은 "131.7mm 불일치 규명 및 정합(clamp WARN 소멸)" 이다
> (`PLANNER_POLICY_v2.md` §5.4). 이 절은 M2 가 어떻게 진행됐는지의 이력으로만 남긴다.

**목적**: 그리퍼가 딸기 132mm 앞에서 멈추는 문제 해결. 이게 되면 "시퀀스 완주"가 "목표 도달"로 올라간다.

**원인 (규명 완료, 2026-09-05 판정 정정)**

`harvest_motion_params.py:62`의 `WALL_SURFACE_Y_M = 0.672`가 **실기 벽의 실측값**이고,
우리가 만든 시뮬 씬이 딸기를 y≈804mm에 배치한 것이 **씬 제작상의 불일치**다.

> ⚠️ **이전 판정 정정**: 672mm를 "실기 캘리브레이션 잔재"로 적었으나 이는 틀렸다.
> 잔재가 아니라 실기 기준값이며, 틀린 쪽은 시뮬 씬이다.

**판정: 플래너를 고치지 않고 시뮬 씬을 실기 기하에 맞춘다.**
당시 v1 정책("시뮬레이터의 부정확함을 실기 코드에 떠넘기지 않는다")에 따른 판단이었고,
결과적으로 씬을 고치는 쪽이 더 정확했으므로 v2에서도 이 조치를 유지한다.

**씬 실측 결과 (2026-09-05 완료, USD 오프라인 측정)**

| 항목 | 측정값 |
|---|---|
| 보드(whiteboard) 앞면 y | **781.5 mm** (bbox y: 781.5~838.5) |
| 딸기(ripe) 중심 y | **803.7 mm** |
| 딸기(ripe) 앞면 y / 지름 | 781.6 mm / 44.4 mm |
| 플래너 `WALL_SURFACE_Y_M` | 672.0 mm (실기 실측값) |
| **차이 (딸기중심 − 672)** | **131.7 mm** → 보고된 132mm와 일치, **원인 확정** |
| **필요한 Y 이동량** | **−109.5 mm** (보드 앞면을 672mm에 정합) |

> ⚠️ **신규 발견 — M2와 별개 문제 (2026-09-05 해소 완료)**: 딸기 앞면(781.6)이
> 보드 앞면(781.5)과 일치해 딸기 구체가 **보드에 매립**되어 있었다. 그리퍼가 감쌀 공간이 없는 상태.
> 실기에서는 딸기가 벽 앞으로 돌출한다. 벽 이동으로는 해소되지 않는 별개 문제였으므로
> **M2와 함께 처리**(사용자 승인)하여 보드 앞면에서 5mm 돌출하도록 재배치했다.

**작업**
- [x] 시뮬 씬의 딸기·벽 현재 실좌표 측정 (Isaac Sim 기동 불필요, USD CLI로 완료)
- [x] 씬 기하를 실기 기준(벽 y=672mm)에 맞춰 이동 — `layout_layer.usd` 오버라이드로 처리,
      순정 USD 애셋은 직접 고치지 않는 원칙 유지
- [x] 딸기 매립 해소 (B 항목 동시 처리)
- [x] `physics_layer.usd`의 stem joint `localPos0` 동기화
- [x] **재테스트하여 접근 오차 변화 측정** — 2026-09-07 런에서 clamp WARN 소멸 확인 (`log/m2_5/`)
- [x] 플래너를 고치지 않고 씬 수정만으로 해결

**적용 결과 (2026-09-05, USD 재측정으로 검증)**

| 항목 | 변경 전 | 변경 후 | 목표 |
|---|---|---|---|
| 보드 앞면 y | 781.5 mm | **672.0 mm** | 672.0 ✅ |
| 딸기(ripe) 중심 y | 803.7 mm | **644.8 mm** | — |
| 딸기 앞면 y | 781.6 mm | **622.6 mm** | — |
| 딸기 뒷면 ↔ 보드 간격 | **−22.2 mm (매립)** | **+5.0 mm (돌출)** | 양수 ✅ |
| 플래너 벽 모델과의 오차 | **131.7 mm** | **0.0 mm** | 0 ✅ |

- 이동량: 보드 −109.5mm / 딸기 −158.9mm (= 벽 정합 109.5 + 매립 해소 49.4)
- pre-approach 지점(벽−`PRE_APPROACH_OFFSET` 60mm = 612.0mm)에서 딸기 앞면까지 **10.6mm** —
  MoveLine 직선 진입 구간이 정상 범위에 들어옴
- 수정 파일: `layout_layer.usd`, `physics_layer.usd` **2개뿐**. 원본 애셋·플래너는 건드리지 않음
- ⚠️ 유지보수 주의: 딸기 좌표는 `layout_layer.usd`와 `physics_layer.usd`(stem joint
  `localPos0`) **양쪽에 중복 보유**된다. 한쪽만 바꾸면 fixed joint(breakForce=20N)가
  딸기를 원래 위치로 끌어당긴다. 두 파일에 주석으로 명시해 둠

**완료 판정 — 재테스트 로그에서 확인할 단 한 줄**

변경 전 (`log/m1/curobo_planner.log:14`):
```
[WARN] Detection Y=804mm > wall surface 672mm (FK calibration drift) — clamped to 672mm
```
변경 후 기대값: 딸기 중심이 **645mm**로 벽(672mm) 앞에 있으므로 **이 WARN이 아예 사라진다.**
(`Detection Y=645mm`로 찍히고 clamp 없음)

- [x] `Detection Y=...clamped to 672mm` **WARN 소멸** ← M2의 핵심 판정. 2026-09-07 16:44 런에서
      전 PICK 헤더가 `det_y=645mm`로 찍혔고 clamp WARN이 한 줄도 없다 (`log/m2_5/curobo_planner.log`)
- [x] 접근 오차 수치 기록 — 131.7mm → **0.0mm** (산출물 C Before/After에 그대로 사용)
- [x] 기존 시퀀스가 깨지지 않음 — 같은 런에서 익은 딸기 6개 시도 → **4개 파지·배치 완주**
- [x] `scripts/check_planner.sh` 재생성 가능 (v2에서 "로직 변경 0" 요건은 폐기)

> 참고: 이 WARN의 문구 "FK calibration drift"는 플래너가 원인을 **FK 드리프트로 오진**한
> 것이었다. 실제 원인은 씬 배치였다. 산출물 C에 쓰기 좋은 소재다.

**주의**: 오차가 0이 되지 않아도 된다. 132mm → 유의미한 감소면 충분하고, 남은 오차는 포트폴리오에 "잔여 이슈"로 정직하게 적는다.

---

### M2.5 — 파지 판정 정직화 + 배치 시퀀스 완주 (예상: 1일)

> **2026-09-05 추가.** 원래 M4였던 "그리퍼 파지 판정"을 승격한 것.
> 승격 근거: 막고 있는 것이 물리가 아니라 **mock의 상수 하나**임이 코드에서 확인됐고,
> place 실행 경로는 플래너에 **이미 전부 구현되어 있다**. 비용 대비 서사 이득이 크다.

**규명된 원인 (2026-09-05, 코드 확인 완료)**

| 단계 | 실제 동작 |
|---|---|
| `use_safe_grasp_action` 기본값 | `False` (`planner_bootstrap.py:102`) |
| → fallback 경로 | `set_position(700)` 호출 (`gripper_client.py:135`) |
| mock `set_position_cb` | 받은 값을 그대로 저장 → `gripper_position = 700` |
| `verify_grasp()` 리드백 | `700 >= GRASP_EMPTY_POSITION_THRESHOLD(695)` → **GRASP_EMPTY** |

딸기 유무·로봇 위치와 **무관하게 항상 빈손 판정**이다. 물리 문제가 아니다.
(반면 mock의 `safe_grasp_cb`는 670을 하드코딩 반환 → `670 <= 685` → 이미 CONTACT 통과한다.)

**배치(place)가 지금 안 도는 이유 — 2겹 (2026-09-07 확인)**

| # | 차단 지점 | 성격 |
|---|---|---|
| 1 | `enable_marker_place_sequence` 기본값 **False** (`planner_bootstrap.py:81`) | **ROS 파라미터** — 켜기만 하면 됨, 코드 수정 불필요 |
| 2 | 파지 판정이 항상 `GRASP_EMPTY` + `allow_unverified_grasp_place` 기본값 False (`:90`) | mock 수정 필요 (가상 제어기, 플래너 무관) |

> 즉 place 실행 경로는 플래너에 이미 다 있고, **파라미터로 꺼져 있을 뿐**이다.
> 1번은 플래너 코드를 건드리지 않고 `-p enable_marker_place_sequence:=true`로 해결된다.

**작업**
- [x] **씬 딸기 12개로 확장** (2026-09-07) — 익은 6 / 안 익은 6.
      익은 것만 탐지되도록 Script Editor 브릿지에 ripeness 필터 추가
      (실기 `strawberry_fusion_node`가 ripe만 발행하므로 이게 실기 동작에 맞다)
- [x] **mock 파지 판정을 기하 기반으로 변경** (2026-09-07) —
      `set_position_cb`가 명령값을 그대로 되돌리던 것을, TCP와 딸기의 실제 거리로 판정하도록 교체.
      `safe_grasp_cb`의 670 하드코딩도 제거. 가상 제어기만 수정
- [x] **place 시퀀스를 고정 taught slot 0에 release까지 완주** (2026-09-07) —
      플래너 코드 수정 없이 파라미터 4개(`enable_marker_place_sequence`,
      `use_taught_slot0_place_reference`, `execute_marker_place_release`,
      `hold_after_taught_slot0_place:=false`)로 경로를 열었고, slot 1 이상에서 J6 한계
      문제가 나 `taught_slot_sequence:="0,0,0,0,0,0"`으로 slot 0에 고정
- [x] place 실패가 런 전체를 잠그던 fail-closed 래치를 "그 자리에 놓고 다음 타겟" 으로 변경
      (`hold_on_place_failure` 기본 false)
- mock의 파지 판정을 **조우 사이 딸기 prim 거리 기반**으로 변경 (반나절)
  - 수정 대상은 `sim_executor_bridge_node.py` — **내가 만든 가상 제어기다**
  - 이렇게 하면 "접근이 틀리면 빈손이 뜬다"가 시뮬에서도 참이 되어, **M2 접근 오차 개선의 성공 지표로 재사용**된다
- place 시퀀스를 **고정 taught slot 1곳**에 release까지 완주시키기 (반나절)

**완료 판정 — ✅ M2.5 완료 (2026-09-07 16:44 런, `log/m2_5/`)**
- [x] 파지 판정이 **기하 조건으로 갈린다** — 브릿지가 `d_tcp`(TCP↔딸기 거리)를 재서
      35mm 이내면 670(접촉), 밖이면 700(빈손)을 리드백. 실측 `d_tcp=13.5~28.9mm → CONTACT`
      ⚠️ 이 런에서는 4/4가 CONTACT라 **EMPTY로 갈리는 사례는 아직 관찰되지 않았다**
      (판정식은 분기하나 실행 증거는 CONTACT 쪽만 있다)
- [x] **딸기 4개**에 대해 파지 → 이동 → release까지 중단 없이 도달
      (`TAUGHT_TRAY_SLOT0_PLACE_RELEASE` ×4, `PICK COMPLETE` ×4)
- [x] 실행 로그 보존 — `log/m2_5/` 5종 + 런 요약 `log/m2_5/README.md`

> ⚠️ **이 런의 한계**: 기동 로그에 `EE_TO_TCP_OFFSET_OVERRIDE`가 없다 → 툴 길이 모델이
> 아직 160mm이던, 즉 **관통 3원인을 고치기 전** 상태의 런이다. 시퀀스·판정 로직은 전부
> 정상이었지만 화면상 관통이 남아 있으므로 **영상 소재로는 재실행분을 써야 한다.**

**범위 한정 — 아래는 하지 않는다**
- ❌ 접촉력·딸기 변형·슬립 등 **파지 접촉 물리** (3절 참조, 영구 범위 밖)
- ❌ ArUco 마커를 씬에 추가한 트레이 비전 위치추정
- ❌ 다중 타겟 연속 수확 (그건 M4의 J1/J2 swing 항목)

**알려진 리스크**: place 경로가 `TAUGHT_SLOT0/1/3_PLACE_REFERENCE` 등 **실기 티칭 좌표**에
의존한다 (`tray_place_policy.py:14`). 시뮬 씬 좌표계에서 이 값이 유효한지는 미검증.
**막히면 marker/taught 경로를 포기하고 "고정 자세에서 release"로 후퇴한다** — 영상 소재로는 충분하다.

---

### M2.7 — 노드↔시뮬 시퀀스 정합 (Stage 1~4, 2026-09-08 신설)

> **왜 생겼나**: 관통·튕김·이상한 파지점이 전부 **"노드는 A를 가정하는데 시뮬은 B"** 한 가지
> 원인에서 나왔다. 개별 증상을 땜질하지 말고 시뮬을 노드 시퀀스에 맞춘다.

**노드가 설계한 수확 시퀀스** (코드로 확인 완료):

| # | 단계 | 코드 근거 |
|---|---|---|
| 1 | 그리퍼를 **stroke 600(살짝 열림)** 으로 열고 딸기 앞으로 이동 | `open_for_stem_descent()` — `pick_sequence_executor.py:801`, 수평 진입 **전** |
| 2 | **+Y 수평 진입** (줄기 위 30mm 높이에서) | final approach + `CRANE_Z_OFFSET_M=30mm` |
| 3 | 열린 조우로 **BASE −Z 하강**해 KP1 정렬 | `execute_open_stem_descent_if_needed()` |
| 4 | **stroke 700 으로 닫아** 줄기 파지 | `close_and_verify_grasp()` → `set_position(700)` |
| 5 | **BASE(월드) −Z 40mm 당겨** 줄기 분리 | `execute_pitch_detach()`, MoveLine `[0,0,−40,0,0,0]` `ref=0` `mode=1` |
| 6 | 리드백 ≤685 (조우가 덜 닫힘) 이면 배치 진행 | `allow_place_after_grasp()` |

> **stroke 규약**: `0 = 열림, 700 = 완전 닫힘`, 관절각 = `stroke/700` rad
> ([`gripper_joint_publisher.py:51`](../src/strawberry_motion/scripts/gripper_joint_publisher.py:51)).
> 따라서 `GRIPPER_APPROACH_POS = 600` 은 **85.7% 닫힌 "살짝 열린" 상태**다.

#### Stage 1 — 그리퍼를 실제로 구동 ✅ (2026-09-08 완료)

종전에는 `set_position` 이 숫자만 저장하고 끝나 **시뮬 그리퍼가 한 번도 움직이지 않았다.**
씬의 초기 개도(거의 완전 닫힘)가 런 내내 유지됐고, cuRobo 는 완전 열림으로 계획해
툴 길이가 27.5mm 어긋나 보드를 관통했다.

- 브릿지가 `stroke/700` rad 로 그리퍼 4축(`rh_p12_rn`, `rh_r2`, `rh_l1`, `rh_l2`)을 발행
- Isaac 스크립트를 **이름 기반 DOF 매핑**으로 교체 (종전 위치 인덱싱은 그리퍼 지정 불가)
- 씬 초기 개도 63°/57.3° → **49.11° (stroke 600)**
- cuRobo `lock_joints` 0.0 → **0.857** (계획 전제를 접근 개도에 일치)
- `ee_to_tcp_offset_m` 208 → **236mm**, 브릿지 `tool_tcp_offset_m` 도 동일

#### Stage 2 — open-stem descent 활성화 ✅ (2026-09-08 완료)

이 단계는 원래 `measured_tcp_260mm` 프로파일에 묶여 있었는데, **그 프로파일은 로드 자체가
안 된다** (yml 의 `ee_link: "grasp_tcp_link"` 가 URDF 에 없는 링크). 그래서 실행에 쓰는
`legacy_160mm` 에서는 시퀀스 3단계가 죽어 있었다. 툴 모델과 무관한 동작이므로 분리했다.

- 신규 파라미터 **`enable_open_stem_descent`** (기본 false = 실기 동작 불변)
- `grasp_variant_pose` 의 게이트를 `measured_tcp_model` → 호출자 플래그로
- ⚠️ `legacy_grasp_endpoint` 에도 crane offset 을 더해야 한다. 안 그러면 pre→grasp 구간이
  **대각선**이 되고, 뒤이은 하강이 한 번 더 내려가 조우가 30mm 낮게 착지한다

**검증 (익은 딸기 6/6)**: pre z = grasp z (z편차 0.002mm, 수평 진입 유지) → 하강 30mm →
최종 z 가 목표와 정확히 일치. 툴 끝 654.6mm (과실 중심 +9.8mm), 보드여유 10.5mm.

#### Stage 3 — 딸기 키네마틱 부착 = SUBMISSION_PLAN **T2** (미착수, T1 재완주 뒤에)

지금은 아무것도 딸기를 붙이지 않아 **5단계 분리도 6단계 배치도 화면에 안 보인다.**
로봇만 혼자 움직인다. 영상의 핵심이 여기다. **명세는 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) T2 가 기준**이며 요지는:

- 대상은 딸기 **루트 prim**(줄기+과육 한 덩어리). 줄기만 따로 다루지 않는다
- CONTACT 판정 시점의 `T_tcp→berry` **상대 트랜스폼을 캡처해 유지**, 매 프레임 `world = T_tcp_world · T_rel`
- **TCP 중심으로 스냅하지 않는다** — 접근이 5mm 빗나갔으면 딸기도 5mm 빗나간 채 매달려야 실기와 같다. 이 오차가 화면에 보이는 것이 검증 루프의 증거다
- release 시 해제하고 그 자리에 정지. 부착 중에는 해당 딸기 콜라이더 비활성
- **런타임 FixedJoint 생성 금지** — 조우가 콜라이더와 겹친 스텝에 "겹치지 마라"와 "붙어 있어라"가 동시에 걸려 떨림·튐이 난다 (`portfolio/H_scope_decisions.md` §2)
- 구현 위치: 부착·해제는 Isaac 스크립트(`strawberry_harvest/scripts/`). CONTACT/release 는 `sim_executor_bridge` 가 이미 `_judge_grasp()` 로
  판정하므로 **ROS 토픽으로 내보내 Isaac 스크립트가 구독**한다. 판정 로직을 Isaac 쪽에 다시 만들지 않는다
- **물리 파지는 불가능**: 조우가 완전히 닫혀도 최소 간격이 14.9mm 이고 팁 부근은 36.6~40.9mm
  인데 과실 단면은 54×62.6mm 다. 어디서도 감싸지지 않는다

#### Stage 4 — 물리 파지 (**착수 금지** — `portfolio/H_scope_decisions.md` §2 "파지 접촉 물리" 영구 범위 밖)

물리로 집으려면 대상이 **약 15~25mm 두께**이고 손가락 밑동(플랜지+120~140mm)에 와야 한다.
실기 상수 주석의 *"calibrated mock stem contact ~670"* 이 이 두께와 맞아, 실기도 굵은
모의 줄기를 썼을 가능성이 크다. 화면 결과는 Stage 3 와 거의 같은데 비현실적으로 굵은 줄기를
넣어야 하므로 **투입 대비 이득이 없다.**

---

### M3 — 포트폴리오 산출물 확보 (예상: 1일 → 2026-09-10 재산정: T1~T6 집중 4~6일)

**목적**: 1절의 A~E 확보. B~E 는 끝났고, A 는 **[`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) §3 영상 명세**로 종료 조건을 확정했다.

**A 종료 조건 (SUBMISSION_PLAN §0·§3)**

| # | 클립 | 실물 소스 | 시뮬 소스 | 우선순위 |
|---|---|---|---|---|
| A-1 | **딸기 분리** 쌍 (5~10초) | 부트캠프 단계별 클립 | 클로즈업, 45mm 직선 진입 구간 | **필수** |
| A-2 | **영역 이동** 쌍 (5~10초) | 발표 영상 구간 컷 | 롱샷, 분면 전환(nw→ne) | 높음 |
| A-3 | **딸기 배치** 쌍 (5~10초) | 발표 영상 구간 컷 | 롱샷, release 구간 | 중간 (T4 결과에 따름) |
| A-4 | **시뮬 전체 수확** (30~60초) | — | 롱샷 메인 + 파지 순간 클로즈업 인서트 | **필수** |

- 촬영은 **뷰포트 카메라 2대**(롱샷 고정 / 클로즈업), 카메라별 런 1회씩 — 같은 사건을 두 시점으로 찍어 편집에서 합친다
- 영상 파일은 커밋하지 않는다. 링크로 참조, 로그만 `log/m3/`
- **선행 조건**: T1 재완주 → (T2 부착 → T3 덩굴 → T4 계란판) → T5 녹화. 절차는 [`portfolio/README.md`](portfolio/README.md)

**작업**
- **A** ☐ : 위 표대로. 자막 불필요, HUD 단계 표시로 충분.
  절차·확인 사항은 [`portfolio/README.md`](portfolio/README.md) "A 녹화 절차"
- **B** ✅ : 실기/시뮬 파이프라인 대조 + 이식 경계 강조 — `portfolio/B_architecture.svg`
- **C** ✅ : Before/After **4건** — 접근 오차 / elbow-flip 다이브 / 파지 판정 / 툴 길이 모델.
  수치마다 로그 줄·실측 출처를 패널에 적었다 — `portfolio/C_before_after.svg`
  (원래 계획의 "관절 진동"은 로그 근거가 0.2°→0.0° 한 줄뿐이라 약해서, 근거가 두꺼운
   파지 판정·툴 길이로 교체했다. 관절 진동은 D의 오버라이드 표에 남아 있다)
- **D** ✅ : 애셋/씬/물리 레이어 관계도 + 오버라이드 4건 표 — `portfolio/D_usd_layers.svg`
- **E** ✅ : 지표마다 출처·재생성 명령 명시 — `portfolio/E_metrics.md`

**완료 판정**
- [x] B~E 파일로 존재 — `portfolio/` (SVG 원본 + PPT용 3200×1800 PNG + 지표 문서)
- [ ] **A 영상** — 클립 A-1·A-4 필수, A-2 높음, A-3 중간. 🙋 사용자 실행·녹화. 절차는 [`portfolio/README.md`](portfolio/README.md) "A 녹화 절차"
- [x] 각 자료에 한 줄 캡션 — `portfolio/README.md`에 슬라이드 캡션과 권장 순서까지 정리
- [ ] **T6** 수치 갱신(재완주 반영) + 원격 푸시 (SUBMISSION_PLAN §4 T6) — 0.0mm 표현 삭제·09-07 잔존 수치 정정은 09-10 선행 처리

**→ A + T6 가 끝나면 스프린트 종료. 서류 제출한다. 그 이후는 이 리포에서 하지 않는다.**

---

### M4 — 제출 이후 (착수 금지)

제출 전에는 손대지 않는다. 면접 일정이 잡히면 그때 우선순위를 다시 정한다.

- J1/J2 swing reject (둘째 타겟 궤적 안전 검사 탈락 → 다중 타겟 연속 수확)
- 궤적 비교·검증 리포트 자동화
- README 정비

---

## 3. 범위 밖 (하지 않는다)

- 신규 기능 추가
- 리팩터링 (동작하는 코드는 건드리지 않는다)
- ~~실기 플래너 노드 수정 금지~~ — **2026-09-06 [`PLANNER_POLICY_v2.md`](PLANNER_POLICY_v2.md)로 폐기.**
  플래너 수정은 이제 **전면 허용**이다. `PLANNER_CHANGES.md`에 한 줄만 남기면 된다
- 테스트 자동화 프레임워크 구축
- 성능 최적화
- 문서 재작성 (기존 7종으로 충분)
- **파지 접촉 물리의 사실성** (PhysX deformable 딸기, 접촉력·마찰·슬립 재현) — **영구 범위 밖**

**2026-09-10 추가 — 착수 금지, 재론하지 않는다** (근거는 [`portfolio/H_scope_decisions.md`](portfolio/H_scope_decisions.md) §2):
- **병든 딸기 에셋·솎아내기 분기 (원안 C·D)** — 지원 직군 가점 0. 검증 안 된 변경 60건 위에 새 에셋·비전 클래스·폐기 분기를 얹으면
  재완주 실패 시 원인 후보가 두 배가 된다. 실물 대조를 발표 영상 전체 → 단계별 클립으로 바꾸면서 맞출 이유도 사라졌다.
  포트폴리오 잔여 이슈에 한 줄: "병든 과실 폐기 분기는 검증 범위에서 제외 — 수확 핵심 경로 아님"
- **대량 배치 배속 영상** — 실기 셀 조건(보드 1500×900, 딸기 12개)을 벗어나면 sim2real 전제가 깨진다. 대안은 T7(위치만 바꾼 두 번째 레이아웃 1회)
- **물리 덩굴 + 자석 이탈력** — 제어 스택이 덩굴의 존재를 모른다(§2 게이트 탈락). 대안은 T3 정적 메쉬
- **비타겟 과육에 blocking 콜라이더** — 시뮬 딸기는 고정체라 "밀려남"이 아니라 **벽**이 된다. 필요하면 영상 확보 후 트리거 볼륨으로 로그만
- **런타임 FixedJoint 생성** — T2 는 kinematic 추종으로
- **M4 전체** (J1/J2 swing, 검증 리포트 자동화, README 대개편) — 제출 후
- **시뮬 전용 좌표 신규 하드코딩** — 계란판 슬롯 좌표는 플래너에 이미 있다

> **왜 파지 물리는 검증 대상이 아닌가** (2026-09-05 판단, 면접에서 그대로 답변할 것)
> 실기 그리퍼부터가 접촉력으로 판정하지 않는다. `verify_grasp()`의 기준은 순전히
> 조우 위치(`pos >= 695` 빈손 / `pos <= 685` 접촉)이고, 전류값은 `"logged only"` — 읽기만 하고
> 판정에 쓰지 않는다. 즉 실기도 "몇 N으로 쥐었나"를 모르고 조우 위치로 추론할 뿐이다.
> 따라서 시뮬이 재현할 대상은 접촉력이 아니라 **조우 위치 리드백**이며, 시뮬 경계도 거기서 끊는다.
> 소프트바디로 구현해봐야 대조할 실기 힘 데이터가 없어 **검증 불가능한 충실도**가 된다.
> 검증 못 하는 정밀도는 자산이 아니라 면접에서 찔리는 지점이다.
> "실기 판정 로직이 position 기반이므로 시뮬 경계도 position에서 끊었다"는 **방어 가능한 설계 판단**이다.

> 이 목록에 있는 작업이 필요해 보이면, 그것은 대개 M2가 막혔다는 신호다.
> 우회하지 말고 M2의 완료 판정 기준을 다시 읽는다.

---

## 4. 이력서·포트폴리오에 쓸 수 있는 표현 / 쓸 수 없는 표현

작업하면서 사실 관계가 바뀌면 이 표를 갱신할 것. 서류에 그대로 옮겨 쓴다.

### 쓸 수 있음 (현재 시점 기준)

- 실기용 모션 플래너(**64파일 · 15,975줄**, 09-09 재계측)를 **Isaac Sim 환경으로 이식**해 수확 핵심 경로 완주
- 가상 dsr_msgs2 서비스 서버를 구현해 **실기 제어 스택을 하드웨어 없이 구동**
- 실기 제어 스택과 시뮬레이터 사이의 **인터페이스 계층 설계 및 구현**
- 이식 변경을 원본 스냅샷 대비 diff로 상시 재생성 — 무엇을 왜 바꿨는지 언제든 설명 가능
- 스캔–탐지–계획–접근–파지–복귀 **시퀀스** 중단 없이 완주 (4회: 2026-07-10/07-16/08-29×2)
- **파지 → 이송 → 배치(release)** 까지 포함한 전 구간을 익은 딸기 **4개 연속** 완주
  (2026-09-07 런, 근거 `log/m2_5/`)
- 시뮬 파지 판정을 실기 판정 로직(조우 위치 기반)과 **동일한 기준으로 모델링** —
  접근 정확도가 파지 성공/실패로 드러나는 검증 루프 구성
- 씬 기하와 플래너 벽 모델의 **131.7mm 불일치를 규명해 정합**(clamp WARN 소멸) — 6개 출처를 `check_params.py` 로 기계 검사
  (~~131.7mm → 0.0mm~~ 는 09-09 보드 810mm 복원으로 근거가 사라져 **쓰지 않는다**)
- IK 다중해(elbow-flip) 안전 가드 실전 검증 — 위험 해 거부·근접 해 실행 두 경로 모두 파이프라인 내 동작 확인
- 이종 파이썬 런타임(3.10/3.11) 브릿지 구조 설계
- cuRobo IK 기반 가상 두산 제어기 구현, FK 검증 오차 0.003mm
- fail-open → fail-closed 재설계
- USD 레이어 컴포지션 — 순정 애셋을 직접 고치지 않고 오버라이드 레이어로만 수정
- 통합 테스트에서 버그 7건 발견, 5건 원인 규명 후 수정·검증
- 직접 설계·3D 프린팅 제작한 커스텀 파츠를 xacro→URDF로 통합

### 쓸 수 없음

- ~~궤적 검증 자동화 파이프라인 구축~~ — 미착수 (M4)
- ~~딸기 수확 성공~~ / ~~파지 물리 검증~~ — 검증한 것은 **판정 로직과 시퀀스**다.
  접촉력·변형·슬립은 영구 범위 밖(§3). "수확 동작 전 구간(파지·배치 포함) 시퀀스 검증"이 정확한 표현
- ⛔ **"0줄 수정" / "무수정 구동" / "시뮬 전용 분기 0줄" / "원본 노드를 그대로 사용"**
  — [`PLANNER_POLICY_v2.md`](PLANNER_POLICY_v2.md) §5.1에 따라 **사용 금지**. 플래너를 수정했으므로 사실과 다르다.
  대체 표현은 v2 §5.2 참고 (핵심 단어: **"이식(port)"**)
- ~~씬 내 딸기 24개 / 72개~~ — 씬의 딸기 prim은 **12개**(익은 6 / 안 익은 6)다.
  로그의 24·72는 스캔 자세마다 누적된 **pick candidate 검출 건수**이지 딸기 개수가 아니다
  (`log/m1/scan_executor.log:11`의 24건, `log/m2_5/scan_executor.log:10`의 72건).
  이 표현은 면접에서 씬을 보여주는 순간 무너진다
- ~~전체 타겟 수확~~ — 익은 딸기 6개 중 **4개 완주**. 1개는 직선 진입 IK 실패,
  1개(`ripe_03`, x=+350mm)는 도달 범위 밖 (09-08 에 (200,740) 으로 이동, IK 재검증 통과)
- ⛔ **"접근 오차 131.7 → 0.0mm"** — 보드 기준값이 09-09 에 810mm(원본 씬 실측값)로 복원돼 0.0 의 근거가 없다.
  **"131.7mm 불일치 규명 및 정합(clamp WARN 소멸)"** 으로
- ⛔ **"병든 과실 선별 / 솎아내기"** — 시뮬 범위 밖. 잔여 이슈에 범위 결정으로만 기재하고, 팀원 관련 경위는 어떤 문서에도 쓰지 않는다
- ⛔ **"전체 시퀀스 재현"** — 원안 대비 폐기 분기가 빠졌으므로 **"수확 핵심 경로 완주"** 로
- ⛔ **"파지 성공" / "수확 성공"** — T2 가 되어도 금지. "파지 후 과실이 그리퍼를 따라 이동·배치되는 것까지 시뮬레이션" 까지만

### 아직 조건부인 표현

- **"관통 없이"** — 툴 길이 모델 수정본으로 재실행해 화면상 관통이 사라진 것을 확인한 뒤에 쓴다.
  2026-09-07 완주 런은 수정 **전** 상태였다
- **"파지 실패도 재현된다"** — 판정식은 CONTACT/EMPTY로 분기하지만 실행 로그에는
  CONTACT 사례만 있다. EMPTY가 실제로 찍힌 런을 확보한 뒤에 쓴다
- **"쿼드트리 4분면 순회 / 분면별 스캔 자세"** — 09-09 코드 완료, T1 실행 검증 뒤에
- **"파지 후 과실이 그리퍼를 따라 이동·배치"** — T2 완료 뒤에

> **주의**: "파지 물리 검증"이라고 쓰면 안 된다. 접촉 물리는 범위 밖이고,
> 검증한 것은 **판정 로직과 시퀀스**다. 3절의 논리를 그대로 쓴다.

---

## 씬 구성 메모 (2026-09-07 작성, 2026-09-09 수치 갱신)

> ⚠️ **수치의 기준은 [`docs/parameters.md`](docs/parameters.md) 다.** 여기 표는 배치 의도를 남긴 것이고,
> 보드가 672 → **810.0mm** 로 복원되면서 y 계열 값이 전부 바뀌었다 (2026-09-09).

### 메인셀 = 보드 전체 (실측)

| | 범위 | 크기 |
|---|---|---|
| x | −494.3 ~ +594.3 mm | 1089 mm |
| z | +265.0 ~ +1055.0 mm | 790 mm |
| **분할 중심** | **x = +50.0, z = +660.0 mm** | |

서브셀: nw(x≤50, z≥660) / ne(x>50, z≥660) / sw(x≤50, z<660) / se(x>50, z<660)

**셀 경계 표시** — 2026-09-10 부터 `assets/props/whiteboard.usd` 의 `highlight/{nw,ne,sw,se}`.
보드와 같은 격자 텍스처에 옅은 주황 tint 를 곱한 판 4장을 보드면 2mm 앞에 두고,
`isaac_sim_hud.py` 가 HUD 영역 값에 맞춰 켠다 (홈 = 보드 전체, 분면 = 그 칸만).
보드 텍스처도 이날 흰 종이 2×2 격자로 복귀 — 격자선이 곧 분면 경계다.
**시각 전용** — 콜라이더 없음, 이름에 strawberry가 없어 탐지도 안 됨, 플래너 입출력 무관.
(종전 `cell_markers.usd` — 분면 꼭지점 9곳에서 −Y 로 300mm 뻗은 봉 — 는 시야를 가려 제거.)

### 딸기 12개 — 익은 6 / 안 익은 6, 모두 y=782.8mm (보드 810.0mm 에서 −27.2mm, 과실 뒷면 5.0mm)

| 보드 서브셀 | 익은 것 | 안 익은 것 |
|---|---|---|
| nw | 2 (ripe_01,02) | 1 (unripe_01) |
| ne | 1 (ripe_03) | 2 (unripe_02,03) |

> **2026-09-08 이동**: `ripe_01` (−250,880)→**(−250,740)**, `ripe_03` (350,800)→**(200,740)**.
> 둘 다 작업영역 가장자리라 pre-approach IK 가 전부 실패해 수확이 안 됐다.
> cuRobo IK 재검증 결과 새 위치에서 **6개 모두 4/4 접근 변형 도달**, 서브셀도 그대로다.
| sw | 3 (ripe_04,05,06) | 0 |
| se | 0 | 3 (unripe_04,05,06) |

### ✅ 서브셀 경계는 이제 보드 고정 격자다 (2026-09-08 수정)

`scan_executor_node` 에 `BOARD_SUBCELL_X_MID_M = 0.050` / `BOARD_SUBCELL_Z_MID_M = 0.660`
(보드 실측 중심)을 두고 그 값으로 분할한다. **딸기 배치와 무관하다.**
따라서 아래의 "익은 딸기 bbox 중점을 보드 중심에 맞춘다"는 제약은 **더 이상 지킬 필요가 없다.**
딸기를 도달 가능한 위치로 자유롭게 옮겨도 서브셀 분류는 흔들리지 않는다.

<details><summary>수정 전 기록 (참고용)</summary>

### ⚠️ (구) 서브셀 경계는 planner 안에서 "상대 분할"이었다

`scan_executor_node._group_poses_by_subcell`은 월드 고정 격자를 쓰지 않는다.
**탐지된 딸기들의 bbox 중점**을 매번 계산한다. 함수 docstring에 따르면 이는 버그가 아니라
"아래쪽부터 수확"하기 위한 **의도된 순서 결정 휴리스틱**이다.

**플래너를 고치지 않고 보드 격자와 일치시킨 방법**: 익은 딸기 6개의 bbox 중점이
보드 중심(x=50.0, z=660.0)과 **정확히 일치**하도록 배치했다 (오차 0.0mm).
그러면 상대 분할의 결과가 보드 4등분과 같아진다.

- x 범위 −250 ~ +350 → 중점 +50.0 ✅
- z 범위 +440 ~ +880 → 중점 +660.0 ✅
- 경계 여유 최소 100mm

> **한계**: 이 일치는 **첫 패스에서만** 성립한다. 딸기가 하나씩 수확·블랙리스트되면
> 남은 것들의 bbox가 줄어 중점이 이동한다. 서브셀 **라벨**이 달라질 뿐 수확 자체는 계속 진행된다.
> 완전한 보드 고정 격자로 만들려면 planner를 고쳐야 한다.

**→ 2026-09-08 에 실제로 그렇게 고쳤다.** 위 내용은 이력으로만 남긴다.

</details>

**배치 제약** (지키지 않으면 오작동):
- 딸기 간 최소 간격 **60mm** 이상 — dedup 30mm, 재시도 블랙리스트 25mm, 딸기 지름 44mm를 모두 넘겨야 함 (현재 최소 67mm)
- 좌표는 `layout_layer.usd`와 `physics_layer.usd`(stem joint `localPos0`) **양쪽에 중복 보유**. 반드시 함께 수정
- ⚠️ **도달성 미검증**: 지금까지 검증된 범위는 x∈[−330, −150]. `ripe_03`(x=+350)과
  안 익은 것 일부는 그 밖이다. J1/J2 swing reject(M4 기지 이슈)가 날 수 있다 — 나면 x 범위를 줄인다

---

## 참고 문서

문서 지도는 [`README.md`](README.md) 의 "문서" 절에 하나로 모았다 (2026-09-09). 자주 쓰는 것만 여기 남긴다:

- **제출 범위·작업 순서(최우선)**: [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md)
- 수치·근거 1차 출처: [`PROGRESS_REPORT.md`](PROGRESS_REPORT.md)
- 수치 기준: [`docs/parameters.md`](docs/parameters.md)
- 실행 절차: [`docs/run_guide.md`](docs/run_guide.md)
- 플래너 원칙/이력: [`PLANNER_POLICY_v2.md`](PLANNER_POLICY_v2.md) · [`PLANNER_CHANGES.md`](PLANNER_CHANGES.md)
- 원안 시퀀스: [`PROJECT_GOAL.md`](PROJECT_GOAL.md) §1-2

## 진행 기록

> 세션마다 한 줄씩 추가. 형식: `- YYYY-MM-DD | 마일스톤 | 한 일 | 다음 할 일`

- 2026-09-07 | M2.5 ✅ + 문서 정합화 | **16:44 런 로그를 분석해 M2·M2.5 완료를 확정**(익은 딸기 6개 시도 → 4개 파지·배치·release 완주, `clamped to 672mm` WARN 소멸, `d_tcp` 기반 파지 판정 동작) 하고 `~/.ros/log/`에 있던 런 로그 5종을 `log/m2_5/`로 보존. **다음 실행을 헛되게 만들 뻔한 결함 선제 수정** — 브릿지의 파지 판정 TCP(160mm)가 플래너의 새 `ee_to_tcp_offset_m:=0.208`과 어긋나 d_tcp가 15mm→63mm(반경 35mm 초과)가 되어 **전 타겟 빈손 판정**이 났을 상황. 브릿지 기본값을 208mm로 맞추고 기동 경고 추가. `check_planner.sh`의 로그 건수 카운터(v1 표 형식 전제) 수정 + sim_core 빌드 정합 검사 추가. stale 문서 정정(파지 mock·breakForce·딸기 개수·PORTFOLIO_SPRINT 구조 파손). **M3 산출물 B·C·D·E 제작 완료**(`portfolio/`) | 🙋 **사용자: 관통 수정본으로 재실행 + 화면 녹화(산출물 A)** — 남은 것은 이것뿐
- 2026-08-29 | — | 6주 공백 후 재개, 스프린트 문서 작성 | M1 착수
- 2026-08-29 | — | 스프린트 문서 리포 반영 + 관련 문서(next_steps, PROJECT_SUMMARY, README) 스프린트 기준으로 최신화 | M1 착수
- 2026-09-09 | MoveSpline 단위 회귀 수정 ✅ | 2026-09-08 에 넣은 `move_spline_cb` 관절공간 보간이 `req.pos`(**deg**)와 `current_joints`(**rad**)를 섞어 보간한 뒤 결과에 `math.degrees` 를 또 입혀 **모든 관절에 57.3배 명령**을 발행했다. 스캔 포즈에서 첫 웨이포인트로 갈 때 발행값이 5156/-5156/6875/9740/-1719/5443 deg — 팔이 판스톱까지 몇 바퀴 돌며 서로 박고, 이후 계획이 전부 무의미해졌다. 구간을 4783토막으로 쪼개 20us 간격 발행까지 겹쳤다. `move_spline_cb` 가 항상 `success=True` 를 되돌려 **어느 로그에도 흔적이 없었던** 것이 발견을 늦췄다. 전 계산을 deg 로 통일하고, `_publish_joint_command` 에 관절 한계 초과 시 발행을 막고 `JOINT_COMMAND_REJECTED` 를 남기는 방어선을 추가 | **재실행 검증 → Stage 3**
- 2026-09-08 | M2.7 Stage1+2 ✅ | **노드 수확 시퀀스를 코드로 검증하고 시뮬을 거기에 정합.** 설계 6단계(살짝 열고 접근 → +Y 수평 진입 → 열린 채 −Z 하강 → 닫기 → BASE −Z 40mm 분리 → 배치) 전부 코드 확인. **Stage1**: 브릿지가 그리퍼 관절을 실제로 발행(종전엔 숫자만 저장해 시뮬 그리퍼가 한 번도 안 움직였음), Isaac 스크립트를 이름 기반 DOF 매핑으로 교체, 씬 초기 개도 49.11°(stroke 600), cuRobo `lock_joints` 0→0.857, offset 208→236mm. **Stage2**: `measured_tcp` 에 묶여 죽어 있던 open-stem descent 를 `enable_open_stem_descent` 로 분리(그 프로파일은 `grasp_tcp_link` 부재로 로드 불가). 익은 딸기 6/6 검증(수평 진입 유지, 하강 후 목표 z 일치, 보드여유 10.5mm). **부수 발견**: cuRobo 가 설정 yml 을 ASCII 로 읽는 경로가 있어 한글 주석이 기동 실패를 일으킴 → 설정 파일 전체 ASCII 화 | **사용자 재실행 검증 → Stage 3(딸기 키네마틱 부착)**
- 2026-09-07 | M2 재정정 | **로봇이 딸기·보드를 관통하던 문제 원인 3개 규명·수정.** (1) 툴 길이 모델이 73mm 짧았음 — `gripper_attach_joint` 오리진이 `xyz="0 0 0"`이라 플래너 ee_link는 플랜지와 동일 위치인데 실제 파지부(커스텀 3D 프린팅 파츠)는 플랜지 +233.3mm까지 뻗음 → `ee_to_tcp_offset_m` ROS 파라미터 신설(실기 기본값 유지, 시뮬은 `:=0.208`). (2) 커스텀 파츠가 cuRobo URDF에도 USD 콜라이더에도 없었음 → cuRobo 모델에 링크+구체 16개 추가. (3) cuRobo 충돌 월드가 사실상 비어 있었음 → `environment.yaml`·`scan_collision_world.yaml`에 보드 큐보이드 추가. **아울러 2026-09-05 벽 캘리브레이션의 전제 2개(두께 0 평면을 반두께 28.5mm로 오인, 3° 기울기 무시)를 정정** — 실제 보드는 672~729mm 범위였고 오차 0.0mm가 아니었음. 이제 전 구간 균일 672.0mm, 노드/시뮬 5개 출처 전부 일치. 익은 딸기 6/6 계획 가능(기하 검증). **딸기 줄기 파단력 2→20N / 1→5Nm 상향** — 2N 은 과실 자중의 10.2배뿐이라 스치면 끊겨 날아갔음. 파지 시 물리 분리는 원래부터 없던 경로(커스텀 파츠 콜라이더 부재 + 기하 기반 파지 판정)라 부작용 없음 | **사용자 재실행 → 실제 관통 해소 확인**
- 2026-08-29 | M1 ✅ | 3차 통합 테스트 2회 완주(SCAN_COMPLETE×2), elbow-flip 가드 실전 검증(다이브 0건), 로그 6종 보존, 런 B 화면 녹화 확보 | M2 착수 (벽 캘리브레이션 132mm)
- 2026-09-07 | 정책 전환 | **`PLANNER_POLICY_v2.md` 채택** — v1(무수정 원칙) 폐기, 플래너 수정 전면 허용. `PLANNER_CHANGES.md`를 한 줄 로그 형식으로 재작성, `check_planner.sh` 목적 재정의, **§5.1 금지 표현("0줄 수정/무수정") 전 문서 전수 제거** — PROJECT_SUMMARY·PROJECT_GOAL·PORTFOLIO_SPRINT·README·_baseline/README | M2.5 place 완주
- 2026-09-07 | 씬 정비 | **보드 기준 셀 재정의** — 메인셀=보드 전체(1089×790mm), 분할중심(50.0, 660.0). 셀 꼭지점 마커 9개 신설(`cell_markers.usd`, 300mm, 시각 전용). 딸기 12개를 보드 서브셀로 재배치하고 **익은 딸기 bbox 중점을 보드 중심에 정확히 일치**시켜 planner를 고치지 않고 상대 분할=보드 격자 달성 | 재테스트
- 2026-09-07 | M2.5 (절반) | 씬 딸기 **2 → 12개** 확장(익은 6/안 익은 6), 브릿지에 ripeness 필터 추가. **파지 판정을 기하 기반으로 교체** — TCP↔딸기 거리로 접촉/빈손 결정, `safe_grasp` 670 하드코딩 제거. 가상 제어기만 수정 | place 완주 남음
- 2026-09-07 | M2 ✅ (재테스트 완주) | 사용자 실행: 빨간 딸기 접근→파지→복귀까지 동작 확인. **FIX #002** — `scan_executor_node`의 `_started` 래치가 해제되지 않아 프로세스당 1회만 스캔 가능하던 결함 수정(재실행 가능). **스테이터스 창 신규**(`status_monitor_node`, 읽기 전용, 플래너 무관). `check_planner.sh`에 src↔build 정합 검사 추가 | M2.5 착수
- 2026-09-05 | M2 (씬 작업 완료, 재테스트 대기) | 보드 −109.5mm 이동해 앞면을 실기 기준 672.0mm에 정합 + 딸기 매립 해소(−158.9mm, 보드 앞면 대비 +5.0mm 돌출)로 플래너 벽 모델 오차 **131.7mm → 0.0mm**. `layout_layer.usd`·`physics_layer.usd` 2개 파일만 수정(플래너는 건드리지 않음). USD 재측정으로 검증 완료 | 재테스트 실행(사용자) → 접근 오차 실측
- 2026-09-05 | — | **플래너 변경 관리 체계 셋업**: 원본 확정(독립 사본 2개 바이트 동일), `_baseline/` 2단 스냅샷(A 플래너·B 설정자산), `PLANNER_CHANGES.md` 대장, `scripts/check_planner.sh`. 소급 감사 결과 이식 변경 86라인이 전부 경로 치환이었음을 확인. M2 판정 정정: 672mm는 잔재가 아니라 실기 실측값 → **씬을 실기에 맞추는 방향**으로 확정 | M2 씬 좌표 측정
- 2026-09-05 | — | 프로젝트 구조 재파악. 파지/배치 범위 재검토 → **M2.5 신설**(파지 판정 정직화 + place 완주, 원인 코드 레벨 규명 완료), 파지 접촉 물리는 영구 범위 밖으로 명문화 | M2 착수 (벽 캘리브레이션 132mm)
- 2026-09-09 | 문서 | 지원서 채팅용 진행상황 보고 `PROGRESS_REPORT.md` 작성 — 08-29 이후 변경점, 마일스톤 현황(09-07 런 4/6 이 입증된 최고 성적, 09-09 수정본은 재완주 미확인), 재계측 지표(플래너 64파일/15,975줄, 변경 로그 101건), 표현 가이드(131.7→0.0mm 는 보드 810 복원으로 표현 변경 권고), GitHub 미커밋 경고 | 재실행 검증(nw 스캔 이동 ARM_ARRIVAL_TIMEOUT) → Stage 3 → 영상
- 2026-09-10 | 범위 확정 | **`SUBMISSION_PLAN.md` 채택** — 산출물 A(시연 영상)까지로 범위 확정, 그 이후는 이 리포에서 하지 않는다. 작업 순서 T1 재완주(블로커) → T2 딸기 키네마틱 부착(1일 한도, FixedJoint 금지) → T3 정적 덩굴 → T4 계란판+slot≥1 조사 → T5 녹화(카메라 2대) → T6 수치 갱신·푸시 → (선택) T7. **착수 금지 확정**: 병든 딸기 분기(원안 C·D), 대량 배치 배속 영상, 물리 덩굴, 비타겟 blocking 콜라이더, 런타임 FixedJoint, 파지 접촉 물리, M4, 시뮬 전용 좌표 하드코딩. 문서 정합: 이 문서 M2.7 Stage3→T2 명세, M3 종료 조건→§3 영상 명세, `PROJECT_GOAL` 각주, `portfolio/README` 카메라 2대 절차·클립 4종 체크리스트, `PLANNER_POLICY_v2` §5.4 표현 델타, `E_metrics`·`C_before_after`·`PROJECT_SUMMARY`·`README` 의 0.0mm/672/1,117줄 계열 정정. T4-2 는 09-07 로그로 이미 판정(slot 0 반복, J6 spline jump 359° 거부 흔적) | **T1 Step 0: 09-09 17:23 런 로그 읽기** (`JOINT_COMMAND_REJECTED` 유무 / 스플라인 실행시간 / 명령값 vs 측정값)
