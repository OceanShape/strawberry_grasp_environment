# 진행상황 보고 — 이력서·경력기술서·포트폴리오 갱신용

> **용도**: 별도 클로드 채팅(지원서 작업)에 그대로 붙여 넣는 현황 브리핑.
> **기준일**: 2026-09-09 17:30 KST. 리포 `~/strawberry_grasp_environment` 의 문서·코드·로그에서 직접 뽑았다.
> **이전 스냅샷과의 관계**: 지원서 채팅이 갖고 있을 자료는 `PROJECT_SUMMARY.md`(2026-08-29 작성, 09-07 갱신)와
> `PORTFOLIO_SPRINT.md`(08-29) 다. 이 문서는 **그 이후 바뀐 것**을 앞에 두고, 전체 요약을 뒤에 둔다.
>
> 표기: ✅ 로그·실측으로 검증됨 / 🔶 코드는 끝났으나 실행 검증 대기 / ❌ 미착수·미해결
>
> **2026-09-10 추가**: 제출 **범위와 작업 순서**는 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) 가 확정했다(T1 재완주 → T2 딸기 부착 → … → T6 푸시).
> 이 문서는 수치·근거의 1차 출처로 계속 쓰고, 범위가 충돌하면 SUBMISSION_PLAN 이 우선한다.

---

## 0. 30초 요약

- **프로젝트**: 팀이 작성한 두산 e0509 딸기 수확 모션 플래너(파이썬 64파일·15,975줄)를 NVIDIA Isaac Sim 5.1 환경으로 **이식**하고,
  가상 두산 제어기(dsr_msgs2 서비스 서버)와 비전 모킹 노드를 구현해 **하드웨어 없이 수확 시퀀스를 구동·검증**하는 디지털 트윈 파이프라인. 1인 담당.
- **입증된 최고 성적** ✅: 2026-09-07 런에서 스캔→탐지→계획→접근→파지→**이송·배치(release)**→복귀 전 구간을
  익은 딸기 **6개 시도 → 4개 연속 완주** (`log/m2_5/`). END-TO-END 완주 런은 누적 **5회**.
- **지금 하는 일** 🔶: 위 런은 툴 길이 모델이 틀린 상태(화면상 관통 잔존)라 영상용으로 못 쓴다.
  09-07~09 에 **노드 시퀀스↔시뮬 정합 작업(M2.7)** 을 대규모로 했고, 오늘(09-09) 재실행 검증 중이다.
  **마지막 시도(09-09 17:23)는 첫 스캔 이동에서 도착 타임아웃으로 미완주** — 수정본의 END-TO-END 재완주는 아직 없다.
- **제출까지 남은 것**: 산출물 A(시연 영상 30~60초) 하나. B~E(아키텍처·Before/After·USD 레이어·정량지표)는 `portfolio/` 에 완성.
- **이력서에 쓸 때 주의**: "0줄 수정/무수정" 금지(→"이식"), "딸기 24개" 금지(씬은 12개), "파지 물리 검증" 금지(검증한 건 판정 로직·시퀀스).
  **접근 오차 "131.7→0.0mm" 는 표현을 바꿔야 한다** (§6 참고 — 보드 기준값이 09-09 에 810mm 로 복원됨).

---

## 1. 2026-08-29 이후 달라진 것 (지원서 채팅이 모르는 내용)

| 날짜 | 무엇 | 상태 |
|---|---|---|
| 08-29 | M1 3차 통합 테스트 2회 완주(SCAN_COMPLETE×2), elbow-flip 가드 실전 검증(다이브 0건), 로그 `log/m1/` 보존, 화면 녹화 1편 확보 | ✅ |
| 09-05 | 플래너 변경 관리 체계(`_baseline/` 원본 스냅샷, `PLANNER_CHANGES.md`, `scripts/check_planner.sh`) 구축. 접근 오차 132mm 의 원인을 씬 기하 불일치로 확정 | ✅ |
| 09-05 | **M2.5 신설** — 파지 판정이 mock 상수 때문에 항상 빈손이던 것을 규명. 파지 접촉 물리는 "영구 범위 밖"으로 명문화 | ✅ |
| 09-06~07 | **정책 전환 v2** — "플래너 무수정" 원칙 폐기, 수정 전면 허용 + 한 줄 로그. 외부 문서에서 "0줄 수정" 표현 전수 제거 | ✅ |
| 09-07 | 씬 딸기 2→**12개**(익은 6/안 익은 6), 보드 4분면 서브셀 배치, 셀 마커 9개. 파지 판정을 **기하 기반**(TCP↔과실 거리)으로 교체 | ✅ |
| 09-07 | **M2·M2.5 완료** — 16:44 런에서 4개 파지·이송·release 완주. `clamped to 672mm` WARN 소멸 | ✅ |
| 09-07 | **관통 3원인 규명**: ① 툴 길이 모델 73mm 부족 ② 커스텀 파츠가 충돌 모델에 없음 ③ cuRobo 충돌 월드 공백 → 수정 | ✅ 기하검증 / 🔶 실행검증 |
| 09-07 | 포트폴리오 산출물 **B·C·D·E 완성** (`portfolio/*.svg/.png`, `E_metrics.md`) | ✅ |
| 09-08 | **M2.7 Stage1**: 시뮬 그리퍼가 한 번도 움직이지 않던 것 발견·수정(브릿지가 그리퍼 4축 발행, Isaac 스크립트 이름 기반 DOF 매핑) | ✅ 헤드리스 검증 |
| 09-08 | **M2.7 Stage2**: 로드 불가 프로파일에 묶여 죽어 있던 open-stem descent 를 파라미터로 분리. 익은 딸기 6/6 기하 검증 | ✅ 헤드리스 검증 |
| 09-08 | ★ **elbow-flip 의 진짜 원인**: cuRobo `solve_single` 에 현재 관절을 `seed_config` 가 아닌 `retract_config` 로 넘기고 있었음. 수정 후 6/6 실패 → **6/6 통과, 스텝당 1°** | ✅ |
| 09-08 | 그리퍼 "가위 모양" 원인(관절쌍 상한 불일치), J6 wrap 263° 점프, 스플라인 12점 다운샘플 보간 등 7차 관찰 4건 수정 | ✅ |
| 09-09 | **MoveSpline deg/rad 회귀**(모든 관절 57.3배 명령) 수정 + 관절 한계 초과 발행 차단 방어선 추가 | ✅ |
| 09-09 | **인-뷰포트 HUD** 도입(별도 상태창 폐기). Kit 이 한글을 못 그려 Pillow 로 라벨 PNG 사전 렌더 | 🔶 Isaac 안 표시 검증 대기 |
| 09-09 | **원안 수확 시퀀스 복원**: 쿼드트리 4분면 순회(nw→ne→se→sw), 분면별 scan pose 를 실기 v12 티칭값으로 복원, 분면 이동 계획 경유, 중단 시 scan pose 복귀 | 🔶 |
| 09-09 | **보드 기준 y 를 672 → 810mm(원본 씬 실측값) 로 복원**, 6개 출처 일괄 정합 + `check_params.py` 기계 검사 신설. 보드 콜라이더를 실기 화이트보드 1500×900 크기로 확장 | 🔶 |
| 09-09 | 파지 판정을 스칼라 거리 → **조우 물림 구간(along/lateral)** 기준으로 교체, 파지 오프셋 사다리를 기구학적 한계 [15,20,25]mm 로 절단, 자기-장애물 등록 버그(부동소수점 경계) 수정 | 🔶 |
| 09-09 | 실행 속도 계열: `sim_speed_scale`, 관절속도 기반 스플라인 하한, 도착 대기(실기 서비스처럼 모션 종료 후 응답), 드라이브 게인 10배 | 🔶 |

> `PLANNER_CHANGES.md` 의 일부 항목은 날짜가 `2026-09-10` 으로 적혀 있으나 실제로는 09-09 세션 작업이다(기준일 확인 완료).

---

## 2. 현재 상태 — 마일스톤

| 마일스톤 | 내용 | 상태 | 근거 |
|---|---|---|---|
| M1 | 3차 통합 테스트 + elbow-flip 가드 검증 | ✅ 08-29 | `log/m1/` 6종 |
| M2 | 씬 기하 ↔ 플래너 벽 모델 정합 | ✅ 09-07 (672 기준) → 🔶 09-09 810 으로 재정합, 실행 재검증 대기 | `log/m2_5/`, `docs/parameters.md` |
| M2.5 | 파지 판정 기하화 + 배치(place) 완주 | ✅ 09-07, 4개 연속 | `log/m2_5/README.md` |
| M2.7 | 노드 수확 시퀀스 ↔ 시뮬 정합 (Stage1~3) | Stage1·2 ✅ 코드/헤드리스, **Stage3(딸기 키네마틱 부착) ❌ 미착수** | `PORTFOLIO_SPRINT.md` M2.7 |
| M3 | 포트폴리오 산출물 A~E | B·C·D·E ✅ / **A(영상) ❌** | `portfolio/` |
| M4 | 제출 이후(J1/J2 swing, 검증 리포트 자동화, README 정비) | ⏸ 착수 금지 | — |

**정직한 현황 한 단락.** 시퀀스·판정·배치 로직은 09-07 런으로 입증됐다. 그 뒤 이틀간 "노드가 가정하는 시퀀스와 시뮬의 실제 거동" 을 맞추는
작업을 대규모로 했고(변경 로그 101건 중 약 60건이 09-08~09), 그 수정본으로는 **아직 END-TO-END 재완주 로그가 없다**.
오늘 마지막 시도는 첫 분면(nw) 스캔 이동에서 팔이 90초 안에 도착하지 못해 중단됐다(관절 오차 130.9°, `ARM_ARRIVAL_TIMEOUT`).
따라서 지원서에는 **09-07 런 수치를 쓰고**, "관통 없이" 같은 표현은 재완주 후에 쓴다.

---

## 3. 아키텍처와 직접 만든 것

```
실기:  비전(YOLO) → scan_executor → curobo_planner → 두산 제어기(dsr_msgs2) → 실물 e0509
시뮬:  fake_vision → scan_executor → curobo_planner → sim_executor_bridge → Isaac Sim (py3.11)
       (입력단 모킹)  ↑───── 실기 플래너 이식 구간 ─────↑   (가상 두산 제어기, py3.10)
```

양 끝단만 바꾸고 가운데 플래너 2노드는 ROS 2 인터페이스 계약(토픽·서비스·액션·QoS)을 그대로 지켜 이식했다.
두 파이썬 런타임(ROS 3.10 / Isaac 3.11)은 라이브러리를 공유할 수 없어 ROS 토픽만을 경계로 연결했다.

| 직접 구현·구축 | 규모(2026-09-09) | 내용 |
|---|---|---|
| 로봇 애셋 파이프라인 | xacro→URDF 1본, USD 19파일 | e0509 + RH-P12-RN + 직접 설계·3D 프린팅한 커스텀 파츠 + D455 더미를 단일 URDF 로 통합, Isaac 임포트 |
| USD 씬 구조 | 애셋/레이아웃/물리 레이어 분리 | 순정·임포트 애셋은 읽기 전용, 커스터마이징은 상위 레이어 오버라이드로만 (URDF 재빌드에도 살아남음) |
| 가상 제어기 계층 (`strawberry_sim_core`) | 파이썬 10파일 / 1,532줄 | `sim_executor_bridge_node`(MoveJoint/MoveLine/MoveSplineJoint + 그리퍼 액션, MoveLine 은 cuRobo IK 실연산), `fake_vision_node`(분면 시야 모사), `quadrant_filter`, `status_monitor_node` |
| Isaac Sim 내부 스크립트 | 8파일 / 1,736줄 (HUD 5파일 912줄 포함) | Script Editor 브릿지(딸기 좌표 발행, `/joint_command` 주입), 인-뷰포트 HUD, 한글 라벨 렌더러 |
| 이식 변경 추적 | `_baseline/` + 변경 로그 101건 + diff 재생성 스크립트 | 원본 스냅샷 대비 diff 를 언제든 재생성. 무엇을 왜 바꿨는지 항목별 한 줄 |
| 파라미터 정합 검사 | `check_params.py` | 보드 y 처럼 6개 파일에 흩어진 값을 기계적으로 대조 |
| 통합 테스트 체계 | 4노드 + Isaac 동시 기동, fail-closed 6단계 게이트 | 로그 보존 규약(`log/m1`, `log/m2_5`) |
| 문서 | `docs/` 8종 + 루트 6종 | 실행 가이드, 파라미터 기준표, 개념 노트 Q&A, 파이프라인 개요 등 |

---

## 4. 해결한 기술 문제 (경력기술서 소재, 강한 순)

번호 뒤 괄호는 근거 위치. 수치는 전부 로그·실측에서 나온 것만 적었다.

1. **IK elbow-flip 의 진짜 원인 규명 — API 오용** ✅ (`PLANNER_CHANGES.md` 09-08).
   처음엔 "같은 목표 30회 중 6회가 팔꿈치 반전" 현상을 최근접 해 선택 + 관절 이동 45° 가드로 막았다(08-29 실전 검증, 다이브 0건).
   이후 cuRobo `solve_single(goal, retract_config, seed_config)` 에 현재 관절을 **`retract_config`(널스페이스 목표) 자리에 넘기고 있었음**을 발견.
   시드가 없어 매번 무작위 브랜치로 수렴하던 것. `seed_config` 로 수정 후 45mm 직선 진입 **6/6 실패 → 6/6 통과, 스텝당 관절 이동 1°**.
   → "증상 가드 → 근본 원인" 두 단계 서사. 면접용 최강 소재.

2. **툴 길이 모델 불일치로 인한 과실·보드 관통, 원인 3개 분해** ✅기하/🔶실행 (09-07).
   (a) URDF `gripper_attach_joint` 오리진이 0 이라 플래너 ee_link 가 플랜지와 같은 자리인데 TCP 를 +160mm 로 가정. 실제 파지부(커스텀 파츠)는 +233~262mm.
   (b) 커스텀 파츠가 cuRobo 구체 모델·PhysX 콜라이더 어디에도 없음. (c) cuRobo 충돌 월드가 10m 밖 더미 하나뿐.
   → `ee_to_tcp_offset_m` 파라미터 분리(실기 기본값 불변), 링크 2개 + 구체 16개 추가, 보드 큐보이드 추가.
   **플래너·브릿지 오프셋이 한 쌍**임을 식으로 도출(예측 15/30mm vs 실측 14.8/28.9mm)해 "전 타겟 빈손" 결함을 실행 전에 선제 차단.

3. **시뮬 파지 판정을 실기 판정 로직과 같은 기준으로 모델링** ✅ (09-07, 09-09 고도화).
   가상 그리퍼가 명령값을 그대로 반향해 무엇을 잡아도 빈손(700≥695)이던 것을, 실기가 전류가 아닌 **조우 위치**로 판정한다는 점에 맞춰
   TCP↔줄기의 기하(조우 물림 구간 −5.5~+26.5mm, 측방 20mm)로 리드백을 생성. 접근 정확도가 파지 성공/실패로 드러나는 검증 루프.
   판정 반경을 넓혀 실패를 성공으로 오판하던 대증요법을 되돌린 이력까지 로그에 있음.

4. **씬 기하와 플래너 벽 모델의 131.7mm 불일치 규명** ✅ (09-05~09).
   플래너 WARN 문구는 "FK calibration drift" 로 오진했으나 실제 원인은 씬 배치. USD 오프라인 실측으로 확정, 오버라이드 레이어만 수정.
   09-07 에는 "두께 0 평면을 반두께로 오인, 3° 기울기 무시" 라는 자신의 전제 오류를 스스로 정정.
   09-09 에 보드 y 가 **6개 파일**에 분산된 구조를 파악해 `check_params.py` 로 기계 검사(실제로 2곳 누락 → 로봇이 딸기 앞을 집는 사고를 잡음).

5. **MoveSpline deg/rad 혼용 회귀 + 방어선** ✅ (09-09).
   관절공간 보간에서 deg 와 rad 를 섞어 모든 관절에 57.3배 명령 발행(5156° 등) → 팔이 여러 바퀴 돌며 자기 충돌.
   콜백이 항상 success=True 라 **어느 로그에도 흔적이 없어** 발견이 늦음. 수정 + 관절 한계 초과 명령 발행 차단(`JOINT_COMMAND_REJECTED`).

6. **가상 제어기를 실기 서비스 의미론에 맞춤** 🔶 (09-09).
   실기 두산 서비스는 모션이 끝나야 응답하는데 브릿지는 즉시 응답 → 잔차가 다음 **상대** MoveLine 에 전파돼 조우가 파지점 앞에서 닫힘.
   도착 대기 추가, 관절속도 기반 실행시간 하한, 측정값을 명령으로 되실어 팔을 얼리던 버그 수정.

7. **fail-open → fail-closed 재설계** ✅ (07-10). MoveLine 실패 시에도 success=True 이던 브릿지를 실패 시 False 로. 플래너의 6단계 시작 거부 게이트를 우회하지 않고 전부 정상 경로로 통과.

8. **PhysX articulation 초기화 실패** ✅ (07-09). 순정 D455 애셋의 RigidBodyAPI 가 그리퍼 링크 하위에 중첩 → 순정 파일을 건드리지 않고 assembly 레이어 오버라이드(`rigidBodyEnabled=False`).

9. **드라이브 게인 튜닝** ✅ (07-10, 09-09). URDF 임포터 기본값 damping≈0 진단 → PD 게인 오버라이드(AT_SCAN_POSE 오차 0.2°→0.0°). 09-09 추종 대역 확장을 위해 비율 유지한 채 10배.

10. **TOOL 좌표계(ref=1) 상대 이동 구현** ✅. EE 자세 회전 변환, FK 검증 오차 0.003mm, FK→IK 왕복 ~1µm.

11. **그리퍼 기구 문제** ✅ (09-08~09). 평행사변형 그리퍼의 관절쌍 상한 불일치(1.1 vs 1.0 rad)로 "가위 모양"이 되던 원인 규명, 커스텀 파츠 접촉각 실측(1.0 rad 에서 간격 9.4mm → 1.08 rad 에서 0.3mm), 6° splay 제거.

12. **의존성·환경** ✅. cuRobo 0.7.8 ↔ warp-lang 1.10+ 비호환(1.9.1 핀), colcon ↔ setuptools 80+(79.0.1 핀), rosidl 비ASCII 경로 버그(리포 이전), cuRobo yml ASCII 경로(설정 주석 영어화), Isaac ROS2 Bridge 확장 필수 환경변수 3종 규명.

13. **인-뷰포트 HUD** 🔶 (09-09). 노드 4개에 각 4줄 계측 가드만 넣고 상태를 스냅샷 파일로 내보내 Isaac 뷰포트에 오버레이. Kit 텍스트 렌더러가 한글을 못 그려 Pillow 로 라벨을 사전 렌더해 이미지로 부착.

---

## 5. 정량 지표 (2026-09-09 재계측)

| 지표 | 값 | 출처 / 재생성 |
|---|---|---|
| 빌드되는 ROS 2 패키지 | **5개** | `ls build/` |
| 이식한 실기 플래너 | **파이썬 64파일 / 15,975줄** (09-07 집계 61/15,256 에서 신설 모듈 3개 증가) | `find src/strawberry_motion -name '*.py'` |
| 직접 구현한 가상 제어기(ROS 측) | **10파일 / 1,532줄** | `src/strawberry_sim_core/` |
| 직접 구현한 Isaac Sim 스크립트 | **8파일 / 1,736줄** (HUD 5파일 912줄 포함) | `strawberry_harvest/scripts/` |
| 플래너·씬 변경 로그 | **101건** (한 줄 형식) | `PLANNER_CHANGES.md` |
| 원본 스냅샷 대비 diff | 09-07 기준 494줄 (이후 증가, 재생성 필요) | `bash scripts/check_planner.sh` |
| USD 애셋·씬 파일 | **19개**, 딸기 prim **12개**(익은 **8**/안 익은 **4** — 09-11 unripe 2개를 위치 그대로 전환; 런 1~9 는 6/6) | `strawberry_harvest/` |
| END-TO-END 완주 | **5회** (07-10 / 07-16 / 08-29×2 / 09-07) | `log/m1/`, `log/m2_5/` |
| 최고 성적 런 | 익은 딸기 6개 시도 → **4개** 파지·이송·release 완주 | `log/m2_5/README.md` |
| 파지 판정 거리 | d_tcp 13.5~28.9mm (반경 35mm, 당시 기준) | `log/m2_5/sim_executor_bridge.log` |
| MoveLine IK FK 검증 | **0.003mm**, FK→IK 왕복 ~1µm, 배치 IK 5/5 | `docs/next_steps.md` |
| elbow-flip 근본 수정 효과 | 6/6 실패 → **6/6 통과**, 스텝당 1° | `PLANNER_CHANGES.md` 09-08 |
| 결함 처리 | 09-07 기준 **17건 수정**(가상제어기 6·USD 5·플래너 2·설정 4) + 09-08~09 **약 15건 추가 규명·수정**(집계는 E_metrics 갱신 시 확정) | `portfolio/E_metrics.md` §5 |
| 미해결 | J1/J2 swing reject, 트레이 이송 J3 swing reject(런 10·11 간헐, 09-12 **실기 플래너 한계로 기록** — H §9; 09-14 스윙 가드 3값 원본 원복), 실기 요청 가속도의 두산 MoveIt 설정 초과(09-14 D3, 기록만), 트레이 slot≥1 J6 한계, 커스텀 파츠 PhysX 콜라이더 부재, 그리퍼 l2/r2 충돌 구체 부재, 09-09 수정본 재완주 미확인 | 〃 |
| 기술 스택 버전 | Isaac Sim 5.1.0 · ROS 2 Humble · cuRobo 0.7.8(소스 빌드) · PyTorch 2.6.0+cu124 · CUDA 12.4 · warp 1.9.1 · Py 3.10/3.11 · Ubuntu 22.04 · RTX 4060 Ti | `E_metrics.md` §6 |

---

## 6. 표현 가이드 — 쓸 수 있음 / 없음 / 조건부

### 쓸 수 있음
- 실기용 모션 플래너(64파일·15,975줄)를 Isaac Sim 환경으로 **이식**해 수확 시퀀스 완주
- 가상 dsr_msgs2 서비스 서버를 구현해 **실기 제어 스택을 하드웨어 없이 구동**
- 실기 제어 스택과 시뮬레이터 사이 **인터페이스 계층 설계·구현** (이종 파이썬 런타임, ROS 토픽 경계)
- 이식 변경을 원본 스냅샷 대비 diff 로 상시 재생성 + 변경 로그 101건 — 무엇을 왜 바꿨는지 언제든 설명 가능
- 스캔–탐지–계획–접근–파지–**이송·배치(release)**–복귀 전 구간을 익은 딸기 **4개 연속** 완주 (2026-09-07)
- END-TO-END 시퀀스 완주 5회
- 시뮬 파지 판정을 실기 판정 로직(조우 위치 기반)과 **같은 기준으로 모델링**해 접근 정확도가 파지 성패로 드러나는 검증 루프 구성
- cuRobo IK elbow-flip: 가드로 1차 방어(실전 검증) → **API 오용(seed_config) 근본 원인 규명**, 6/6 실패→6/6 통과
- 씬 기하와 플래너 벽 모델의 **131.7mm 불일치 규명**, 파라미터 6개 출처 정합 검사 스크립트로 기계화
- fail-open → fail-closed 재설계, 6단계 안전 게이트 정상 경로 통과
- USD 레이어 컴포지션 — 순정 애셋 무수정, 오버라이드 레이어로만 수정
- 직접 설계·3D 프린팅한 커스텀 파츠를 xacro→URDF 로 통합, 실측으로 툴 길이 모델 정정
- 통합 테스트로 결함 17건(+α) 발견·수정, 미해결은 회피책과 함께 문서화

### 쓸 수 없음
- ⛔ "0줄 수정 / 무수정 구동 / 시뮬 전용 분기 0줄 / 원본 노드를 그대로 사용" → **"이식(port)"**
- ⛔ "딸기 24개 / 72개" → 씬의 딸기는 **12개**. 24·72 는 스캔 프레임 누적 검출 건수
- ⛔ "파지 물리 검증 / 딸기 수확 성공" → 검증한 것은 **판정 로직과 시퀀스**. 접촉력·변형·슬립은 영구 범위 밖
  (근거: 실기 `verify_grasp()` 도 전류가 아닌 조우 위치로 판정. 면접 답변 논리는 `PORTFOLIO_SPRINT.md` §3)
- ⛔ "전체 타겟 수확" → 6개 중 4개
- ⛔ "궤적 검증 자동화 파이프라인 구축" → 미착수(M4)
- ⚠️ **"접근 오차 131.7mm → 0.0mm"** — 09-07 문서·산출물 C·E 에 이 수치가 있으나, 09-09 에 보드 기준을 672 → 810mm(원본 씬 실측값) 로
  되돌렸다. 09-05 의 "672 는 실기 실측값" 판정에 1차 출처가 없었음을 09-08 에 확인했기 때문.
  안전한 표현: **"플래너 벽 모델과 씬 기하의 131.7mm 불일치를 규명해 정합(clamp WARN 소멸)"**. "0.0mm" 는 쓰지 않는 편이 안전.
  `portfolio/C_before_after.svg`·`E_metrics.md` 의 해당 수치는 갱신 필요.

### 조건부 (재완주 후에 쓴다)
- "관통 없이" — 툴 길이 수정본으로 END-TO-END 재완주가 확인된 뒤
- "파지 실패도 재현된다(EMPTY 판정)" — 09-09 세션에서 EMPTY 사례가 실제로 관찰됐다는 변경 로그가 있으나 보존 로그가 없음
- "쿼드트리 4분면 순회 / 분면별 스캔 자세" — 09-09 코드 완료, 실행 검증 대기
- "딸기가 실제로 분리·배치되는 화면" — Stage3(키네마틱 부착) 미착수. 지금은 로봇만 움직이고 딸기는 제자리

---

## 7. 남은 작업과 리스크

> 작업 순서는 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) §4 (T1~T7). 아래 표는 리스크 메모다.

| 항목 | 내용 |
|---|---|
| **A 영상** (제출 조건) | 09-09 수정본으로 재실행 → END-TO-END 재완주 → 화면 녹화(롱샷 + 클로즈업) → `log/m3/` 보존. 절차 `portfolio/README.md` |
| 재실행 중 걸림 | 09-09 17:23 런: nw 분면 스캔 이동 `ARM_ARRIVAL_TIMEOUT`(관절 오차 130.9°). 다음 세션 첫 과제 |
| Stage3 | 딸기 키네마틱 부착(CONTACT 시 그리퍼에 부착, release 시 분리). 영상의 설득력에 직결. 물리 파지는 구조적으로 불가(조우 간격 vs 과실 단면) |
| 산출물 수치 갱신 | C·E 의 672/0.0mm 계열 수치, 파일·줄 수, 결함 건수 → 이 문서 §5 값으로 |
| **GitHub 리포 낡음** | 원격 `OceanShape/strawberry_grasp_environment` 마지막 커밋 **2026-07-07**. 그 뒤 두 달치(변경 파일 75+, 플래너·브릿지·문서·portfolio 전부)가 **미커밋**. 이력서에 리포 링크를 넣으려면 커밋·푸시가 먼저 |
| 문서 정합 | `PROJECT_SUMMARY.md`·`README.md` 는 09-07 상태(672mm, 1,117줄 등) 잔존 |

---

## 8. 면접 예상 질문 대비 메모

- **왜 파지 물리를 안 했나** → 실기 판정이 position 기반이라 시뮬 경계도 position 에서 끊었다. 소프트바디는 대조 데이터가 없어 검증 불가능한 충실도.
- **플래너를 고쳤나** → 고쳤다. 처음엔 무수정 원칙이었으나 제출 일정 우선으로 정책을 바꿨고, 모든 변경을 원본 스냅샷 대비 diff 와 한 줄 로그로 남겼다. 실기 기본 동작은 파라미터 기본값으로 보존(예: `ee_to_tcp_offset_m`, `enable_open_stem_descent`).
- **가장 어려웠던 버그** → elbow-flip. 가드로 막고 나서 진짜 원인(seed 인자 위치)을 찾은 과정. 또는 MoveSpline 57.3배 — 로그에 안 남는 버그를 방어선으로 잡은 이야기.
- **재현 가능한가** → 실행 절차 `docs/run_guide.md`, 파라미터 기준표 `docs/parameters.md`, 정합 검사 `check_params.py`, 로그 보존 규약.
- **무엇이 미완인가** → 수정본 재완주와 영상, 딸기 키네마틱 부착, J1/J2 swing, 커스텀 파츠 PhysX 콜라이더.

---

## 9. 참고 파일 지도

| 알고 싶은 것 | 파일 |
|---|---|
| 우선순위·마일스톤·진행 기록 | `PORTFOLIO_SPRINT.md` |
| 이력서용 요약 스냅샷(09-07 기준) | `PROJECT_SUMMARY.md` |
| 정량 지표 + 출처 | `portfolio/E_metrics.md` |
| 슬라이드 캡션·순서·녹화 체크리스트 | `portfolio/README.md` |
| 변경 이력(101건) + 관찰 기록 | `PLANNER_CHANGES.md` |
| 정책(허용·금지 표현) | `PLANNER_POLICY_v2.md` |
| 지금 값의 기준(보드·툴·파지 기하) | `docs/parameters.md` |
| 실행 절차 | `docs/run_guide.md` |
| 원안 수확 시퀀스 | `PROJECT_GOAL.md` §1-2 |
| 아키텍처 설명 | `docs/pipeline_overview.md`, `portfolio/B_architecture.svg` |
| 검증 로그 | `log/m1/`, `log/m2_5/README.md` |
