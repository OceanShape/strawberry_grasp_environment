# 진행상황 보고 — 이력서·경력기술서·포트폴리오·취업 전략용

> **용도**: 별도 클로드 채팅(지원서·취업 전략)에 그대로 붙여 넣는 현황 브리핑. 이 문서 하나로 프로젝트 전모와 현재 위치를 알 수 있게 쓴다.
> **기준일**: 2026-09-15 14:30 KST. 리포 `~/strawberry_grasp_environment` 의 문서·코드·로그(`log/m3/`)에서 직접 뽑았다.
> **관계**: 범위·순서는 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) §4-0 이 결정하고, 충돌하면 그쪽이 우선한다. 런별 판정 근거는 [`log/m3/README.md`](log/m3/README.md).
> 이 문서는 수치·근거의 1차 출처로 계속 쓴다. 09-09 판(구판)은 git 이력에 있다.
>
> 표기: ✅ 로그·실측으로 검증됨 / 🔶 코드는 끝났으나 확인 대기 / ❌ 미착수·미해결

---

## 0. 30초 요약

- **프로젝트**: 팀이 작성한 두산 e0509 딸기 수확 모션 플래너(ROS 2 노드 2개)를 NVIDIA Isaac Sim 5.1 환경으로 **이식**하고,
  가상 두산 제어기(dsr_msgs2 서비스 서버)와 비전 모킹 노드를 직접 구현해 **하드웨어 없이 실기 제어 스택을 구동·검증**하는 디지털 트윈. 1인 담당.
- **제출본이 주장하는 것은 하나**: *실기 제어 스택을 하드웨어 없이 구동하고, 실기와 시뮬의 불일치를 찾아 정합했다.*
- **검증 원칙 (09-14, 최우선)**: 실기 노드는 "핵심 시퀀스 설계와 맞지 않을 때"만 고친다. 시뮬은 어디서도 원본보다 관대하면 안 된다.
  실기 노드의 실패는 통과시키지 않고 **재현·원인과 함께 한계로 기록**한다. "이렇게 고치면 동작한다"는 주장은 하지 않는다.
- **지금 위치**: 작업 T1~T4d 완료(09-10~15). T4c(실패를 정직하게 보여주기)는 런 13 에서 나온 결함(낙하 과실 바닥 관통, 노드 무재시작 재트리거)을 고친 뒤 런 14 로그로 완료 기준을 채웠다. 화면 육안 항목만 사용자 확인이 남았다.
  T4d 는 서로 다른 과실 배치 5개가 사람 손 없이 전부 끝까지 돌아 완료됐고, 시연 배치에서 안 보이던 실기 노드 도달 한계 하나를 찾았다. 남은 순서는 T5 녹화 → T6 수치 정리·원격 푸시.
- **최신 성적 (런 14, 실기 원본 값 복원 뒤, 익은 딸기 8개 씬)**: 파지·분리 8, 트레이 배치 **4**, 낙하 4. 낙하 4개는 실기 플래너가 이송 계획을 거부해 보드 앞에서 놓은 것이다(J6 spline jump 3, J3 스윙 1). 런 12 는 분리 7 / 배치 4 였다.
  이 프로젝트가 가드를 넓혀 두었던 런 10 은 배치 7 이었다. 넓힌 값을 스스로 찾아 되돌렸고, 떨어진 수치를 그대로 보여주는 것이 현재 방침이다.
- **제출물 상태**: 산출물 B·C·D·E(아키텍처·Before/After·USD 레이어·정량지표) 완성, **A(시연 영상) 남음**. 면접용 자료 F·G·H 완성.
- **GitHub**: 원격 마지막 푸시 07-07(로컬 추적 기준). 로컬은 09-10 이후 32커밋 앞서 있다. 리포 링크를 이력서에 넣기 전에 푸시가 필요하다(T6).

---

## 1. 2026-09-10 이후 달라진 것

09-09 이전 이력(08-29 M1 완주, 09-07 배치 첫 완주, 09-08 elbow-flip 근본 원인 등)은 §4 에 문제 단위로 정리했다.

| 날짜 | 무엇 | 상태 |
|---|---|---|
| 09-10 | **T1 재완주** — 09-08~09 정합 수정본으로 END-TO-END 완주(런 1). 사용자 육안: 손끝이 타겟 윗줄기에서 멈추고 보드에 들어가지 않음 | ✅ |
| 09-10 | **쿼드트리 가지치기** — 1차 스캔에서 익은 과실 없는 분면을 건너뜀. 런 364.9초 → 240.6초(런 2) | ✅ |
| 09-10 | **T2 딸기 키네마틱 부착** — 파지 판정 순간의 그리퍼-과실 상대 변환을 잡아 따라가게 함. 런타임 FixedJoint·TCP 스냅 없이. 부착 6/6(런 4) | ✅ |
| 09-10 | **T3 정적 덩굴 메쉬 12개**, 물리 없음. 파지 하강 경로가 덩굴 중심선을 따라가게 설계 | ✅ |
| 09-10 | **T4-1 배치 슬롯 진행** — 6개가 슬롯 0 한 칸에 겹치던 실기 설정을 발견. 슬롯 순서를 파라미터로 지정(런 5) | ✅ |
| 09-10 | **배치가 들쭉날쭉한 진짜 원인** — 직선 이동 가설을 계측으로 반증하고, 도착 판정 허용오차 1.5°가 원인임을 규명. 0.3°로 조여 팔 오차 10~24mm → 1.9~5.3mm(런 6·7) | ✅ |
| 09-11 | **실기 티칭 격자가 평행사변형(84.26°)** 임을 발견 — 실기 데이터라 고치지 않고 시뮬 전용 파라미터로 분리, 실기 기본값 보존(런 8) | ✅ |
| 09-11 | **T4 계란판 메쉬 4차** — 화면에서 옆 컵 뚫림 결함을 보고 피치·컵 형상을 다시 설계. 인접 배치 오류 0(런 9) | ✅ |
| 09-11 | 씬의 익은 딸기 6 → **8** (안 익은 6 → 4, 총 12 유지) | ✅ |
| 09-11 | **T4b 적응 분할** — 후보가 몰린 분면만 2×2 세부 칸으로 쪼개 부모 자세에서 유도한 세부 자세로 재스캔. 오프라인 16칸 검사 도구 동반 | ✅ |
| 09-11 | **HUD 쿼드트리 패널** — 실행기 코드를 고치지 않고 함수 래핑으로 순회 상태(제외·분할·세부 칸)를 뷰포트에 실시간 표시 | ✅ |
| 09-12 | **런 10: 적응 분할 첫 실행** — nw·sw 분할, 세부 자세 5칸 중 4 수락·1 거부 뒤 부모 자세로 퇴화. 8 분리 / 7 배치 | ✅ |
| 09-12 | **실기 플래너 실패 경로 발견** — 이송 계획이 팔꿈치 반대 분기를 골라 스윙 가드에 거부 → 과실을 보드 앞에서 놓음. 오프라인 재현(해당 후퇴 자세는 9칸 전부 거부, 실기 격자에서도 동일), 런 11 로 간헐성 확인. **고치지 않고 한계로 기록** | ✅ 기록 |
| 09-14 | **검증 원칙 채택 + 원복** — 이 프로젝트가 09-08·10 에 올렸던 스윙 가드 3값(J1 75→95, J2 100→130, J3 120→175)과 J6 운용 한계(±225→±360), 배치 실패 시 동작 기본값을 전부 실기 원본으로 되돌림 | ✅ |
| 09-14 | **E0509 공식 URDF 대조** — 시뮬이 공식 제원보다 관대한 곳 정리. 브릿지 관절 한계·MoveJoint 사다리꼴 속도 프로파일·실행 속도 배율 1.0, 시뮬 로봇 J2/J3/J5 한계를 실기 값으로. 실기가 두산 MoveIt 설정보다 큰 가속도를 요청하는 것은 자르지 않고 기록만 | ✅ |
| 09-14 | **J6 설정 통일안 오프라인 A/B** — 넓힌 값 6/8, 원본 4/8, 설정 통일 4/8. 통일로는 회복되지 않음을 확인 | ✅ |
| 09-14 | 적응 분할 깊이 2 자세를 **실기 티칭 평면(보드에서 377mm)** 으로 옮기고 세부 칸 시야 모사 추가. D455 최소 인식 거리는 실기 설정 로그가 없어 단정하지 않고 기록 | ✅ |
| 09-15 | **런 12: 원본 값 복원 첫 실행** — 7 분리 / 4 배치 / 3 이송 거부(J6 spline jump) / 1 직선 진입 실패. 예상 4/8 과 일치 | ✅ |
| 09-15 | **T4c** — 트레이 밖에서 놓은 과실을 공중에 멈춰 두던 표현을 **중력 낙하**로 바꾸고 바닥 콜라이더 신설, HUD 완료 줄에 `배치 n · 낙하 m` | ✅ 헤드리스 |
| 09-15 | **런 13: T4c 첫 화면** — 낙하 과실 하나가 20mm 바닥을 뚫고 사라짐(1.4m 낙하, 스텝당 88mm) → 바닥 두께 1m + 연속 충돌 검출(CCD), 낙하 3초 뒤 정지 위치 로그. 헤드리스 재검증 통과. 노드를 재시작하지 않고 재트리거해 트레이 슬롯 포인터가 이어진 런이라 **배치 수는 측정에서 제외**. 떨어진 과실이 아래 안 익은 과실에 걸리는 것은 물리 그대로 두고 기록만 | ✅ 수정 |
| 09-15 | **런 14: T4c 완료 기준 런** — 노드 재기동. 8 파지 / 4 배치 / 4 낙하, 낙하 4개 전부 바닥에 정지(관통 0), HUD 완료 줄 카운트가 브릿지·플래너와 일치. 이송 거부는 J6 spline jump 3 + J3 스윙 201.1° > 120°(원본 가드) 1 | ✅ 로그 / 화면은 사용자 확인 |
| 09-15 | **T4d 배치 변경 런 5개** — 서로 다른 무작위 배치 5개가 재로드·노드 기동·트리거 자동화로 전부 완주. 배치 실패는 시연 배치와 같은 실기 가드. **새 한계: 보드 가장자리 위쪽 과실 8개는 파지 후보 전부 IK 해 없음** — 높은 과실 처리가 로드 불가 프로파일에만 걸려 있어 기록만. 분할은 배치마다 다른 분면. 30 런 비율·신뢰구간은 하지 않기로 계획 개정 | ✅ |

---

## 2. 현재 위치 — 작업 번호 T

마일스톤 번호 M1~M4 는 09-14 에 동결된 이력이다. 현행 번호는 T 만 쓴다.

| 순서 | 작업 | 상태 |
|---|---|---|
| T1 | 09-09 수정본 END-TO-END 재완주 | ✅ 09-10 (런 1~3) |
| T2 | 딸기 키네마틱 부착 | ✅ 09-10 (런 4) |
| T3 | 정적 덩굴 메쉬 | ✅ 09-10 |
| T4 | 배치 슬롯 진행 + 계란판 메쉬 + 익은 8/4 | ✅ 09-11 (런 5~9) |
| T4b | 적응 분할 + HUD 쿼드트리 패널·세부 칸 하이라이트 | ✅ 09-12 실행(런 10), 09-14 보강(런 12). 화면 육안 확인 일부 남음 |
| **T4c** | 실기 원본 값 복원 뒤 실패를 정직하게 보여주기 — 낙하 물리, HUD 낙하 카운트, 런 12·13·14 | ✅ 09-15 (런 12~14, 화면 육안은 사용자 확인) |
| **T4d** | 서로 다른 과실 배치 3~5개 완주 + 배치별 기록 — 시연 배치에만 맞춘 것이 아님을 보이고, 새 배치가 드러내는 실기 노드 한계를 기록. **09-15 개정: 30 런 비율·신뢰구간은 버림**(비교 대상 없음, 모집단이 자체 정의, 원인 이미 확정) | ✅ 09-15 (배치 5개) |
| T5 | 녹화 — 롱샷·클로즈업 카메라 2대, 런 2회. 실패 장면도 자막 없이 그대로 | ☐ |
| T6 | 수치 갱신·결함 최종 집계·문서 정합·원격 푸시 | ☐ |

**정직한 현황 한 단락.** 수확 시퀀스(스캔→탐지→계획→접근→파지→분리→이송→배치→복귀)는 09-10 부터 매 런 끝까지 돈다.
09-12 까지는 이 프로젝트가 실기 가드를 넓혀 둔 상태라 배치가 거의 다 성공했다. 09-14 에 그 완화를 스스로 찾아 원본으로 되돌렸고,
그 뒤로는 익은 8개 중 4개만 트레이에 놓인다. 나머지는 실기 플래너가 이송 계획을 거부해 보드 앞에서 놓는다.
원인은 둘 다 실기 플래너 쪽이다. 하나는 목표 자세의 팔꿈치 분기를 시작 자세와 무관하게 고르는 것(J3 스윙 거부)이다. 다른 하나는 티칭된 트레이 자세의 손목 방향과 후퇴 자세의 조합이 원본 J6 운용 한계 창에 들어가지 않는 것(J6 spline jump 거부)이다. 둘 다 고치지 않고 재현 조건·원인 위치와 함께 기록했다.
실기 로봇에서도 같은 일이 나는지는 확인하지 못했다.

---

## 3. 아키텍처와 직접 만든 것

```
실기:  비전(YOLO) → scan_executor → curobo_planner → 두산 제어기(dsr_msgs2) → 실물 e0509
시뮬:  fake_vision → scan_executor → curobo_planner → sim_executor_bridge → Isaac Sim (py3.11)
       (입력단 모킹)  ↑───── 실기 플래너 이식 구간 ─────↑   (가상 두산 제어기, py3.10)
```

양 끝단만 바꾸고 가운데 플래너 2노드는 ROS 2 인터페이스 계약(토픽·서비스·액션·QoS)을 그대로 지켜 이식했다.
두 파이썬 런타임(ROS 3.10 / Isaac 3.11)은 라이브러리를 공유할 수 없어 ROS 토픽만을 경계로 연결했다.

| 직접 구현·구축 | 내용 |
|---|---|
| 로봇 애셋 파이프라인 | e0509 + RH-P12-RN + 직접 설계·3D 프린팅한 커스텀 파츠 + D455 를 단일 xacro→URDF 로 통합, Isaac 임포트 |
| USD 씬 구조 | 애셋/레이아웃/물리 레이어 분리. 순정·임포트 애셋은 읽기 전용, 수정은 상위 레이어 오버라이드로만 |
| 가상 제어기 (`strawberry_sim_core`) | MoveJoint/MoveLine/MoveSplineJoint + 그리퍼 액션. MoveLine 은 cuRobo IK 실연산, 도착 대기·관절 한계 방어선·사다리꼴 속도 프로파일. 파지 판정은 조우 물림 기하 |
| 비전 모킹 (`fake_vision_node`) | Isaac 딸기 prim 좌표를 실기 비전과 같은 토픽·메시지로 발행, 분면·세부 칸 시야 모사 |
| Isaac 내부 스크립트 | 딸기 좌표 발행, `/joint_command` 주입, 키네마틱 부착·해제, 트레이 밖 낙하 |
| 인-뷰포트 HUD | 노드 생존·단계·쿼드트리 순회 상태·완료 결과(`배치 n · 낙하 m`). 실행기 코드는 고치지 않고 함수 래핑 프로브로 상태를 뽑음 |
| 씬 생성·검증 도구 | 덩굴·계란판 생성기와 검증기(일반 python3 + usd-core, Isaac 불필요) |
| 오프라인 재현 도구 | 트레이 슬롯 도달성 검사(플래너와 같은 MotionGen·충돌 월드, 런 JSONL 의 후퇴 자세 입력), 세부 칸 스캔 자세 16칸 검사 |
| 이식 변경 추적 | `_baseline/` 원본 스냅샷 + 한 줄 변경 로그 + diff 재생성 스크립트 |
| 파라미터 정합 검사 | `check_params.py` — 여러 파일에 흩어진 기준값 기계 대조 |
| 런 로그 보존 규약 | `log/m3/<run_id>/` 에 노드 로그 4종 + 플래너 JSONL + Isaac Kit 로그, 런마다 판정표 |

---

## 4. 해결한 기술 문제 (경력기술서 소재, 강한 순)

번호 뒤 괄호는 근거 위치. 수치는 전부 로그·실측에서 나온 것만 적었다.

1. **IK elbow-flip 의 진짜 원인 — API 오용** ✅ (`PLANNER_CHANGES.md` 09-08).
   처음엔 팔꿈치 반전 해를 최근접 해 선택 + 관절 이동 45° 가드로 막았다(08-29 실전 검증, 다이브 0건).
   이후 cuRobo `solve_single` 에 현재 관절을 `seed_config` 가 아닌 `retract_config` 자리로 넘기고 있었음을 발견. 수정 후 45mm 직선 진입 **6/6 실패 → 6/6 통과, 스텝당 관절 이동 1°**.
   → "증상 가드 → 근본 원인" 두 단계 서사.

2. **시뮬이 실기보다 관대했던 곳을 스스로 찾아 되돌림** ✅ (09-14, `docs/e0509_spec_audit.md`, `PLANNER_POLICY_v2.md` §0-1).
   초기 기동 때 과실별로 막히는 것을 풀려고 실기 스윙 가드 3값과 J6 운용 한계를 넓혀 둔 이력을 발견. 검증 대상 코드를 넓혀 통과시킨 것이라 전부 원복했다.
   원복 영향은 실행 전에 오프라인으로 산정했다(최근 3런의 실행 계획 109개 중 거부 1, J6 통일안 A/B 로 배치 4/8 예상). 런 12 실측이 예상과 일치했다.
   공식 URDF·두산 MoveIt 설정과도 대조해 시뮬 로봇 관절 한계·속도 프로파일을 실기 값에 맞췄다.

3. **실기 플래너의 실패 경로 규명 — 고치지 않고 기록** ✅ (09-12, `portfolio/H_scope_decisions.md` §9).
   8개 연속 런에서 이송 계획이 가끔 거부됐다. 로그와 오프라인 재현으로 원인을 좁혔다. 플래너가 목표 자세의 팔꿈치 분기를 시작 자세와 무관하게 고르고, 반대 분기면 가드가 막고 재시도가 없다.
   시뮬 쪽 손잡이(과실 위치·슬롯 순서)로는 피할 수 없음을 확인했고, 실기 티칭 격자에서도 재현됐다. 수정 후보는 적었지만 채택하지 않았다.

4. **배치 편차의 진짜 원인 — 가설 반증 → 계측 → 원인** ✅ (09-10, 런 5~7).
   배치가 들쭉날쭉한 원인을 직선 이동 드리프트로 의심했으나 종점 오차 계측(측방 0.1mm)으로 반증.
   실제 원인은 도착 판정이 6축 각각 1.5°를 허용해 손끝 RMS 23.6mm 가 남는 것. 0.3°로 조여 팔 오차 **10.0~24.3mm → 1.9~5.3mm**.

5. **툴 길이 모델 불일치로 인한 과실·보드 관통, 원인 3개 분해** ✅ (09-07 규명, 09-10 실행 확인).
   (a) URDF 부착 오리진이 0 이라 플래너 ee_link 가 플랜지 위치인데 TCP 를 +160mm 로 가정(실제 파지부 +233mm) (b) 커스텀 파츠가 충돌 모델에 없음 (c) cuRobo 충돌 월드 공백.
   → 툴 오프셋을 파라미터로 분리(실기 기본값 불변), 충돌 구체·보드 큐보이드 추가. 09-10 재완주에서 사용자 육안으로 보드 미진입 확인.

6. **쿼드트리 순회 복원 + 가지치기 + 적응 분할** ✅ (09-09~14, `portfolio/G_quadtree_interview.md`).
   실기 최종본이 발표용으로 축소하며 잃은 원안 시퀀스(분면 순회·분면별 스캔 자세)를 복원하고, 빈 분면 가지치기로 런 **364.9 → 240.6초**.
   적응 분할은 런 10·12 에서 메커니즘이 돌았다. **시뮬에서는 분할의 이득이 측정되지 않는다**(좌표가 정답값이라 가까이 가도 인식이 좋아질 게 없음). 이 한계를 먼저 말한다.

7. **시뮬 파지 판정을 실기 판정 로직과 같은 기준으로 모델링** ✅ (09-07, 09-10 고도화).
   가상 그리퍼가 명령값을 그대로 반향해 무엇을 잡아도 빈손이던 것을, 실기가 전류가 아닌 조우 위치로 판정한다는 점에 맞춰 조우 물림 구간 기하로 교체.
   판정 반경을 넓혀 실패를 성공으로 오판하던 대증요법을 스스로 되돌린 이력도 로그에 있다.

8. **실기 티칭 데이터의 결함을 실기 기본값 보존 원칙으로 처리** ✅ (09-10~11).
   과실 6개가 슬롯 0 한 칸에 겹치는 실기 설정, 트레이 격자가 직교가 아닌 평행사변형(84.26°)인 티칭 오차를 발견.
   실기 기본값은 그대로 두고 시뮬 전용 파라미터(기본값 = 실기 동작)로 분리했다.

9. **씬 기하와 플래너 벽 모델의 131.7mm 불일치 규명** ✅ (09-05~09).
   플래너 경고 문구는 "FK calibration drift" 였으나 실제 원인은 씬 배치. 보드 기준값이 6개 파일에 분산된 구조를 `check_params.py` 로 기계 검사(실제로 2곳 누락 발견).

10. **MoveSpline deg/rad 혼용 회귀 + 방어선** ✅ (09-09). 모든 관절에 57.3배 명령이 나가 팔이 여러 바퀴 돌았는데, 콜백이 항상 성공을 돌려 로그에 흔적이 없었다. 수정 + 관절 한계 초과 명령 발행 차단.

11. **딸기 키네마틱 부착** ✅ (09-10). 물리 파지는 조우 간격과 과실 단면 때문에 구조적으로 불가 → 파지 판정 순간의 상대 변환 추종으로 설계. 부수 결함(마지막 딸기 부착 뒤 빈 좌표 배열을 발행하지 않아 하류가 23.7초 정체)도 발견·수정.

12. **가상 제어기를 실기 서비스 의미론에 맞춤** ✅ (09-09~14). 실기 서비스는 모션이 끝나야 응답한다 → 도착 대기 추가. MoveJoint 즉시 발행(사실상 무한 가속)을 실기 명령 속도·가속도의 사다리꼴 프로파일로.

13. **실패를 가리지 않는 시각화** ✅ (09-15). 이송 거부로 놓은 과실이 공중에 멈춰 보이면 실패가 가려진다 → 중력 낙하 + HUD 낙하 카운트. 첫 화면 확인(런 13)에서 고속 낙하 과실이 얇은 바닥을 뚫는 것을 발견해 바닥 두께·연속 충돌 검출로 고쳤다. 같은 런에서 노드 재시작 없이 재트리거하면 트레이 슬롯 포인터가 이어져 측정 조건이 달라진다는 것도 확인해 실행 절차에 넣었다. 런 14 에서 낙하 4개 전부 바닥 정지, HUD 카운트 일치.

14. **배치를 바꿔 실기 노드의 숨은 한계를 찾음** ✅ (09-15, T4d, `H_scope_decisions.md` §10). 과실 배치 생성·씬 재로드·노드 기동·지표 수집을 자동화해 서로 다른 배치 5개를 무인으로 돌렸다. 30 런 성공률 대신 배치별 기록을 택했다(비교할 실기 비율이 없고 분포는 내가 정했으므로). 그 결과 시연 배치에 없던 높이에서 실기 파지 경로의 도달 포락선을 찾았다. 실기에 대응 코드가 있지만 로드 불가 프로파일에만 걸려 있어 고치지 않고, 배치 범위를 좁혀 가리지도 않았다.

15. **기타 기반 작업** ✅. fail-open → fail-closed 브릿지 재설계(07-10), 순정 D455 중첩 강체로 인한 PhysX 초기화 실패를 오버라이드로 해결(07-09), 드라이브 게인 튜닝, TOOL 좌표계 상대 이동(FK 검증 오차 0.003mm), 그리퍼 관절쌍 상한 불일치("가위 모양") 규명, 의존성 비호환(cuRobo↔warp, colcon↔setuptools, rosidl 한글 경로) 해결.

---

## 5. 정량 지표

### 5-1. 런 기록 (`log/m3/`, 09-10~15)

익은 딸기는 런 1~9 가 6개, 런 10 부터 8개다. "분리"는 `PICK COMPLETE`, "배치"는 트레이 안 릴리스.

| 런 | 날짜 | 검증한 것 | 분리 / 배치 | 전체 시간 |
|---|---|---|---|---|
| 1 | 09-10 | 09-09 수정본 재완주, 4분면 전수 순회 | 6 / 과실 부착 전 | 364.9초 |
| 2 | 09-10 | 쿼드트리 가지치기 | 6 / 과실 부착 전 | 240.6초 |
| 3 | 09-10 | MoveLine 스텝 상한 | 6 / 과실 부착 전 | 208.5초 |
| 4 | 09-10 | T2 키네마틱 부착 | 6 / 6 (슬롯 0 한 칸) | — |
| 5~7 | 09-10 | 슬롯 진행, 배치 간격·편차 원인 규명 | 6 / 6 | 222.5초(런 7) |
| 8~9 | 09-11 | 격자 직교화, 계란판 3·4차, 인접 배치 | 6 / 6 | 217.2초(런 9) |
| 10 | 09-12 | 적응 분할 첫 실행, 익은 8 | 8 / 7 | 283.0초 |
| 11 | 09-12 | 같은 설정 재실행(간헐성 확인) | 7 / 6 | — |
| 12 | 09-15 | **실기 원본 값 복원** 첫 실행 | 7 / 4 | 277초 |
| 13 | 09-15 | T4c 낙하 표현 첫 화면 — 노드 무재시작 재트리거라 **측정 제외** | 8 / (슬롯 포인터가 이어져 조건 다름) | — |
| 14 | 09-15 | **T4c 완료 기준 런** (노드 재기동, 두꺼운 바닥) | 8 / 4 (낙하 4) | 292초 |

- 런 1~14 모두 시퀀스가 끝까지 진행됐다. 그 이전 완주는 5회(07-10 / 07-16 / 08-29×2 / 09-07).
- 런 12 의 배치 실패 3건은 전부, 런 14 는 4건 중 3건이 이송 계획의 `J6 spline jump` 거부다. 런 14 의 나머지 1건은 J3 스윙 거부다. 두 런 모두 배치 4 로, 원본 값 복원 뒤의 오프라인 예상(4/8)과 일치한다.
- 런 12 의 진입 실패 1건(ripe_08)은 런 13·14 에서는 나지 않았다. 런 14 는 pre-approach 해가 런 12 와 달랐다. 런마다 계획 해가 달라 나는 간헐 실패이며, 어느 관절이 막는지는 미확인이다.
- **T4d 배치 변경 런 5개** (`log/m3/random/README.md`, 비율로 쓰지 않는다): 합계 파지 판정 32 / 분리 32 / 배치 19 / 낙하 13, 런 233~264초. 배치 실패 J6 spline 10 · J3 swing 2 · J6 swing 1. 미파지 8 은 전부 보드 가장자리 위쪽 과실. 직선 진입 실패 0.

### 5-2. 규모와 검증 수치

| 지표 | 값 | 출처 / 재생성 |
|---|---|---|
| 빌드되는 ROS 2 패키지 | **5개** (이식 플래너 1 · 가상 제어기 1 · 데이터 1 · 인터페이스 2) | `ls build/` |
| `strawberry_motion` 파이썬 (이식 플래너 + 이 프로젝트가 추가한 모듈·도구) | **67파일 / 16,979줄** (09-15). 09-09 는 64 / 15,975 | `find src/strawberry_motion -name '*.py'` |
| 가상 제어기(ROS 측) | **10파일 / 1,891줄** (09-15). 09-09 는 1,532 | `src/strawberry_sim_core/` |
| Isaac Sim 스크립트 (HUD·씬 생성기 포함) | **16파일 / 4,083줄** (09-15, HUD 6파일 1,479줄). 09-09 는 8 / 1,736 | `strawberry_harvest/scripts/` |
| 플래너·씬 변경 로그 | **145항목** (09-15) | `grep -c '^- \[' PLANNER_CHANGES.md` |
| 원본 스냅샷 대비 diff | 09-10 재생성 변경 592줄 (이후 증가, T6 에서 재생성) | `bash scripts/check_planner.sh` |
| USD 애셋·씬 파일 | **20개**, 딸기 prim **12개**(익은 8 / 안 익은 4) | `find strawberry_harvest -name '*.usd*'` |
| 원복 영향 사전 산정 | 실행 계획 109개에 원본 가드 재적용 시 거부 1 / J6 A/B: 넓힌 값 6/8 · 원본 4/8 · 설정 통일 4/8 | `docs/e0509_spec_audit.md` §7, `H_scope_decisions.md` §9 |
| 도착 허용오차 조임 효과 | 팔 오차 10.0~24.3mm → **1.9~5.3mm** | `log/m3/README.md` 런 7 |
| 가지치기 효과 | 364.9초 → **240.6초** | `log/m3/README.md` 런 2 |
| MoveLine IK FK 검증 | **0.003mm**, FK→IK 왕복 ~1µm | `docs/next_steps.md` |
| elbow-flip 근본 수정 효과 | 6/6 실패 → **6/6 통과**, 스텝당 1° | `PLANNER_CHANGES.md` 09-08 |
| 결함 처리 | 09-07 집계 17건 + 09-08~15 추가 규명·수정 다수. 최종 집계는 T6 | `portfolio/E_metrics.md` §5 |
| 기술 스택 | Isaac Sim 5.1.0 · ROS 2 Humble · cuRobo 0.7.8(소스 빌드) · PyTorch 2.6.0+cu124 · CUDA 12.4 · warp 1.9.1 · Py 3.10/3.11 · Ubuntu 22.04 · RTX 4060 Ti | `E_metrics.md` §6 |

### 5-3. 미해결 (전부 기록·회피책 있음)

| 이슈 | 처리 |
|---|---|
| 트레이 이송 계획 거부 — J3 스윙(목표 IK 분기 선택, 간헐) · J6 spline jump(티칭 트레이 손목 방향 × 후퇴 자세가 원본 ±225 창 밖, 설정 통일로도 회복 안 됨) | 실기 플래너 한계로 기록, 수정 안 함 (`H_scope_decisions.md` §9, `docs/e0509_spec_audit.md` §7) |
| 보드 가장자리 위쪽 과실(중심 z 790~897mm, \|x\| ≥ 185mm) 파지 후보 전부 IK 해 없음 | 실기 노드 한계로 기록. 높은 과실 처리가 로드 불가 프로파일에만 걸려 있음. 배치 범위도 좁히지 않음 (`H_scope_decisions.md` §10) |
| 실기가 두산 MoveIt 설정보다 큰 가속도를 요청 | 실기 노드 기록 사항, 시뮬은 자르지 않고 로그만 |
| D455 최소 인식 거리 — 깊이 2 자세의 카메라-과실 거리가 해상도에 따라 경계 밖일 수 있음 | 실기 해상도 설정 로그가 없어 단정하지 않음. 시뮬 비전에 거리 모델 없음 = 이 축은 시뮬이 관대, 한계로 기록 (`docs/d455_min_range.md`) |
| 직선 진입 실패 간헐(ripe_08, 런 11·12 — 런 10·13·14 성공) | 간헐(cuRobo 해 선택). 파일럿 4 런에서 재발 0 → 선행 과제로 두지 않는다(09-15 판정, `log/m3/random/README.md`) |
| 그리퍼가 이웃 과실과 겹쳐 지나감 | 파지 접촉 물리는 범위 밖으로 판정 (`H_scope_decisions.md` §4) |
| 커스텀 파츠 PhysX 콜라이더 부재, 그리퍼 l2/r2 충돌 구체 부재 | 계획 단계에서 차단, 부분 대체 |

---

## 6. 표현 가이드 — 쓸 수 있음 / 없음 / 조건부

### 쓸 수 있음
- 실기용 모션 플래너를 Isaac Sim 환경으로 **이식**하고 가상 dsr_msgs2 서비스 서버를 구현해 **실기 제어 스택을 하드웨어 없이 구동**
- 스캔–탐지–계획–접근–파지–분리–이송–배치–복귀 **수확 핵심 경로**를 하나의 런으로 연속 실행 (익은 딸기 8개 씬)
- 시뮬이 실기보다 관대해진 설정을 스스로 찾아 원본으로 되돌리고, 그 결과 떨어진 배치 수(8개 중 4개)를 그대로 공개
- 실기 플래너의 간헐적 실패 경로를 오프라인으로 재현하고 원인 코드 위치까지 좁혀 **한계로 기록**
- cuRobo IK elbow-flip: 가드로 1차 방어 → **API 오용(seed_config) 근본 원인 규명**, 6/6 실패 → 6/6 통과
- 도착 판정 허용오차가 배치 편차의 원인임을 계측으로 규명, 팔 오차 10~24mm → 1.9~5.3mm
- 쿼드트리 분면 순회·가지치기로 런 시간 364.9 → 240.6초
- 시뮬 파지 판정을 실기 판정 로직(조우 위치 기반)과 같은 기준으로 모델링
- 씬 기하와 플래너 벽 모델의 **131.7mm 불일치 규명·정합**(clamp 경고 소멸)
- 이식 변경을 원본 스냅샷 대비 diff 로 상시 재생성 + 한 줄 변경 로그
- USD 레이어 컴포지션 — 순정 애셋은 건드리지 않고 오버라이드 레이어로만 수정
- "관통 없이" — **보드·타겟 과실 기준**으로만 (09-10 사용자 육안 확인). 이웃 과실과 그리퍼 겹침이 있으니 범위를 붙여 쓴다
- 서로 다른 과실 배치 5개에서 시퀀스가 사람 손 없이 끝까지 돌았고, 배치 실패는 시연 배치와 같은 실기 가드에서 났다
- 배치를 바꿔 시연 배치에서 안 보이던 실기 파지 경로의 도달 한계(보드 가장자리 위쪽 과실)를 찾아 기록했다
- ⛔ 대신 "무작위 배치 성공률 X%" / "신뢰구간" / "통계적으로 검증"은 쓰지 않는다

### 쓸 수 없음
- ⛔ "0줄 수정 / 무수정 구동 / 원본 노드를 그대로 사용" → **"이식(port)"**
- ⛔ "접근 오차 131.7 → 0.0mm" → "131.7mm 불일치 규명·정합"
- ⛔ "파지 성공 / 수확 성공 / 파지 물리 검증" → 검증한 것은 **판정 로직과 시퀀스**
- ⛔ "전체 타겟 수확 / 8개 전부 배치" → 원본 값 기준 배치는 8개 중 4개
- ⛔ "전체 시퀀스 재현" → "수확 핵심 경로" (병든 과실 분기는 범위 밖)
- ⛔ "병든 과실 선별 / 솎아내기"
- ⛔ "딸기 24개 / 72개" → 씬의 딸기는 12개, 그 숫자는 스캔 프레임 누적 검출 건수
- ⛔ "적응 분할로 파지·인식이 좋아졌다" → "메커니즘이 런에서 돌았다"까지만. 시뮬에서 이득은 측정 안 됨
- ⛔ "세부 자세 유도가 어느 분면이든 된다" → 16칸 중 4칸은 IK 해가 없어 부모 자세로 퇴화
- ⛔ "실기 노드를 이렇게 고치면 동작한다" / "실기 로봇도 같은 실패를 낸다" → 둘 다 확인하지 않았다. 재현 조건과 원인 위치까지만
- ⛔ "실기가 깊이 2 자세에서 봤다" → 티칭 자세가 한 평면에 있다는 사실까지만

### 조건부
- (09-15 T4d 완료로 해제 — 위 "쓸 수 있음"으로 옮김)
- 낙하 장면이 포함된 영상 — 런 14 로그로 준비됨, 화면 육안 확인 뒤

---

## 7. 남은 작업과 리스크

| 항목 | 내용 |
|---|---|
| 화면 육안 | 런 14 의 HUD 트리·세부 칸 하이라이트·완료 줄·낙하 장면을 사용자가 확인한다 |
| T4d | ✅ 09-15 완료 — 배치 5개, 기록표 `log/m3/random/README.md`, 새 한계 H §10. T5 전에 Isaac 씬 재로드(배치 런 뒤) |
| T5 녹화 | 롱샷·클로즈업 카메라 2대, 런 2회. 실물 대조 클립쌍(분리·영역 이동·배치). 절차 `portfolio/README.md` |
| T6 | 결함 최종 집계, 산출물 C·E 수치 갱신, diff 재생성, **원격 푸시**(07-07 이후 없음) |
| 리스크 | 직선 진입 간헐 실패의 원인 미확인 · 무작위 배치에서 계란판 착지 봉투 밖 착지 가능성(`random_layout_audit.md` A5) · 배치별 로그 읽기 시간 |

---

## 8. 면접 예상 질문 대비 메모

- **플래너를 고쳤나** → 이식하면서 고친 것은 전부 원본 스냅샷 대비 diff 와 한 줄 로그로 남겼다. 09-14 부터는 원칙이 하나다. 실기 노드는 핵심 시퀀스 설계와 안 맞을 때만 고치고, 실패는 기록한다. 예전에 가드를 넓혔던 이력을 스스로 찾아 되돌렸다.
- **그럼 처음엔 왜 넓혔나** → 09-07 첫 배치 완주 직후 나머지 과실까지 시퀀스를 끝까지 보려고, 목표가 운용 한계 안인데 이동량 가드에 막힌 곳을 풀었다(당시 정책은 허용). 변경 로그에는 남았지만 "되돌릴 임시 완화"로 표시되지 않아 부채로 남았다. 09-14 원본 대조에서 찾아 영향을 먼저 산정한 뒤 원복했고, 재발을 막으려고 §0-1 을 정책으로 명문화했다. "추적되지 않았다"가 아니라 "기록은 있었지만 부채로 관리되지 않았다"로 말한다. 상세 답변 `H_scope_decisions.md` §9.
- **실패를 발견했는데 왜 안 고쳤나** → 고칠 수 있는 곳이 전부 검증 대상 코드 안이었다. 가드를 올리면 시뮬이 실기보다 관대해지고, 경로 계산을 바꾸면 검증 대상이 바뀐다. "내 코드는 고치고, 검증 대상은 기록한다" (`H_scope_decisions.md` §9).
- **그럼 어떻게 고치면 되나** → 후보(시작 자세 시드 IK, 경유 자세)는 기록했지만 채택하지 않았고, 동작한다고 주장하지 않는다. 실기 로봇에서 같은 실패가 나는지도 확인하지 못했다.
- **가장 어려웠던 버그** → elbow-flip(가드 뒤에 숨은 API 오용), 또는 배치 편차(가설을 계측으로 반증하고 도착 허용오차를 찾은 과정).
- **왜 파지 물리를 안 했나** → 실기 판정이 조우 위치 기반이라 시뮬 경계도 거기서 끊었다. 접촉력·변형은 대조 데이터가 없어 검증 불가능한 충실도다.
- **적응 분할의 효과는** → 시뮬에서는 측정되지 않는다고 먼저 말한다. 좌표가 정답값이라 가까이 가도 인식이 좋아질 게 없다. 검증한 것은 메커니즘과 비용이다 (`G_quadtree_interview.md` §6-2).
- **재현 가능한가** → 실행 스크립트 `scripts/run_nodes.sh`, 절차 `docs/run_guide.md`, 런마다 로그 보존과 판정표 `log/m3/README.md`, 오프라인 재현 도구.
- **무엇이 미완인가** → 영상, 원격 푸시. 실기 플래너의 이송 거부는 미완이 아니라 기록된 한계다.
- **왜 30 런 통계를 안 했나** → 비교할 실기 비율이 없고, 모집단은 내가 정한 분포라 신뢰구간을 좁혀도 실기로 옮겨지지 않는다. 실패 원인은 이미 확정됐다. 배치를 바꿔 돌린 이유는 세 가지다. 시연 배치에만 맞춘 게 아님, 분할 임계값이 배치 맞춤이 아님, 새 배치가 드러내는 실기 노드 한계 기록.
- **배치를 바꿔 보니 뭐가 나왔나** → 보드 가장자리 위쪽 과실 8개는 실기 파지 경로가 IK 해를 못 찾았다. 시연 배치에 없던 높이라 안 보이던 경로다. 실기 코드에 높은 과실 처리가 있지만 로드되지 않는 프로파일에만 걸려 있어 고치지 않고 기록했고, 배치 범위를 좁혀 가리지도 않았다. 실기 로봇도 못 잡는지는 확인하지 못했다 (`H_scope_decisions.md` §10).

---

## 9. 참고 파일 지도

| 알고 싶은 것 | 파일 |
|---|---|
| 범위·작업 순서 (최우선) | `SUBMISSION_PLAN.md` §4-0 |
| 이력서용 요약 스냅샷 | `PROJECT_SUMMARY.md` |
| 런별 판정표·로그 | `log/m3/README.md` |
| 정량 지표 + 출처 | `portfolio/E_metrics.md` |
| 면접 — 코드 워크스루·약점 | `portfolio/F_code_walkthrough.md` |
| 면접 — 쿼드트리·적응 분할 | `portfolio/G_quadtree_interview.md` |
| 면접 — 재현하지 않은 것·실기 플래너 한계 | `portfolio/H_scope_decisions.md` |
| 녹화 절차·슬라이드 | `portfolio/README.md` |
| 수정 원칙·표현 규칙 | `PLANNER_POLICY_v2.md` |
| 변경 이력 | `PLANNER_CHANGES.md` |
| 공식 제원 대조·원복 근거 | `docs/e0509_spec_audit.md` |
| 무작위 배치 전환 조사 | `docs/random_layout_audit.md` |
| D455 최소 거리 | `docs/d455_min_range.md` |
| 기준값(보드·툴·파지 기하) | `docs/parameters.md` |
| 실행 절차 | `docs/run_guide.md` |
| 원안 수확 시퀀스 | `PROJECT_GOAL.md` §1-2 |
| 배치 변경 런 기록(T4d) | `log/m3/random/README.md` |
| 마일스톤 이력(동결) | `PORTFOLIO_SPRINT.md` |
