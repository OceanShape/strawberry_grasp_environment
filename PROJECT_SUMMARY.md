# 딸기 수확 로봇 시뮬레이션 프로젝트 요약

> **담당: 이력서·포트폴리오용 스냅샷.** 수치는 [`docs/parameters.md`](docs/parameters.md),
> 실행은 [`docs/run_guide.md`](docs/run_guide.md), **제출 범위·작업 순서는 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md)**,
> 마일스톤 이력은 [`PORTFOLIO_SPRINT.md`](PORTFOLIO_SPRINT.md) 가 기준이다.
> 여기 숫자가 그쪽과 다르면 **여기가 낡은 것**이다.

> 이력서·경력기술서 작성용 자료. 이 문서만으로 프로젝트 전모를 파악할 수 있도록 작성됨.
> 작성일: 2026-08-29 · **최종 갱신: 2026-09-17** (상태 줄은 09-15 기준, §4 15·19번·§5 5.5행·검증 태도 줄의 NW 서술 정정·J6 사례만 09-17) (수치는 [`PROGRESS_REPORT.md`](PROGRESS_REPORT.md) §5, 런별 근거는 [`log/m3/README.md`](log/m3/README.md))
> **취업 전략·지원서 채팅에 붙일 현황 브리핑은 [`PROGRESS_REPORT.md`](PROGRESS_REPORT.md)** 다. 이 문서는 경력기술서용 요약이다.
> 정량 지표의 출처·재생성 방법은 [`portfolio/E_metrics.md`](portfolio/E_metrics.md)에 따로 정리했다.
>
> **제출본이 주장하는 것은 하나다**: *실기 제어 스택을 하드웨어 없이 구동하고, 실기와 시뮬의 불일치를 찾아 정합했다.*
> 표현 규칙은 [`PLANNER_POLICY_v2.md`](PLANNER_POLICY_v2.md) §5 — "0줄 수정", "131.7→0.0mm", "병든 과실 선별", "전체 시퀀스 재현", "파지 성공" 은 쓰지 않는다.

---

## 1. 프로젝트 한 줄 요약

**팀에서 작성한 두산 로봇 모션 플래너를 NVIDIA Isaac Sim 환경으로 이식하고, 가상 두산 제어기(dsr_msgs2 서비스 서버)를 구현해 하드웨어 없이 수확 시퀀스를 구동·검증하는 Digital Twin 파이프라인 구축** — 실물 로봇 없이 수확 모션의 통신·시퀀스·궤적을 검증하는 시뮬레이션 인프라.

- **도메인**: 농업용 딸기 수확 로봇 (두산 e0509 6축 협동로봇 + 로보티즈 RH-P12-RN 그리퍼 + Realsense D455)
- **역할**: 시뮬레이션 환경 구축 전체 담당 1인 (URDF/USD 애셋 파이프라인, 가상 제어기 브릿지, 통합 테스트)
- **기간**: 2026년 상반기 \~ 진행 중. 스캔부터 배치·복귀까지 수확 핵심 경로가 09-10 부터 매 런 끝까지 돈다(`log/m3/` 런 1\~14).
  09-14 에 시뮬이 실기보다 관대했던 설정을 전부 원본으로 되돌렸고, 그 뒤 익은 딸기 8개 중 **트레이 배치는 4\~6개**다(런마다 이송 거부 수가 다름)(나머지는 실기 플래너의 이송 계획 거부, 한계로 기록).
  서로 다른 과실 배치 5개에서도 끝까지 돌았다(T4d, 09-15). 남은 것은 시연 영상(T5)·수치 정리와 원격 푸시(T6)다 (`SUBMISSION_PLAN.md` §4-0, 그 이후는 이 리포에서 하지 않는다)

## 2. 기술 스택

| 분류 | 기술 |
|---|---|
| 시뮬레이터 | NVIDIA Isaac Sim 5.1.0 (USD, PhysX, Articulation) |
| 미들웨어 | ROS 2 Humble (rclpy, colcon, 커스텀 srv/action 인터페이스) |
| 모션 플래닝 | NVIDIA cuRobo v0.7.8 (GPU 가속 MotionGen / IK, 소스 빌드) |
| 로봇 기술 | URDF/xacro, USD 레이어 컴포지션(payload/reference/override), FK·IK, PD 드라이브 튜닝 |
| ML/GPU | PyTorch 2.6.0 + CUDA 12.4, RTX 4060 Ti |
| 언어/환경 | Python 3.10(ROS)·3.11(Isaac Sim) 이중 런타임, Ubuntu 22.04 |

## 3. 아키텍처

핵심 설계: 실제 시스템의 **양 끝단(비전 입력·로봇 제어 출력)을 시뮬레이션으로 대체**하고, 가운데의 모션 플래닝 노드 2종은 실기 코드를 이식해 사용.

```
실기:  비전(YOLO) → scan_executor → curobo_planner → 두산 제어기 → 실물 e0509
시뮬:  fake_vision → scan_executor → curobo_planner → sim_executor_bridge → Isaac Sim
       (입력단 모킹)   ↑── 실기 플래너 이식 구간 ──↑      (가상 두산 제어기)
```

**직접 개발/구축한 구성 요소:**

| 구성 요소 | 내용 |
|---|---|
| 로봇 애셋 파이프라인 | 두산 e0509 + RH-P12-RN + 커스텀 파츠 + D455를 단일 xacro→URDF로 통합, Isaac Sim 임포트 |
| USD 씬 구조 | 애셋/씬/물리 레이어 분리. **USD 애셋 파일은 직접 고치지 않고 오버라이드 레이어로만 수정**하는 원칙 적용 — 협업 가능한 USD 구성 |
| `fake_vision_node` | Isaac Sim 딸기 prim 좌표를 실기 비전 노드와 **동일한 토픽·메시지·QoS**로 발행 (플래너가 차이를 인지 못함) |
| `sim_executor_bridge_node` | 두산 dsr_msgs2 서비스(MoveJoint/MoveLine/MoveSplineJoint)·그리퍼 액션을 모방하는 가상 제어기. MoveLine은 cuRobo IK로 실제 연산 (BASE/TOOL 좌표계, 최근접 해 선택, 관절 점프 가드) |
| Script Editor 브릿지 | Isaac Sim 내부(py3.11)에서 rclpy로 딸기 좌표 발행 + `/joint_command`를 articulation에 주입 — 두 파이썬 세계를 ROS 토픽으로만 연결 |
| 이식 변경 추적 | 원본 스냅샷(`_baseline/`) 대비 diff를 상시 재생성해, 이식 과정에서 무엇을 왜 바꿨는지 언제든 설명할 수 있게 관리 — `PLANNER_CHANGES.md`(101건), `scripts/check_planner.sh` |
| 파라미터 정합 검사 | `check_params.py` — 보드 y 처럼 6개 파일에 흩어진 값을 기계적으로 대조. 실제로 2곳 누락을 잡아 "딸기 앞을 집는" 사고를 선제 차단 |
| 상태 HUD | `isaac_sim_viewport_display.py` 의 HUD 패널 — Isaac Sim 뷰포트 위에 노드 생존·현재 단계·**쿼드트리 순회 상태(제외·분할·세부 칸)**·타겟·비대상 수·결과 바(`배치 성공 n · 배치 실패 m · 분리 실패 k`)를 얹는다. 실행기 코드는 고치지 않고 함수 래핑 프로브로 상태를 뽑고, HUD 는 스냅샷 파일을 읽기만 한다. 화면 하나만 녹화하면 동작과 상태가 같이 찍힌다. **(09-18)** '분리 실패' 는 프리어프로치 도달 후 배치 호출 직전까지(진입·하강·그리퍼 닫기·파지 판정·당김·후퇴) 어디서 막히든 세는 것으로 범위가 넓어졌고, 단계 표시의 DETACH 라벨은 '분리' 대신 '당김' 이다 |
| 오프라인 재현 도구 | 트레이 슬롯 도달성 검사(플래너와 같은 MotionGen·충돌 월드에 런 JSONL 의 후퇴 자세 입력), 세부 칸 스캔 자세 검사, 덩굴·계란판 생성기·검증기 — 런에서 본 실패를 Isaac 없이 재현 |
| 통합 테스트 체계 | 4개 노드 + Isaac Sim 동시 기동, fail-closed 6단계 안전 게이트 통과, END-TO-END 시나리오 완주 |

## 4. 해결한 주요 기술 문제 (경력기술서 소재)

1. **이종 파이썬 런타임 브릿지**: Isaac Sim(py3.11 venv)과 ROS 2 Humble(시스템 py3.10)은 라이브러리를 공유할 수 없음 → ROS 토픽만을 경계로 하는 브릿지 구조 설계. Isaac Sim ROS2 Bridge 확장의 번들 rclpy 동작 원리(sys.path 주입)와 필수 환경변수 3종을 규명해 문서화.
2. **Articulation 초기화 실패 디버깅**: NVIDIA 순정 D455 애셋이 독립 강체(RigidBodyAPI)로 설계되어 그리퍼 링크 하위 조립 시 "강체 안의 강체"가 되어 PhysX가 로봇 전체 초기화를 거부 → 순정 애셋 파일을 직접 고치지 않고 assembly 레이어에서 `rigidBodyEnabled=False` 오버라이드로 해결.
3. **관절 진동·출렁임 해결**: URDF 임포터 기본 드라이브 게인이 damping≈0(감쇠 없는 스프링)임을 진단 → PD 게인(stiffness/damping)을 팔 1e5/1e4, 그리퍼 1e4/1e3으로 오버라이드 튜닝.
4. **가상 제어기의 IK 안전성 — 증상 가드에서 근본 원인까지**: MoveLine 구현에서 cuRobo IK가 elbow-flip 해(팔꿈치 반전)를 반환해 로봇이 다이브하는 문제 발견 → 1차로 현재 관절 최근접 해 선택 + 관절 이동 45° 상한 가드 추가(08-29 실전 검증, 다이브 0건). 이후 cuRobo `solve_single` 에 현재 관절을 `seed_config` 가 아닌 `retract_config` 자리로 넘기고 있었음을 발견(09-08) — 시드가 없어 매번 무작위 브랜치로 수렴하던 것. 수정 후 45mm 직선 진입 **6/6 실패 → 6/6 통과, 스텝당 관절 이동 1°**. fail-open(실패해도 성공 응답)이던 브릿지를 fail-closed로 재설계.
5. **TOOL 좌표계 지원**: 두산 API의 ref=1(툴 좌표) 상대 이동을 EE 자세 회전 변환으로 구현, FK 검증 오차 0.003mm 달성.
6. **의존성 지옥 해결**: cuRobo 0.7.8 ↔ warp-lang 1.10+ 비호환(지연 로딩 변경), colcon ↔ setuptools 80+ 비호환, rosidl의 비ASCII 경로 버그(한글 경로에서 메시지 생성 실패) 등을 각각 원인 규명 후 버전 핀·경로 이전으로 해결하고 문서화.
7. **fail-closed 안전 설계 준수**: 실기용 플래너의 6단계 시작 거부 게이트(execute_motion 옵트인, YAML 승인 플래그, live joint state, target_cell 화이트리스트 등)를 우회하지 않고 전부 정상 경로로 통과시켜 테스트.
8. **툴 길이 모델 불일치로 인한 관통 규명**: 로봇이 과실·보드를 통과하는 현상의 원인을 3개로 분해 — (a) cuRobo URDF의 `gripper_attach_joint` 오리진이 `xyz="0 0 0"`이라 플래너의 ee_link가 플랜지와 같은 자리인데 TCP를 +160mm로 가정(실제 파지부는 +233.3mm), (b) 직접 제작한 커스텀 파츠가 cuRobo·PhysX 어느 충돌 모델에도 없었음, (c) cuRobo 충돌 월드가 사실상 비어 있어 보드 관통 궤적이 정상 판정. 툴 오프셋을 ROS 파라미터로 분리(실기 기본값은 유지, 시뮬은 236mm)하고 충돌 모델·월드를 채워 해결 — 09-10 재완주에서 손끝이 타겟 윗줄기에서 멈추고 보드에 들어가지 않음을 육안 확인.
9. **시뮬 파지 판정을 실기 판정 로직과 같은 기준으로 모델링**: 가상 그리퍼가 명령값을 그대로 반향해 무엇을 잡아도 "빈손"으로 판정되던 것을, TCP와 과실의 실제 거리로 조우 개도를 리드백하도록 교체. 실기 그리퍼도 전류가 아니라 **조우 위치로 판정**하므로(전류는 로그 전용), 시뮬 경계를 position에서 끊은 것은 재현 대상과 일치하는 설계 판단이다. 그 결과 접근 정확도가 파지 판정 결과로 드러나는 검증 루프가 생겼다.
10. **MoveSpline deg/rad 혼용 회귀와 방어선**: 관절공간 보간에서 deg 와 rad 를 섞어 모든 관절에 57.3배 명령이 나가 팔이 여러 바퀴 돌며 자기 충돌. 콜백이 항상 success=True 라 어느 로그에도 흔적이 없어 발견이 늦었다 → 단위 통일 + 관절 한계 초과 명령 발행 차단(`JOINT_COMMAND_REJECTED`).
11. **씬 기하와 플래너 벽 모델의 131.7mm 불일치 규명**: 플래너 WARN 은 "FK calibration drift" 로 오진했으나 실제 원인은 씬 배치. USD 오프라인 실측으로 확정하고 오버라이드 레이어만 수정해 clamp WARN 소멸. 이후 "672mm = 실기 실측" 전제에 1차 출처가 없음을 확인하고 원본 씬 실측값(810mm)으로 복원, 6개 출처를 `check_params.py` 로 정합.
12. **시뮬이 실기보다 관대했던 곳을 스스로 찾아 원복 (09-14)**: 09-07 첫 배치 완주 직후 나머지 과실까지 시퀀스를 끝까지 보려고 실기 스윙 가드 3값과 J6 운용 한계를 넓혔다(당시 정책은 허용). 변경 로그에는 남았지만 되돌릴 임시 완화로 표시되지 않아 부채로 남았고, 이를 찾아 전부 원본으로 되돌린 뒤 재발을 막으려고 정책 §0-1 로 명문화했다. 원복 영향은 실행 전에 오프라인으로 산정했고(최근 3런 실행 계획 109개 중 거부 1, J6 통일안 A/B 로 배치 4/8), 런 12 실측이 일치했다. 두산 공식 URDF·MoveIt 설정과도 대조해 시뮬 로봇 관절 한계·속도 프로파일을 실기 값에 맞췄다 (`docs/e0509_spec_audit.md`).
13. **실기 플래너의 실패 경로를 재현하고 한계로 기록 (09-12)**: 8개 연속 런에서 이송 계획이 간헐적으로 거부됐다. 오프라인 재현으로 원인을 좁혔다 — 목표 자세의 팔꿈치 분기를 시작 자세와 무관하게 고르고 거부 뒤 재시도가 없다. 시뮬 쪽 손잡이로는 피할 수 없고 실기 티칭 격자에서도 재현됐다. 고칠 수 있는 곳이 검증 대상 코드 안이라 고치지 않고 기록했다 (`portfolio/H_scope_decisions.md` §9).
14. **배치 편차의 진짜 원인 — 가설 반증 (09-10)**: 직선 이동 드리프트 가설을 종점 오차 계측으로 반증하고, 도착 판정 허용오차 1.5°(6축 각각)가 원인임을 규명. 0.3°로 조여 팔 오차 10.0\~24.3mm → 1.9\~5.3mm.
15. **쿼드트리 순회 순서 복원·가지치기·적응 분할 (09-09\~14)**: 실기 최종본은 발표 시연용으로 sw 부터 도는 순서(`[sw, nw_flat, ne, se]`)였고 1차 스캔·가지치기가 빠져 있었다. 순회 순서를 원 팀 기록(민1 STEP 6)대로 nw 시작으로 되돌리고, 빈 분면 가지치기로 런 364.9 → 240.6초. 적응 분할은 런 10·12 에서 메커니즘이 돌았으나 시뮬에서 이득은 측정되지 않는다고 명시 (`portfolio/G_quadtree_interview.md`).
16. **실기 티칭 데이터 결함을 실기 기본값 보존으로 처리 (09-10\~11)**: 과실 6개가 슬롯 0 한 칸에 겹치는 설정, 트레이 격자가 평행사변형(84.26°)인 티칭 오차를 발견. 실기 기본값은 두고 시뮬 전용 파라미터로 분리.
17. **딸기 키네마틱 부착 (09-10)**: 물리 파지는 조우 간격과 과실 단면 때문에 구조적으로 불가 → 파지 판정 순간의 상대 변환 추종으로 설계(런타임 FixedJoint·TCP 스냅 없음). 부착 6/6.
18. **배치를 바꿔 실기 노드의 숨은 도달 한계를 찾음 (09-15)**: 과실 배치 생성·씬 재로드·노드 기동·지표 수집을 자동화해 서로 다른 배치 5개를 무인으로 돌렸다. 30 런 성공률 대신 배치별 기록을 택했다 — 비교할 실기 비율이 없고 분포는 직접 정한 것이기 때문이다. 시연 배치에 없던 높이에서 보드 가장자리 위쪽 과실 8개의 파지 후보가 전부 IK 해 없음을 찾았고, 실기의 높은 과실 처리가 로드 불가 프로파일에만 걸려 있음을 확인해 고치지 않고 기록했다 (`portfolio/H_scope_decisions.md` §10).
19. **플랜지(J6) 한 바퀴 회전 — 실기 노드 안의 설계 불일치를 고친 첫 사례 (09-16)**:
    **증상** — 런 14(09-15)와 09-16 02:36 런에서 픽 시작 때 플랜지가 360° 돌았다. 픽당 2회, 매번 같은 자리(ne 첫 픽, sw/se 픽)라 결정적이고, 브릿지 스플라인 실소요가 3.25·3.86 s(02:36 런, 런 14 는 3.25·3.88 s; 보통 0.58 s)였다.
    **원인** — 실행기(`scan_executor_node.py`)는 MoveJoint 목표의 J6 등가각을 `_JOINT_LIMITS_RAD` ±359.4° 창에서 골라 로봇을 244.4°·−311.7° 같은 자리에 세웠다. 플래너는 운용 한계 ±225°(실기 원본)로 cuRobo 궤적을 고쳐 써서, 그 자리에서 시작한 계획의 첫 점이 로봇 위치에서 정확히 360° 떨어졌다. 점프·스윙 가드는 "현재 위치 → 첫 점"을 보지 않는다. 한 노드 묶음 안의 두 부품이 같은 관절을 다르게 본 설계 불일치다.
    **처리** — `PLANNER_POLICY_v2.md` §0-1 이 허용하는 유일한 사유라 **§0-1(09-14) 이후 처음으로 설계 불일치를 사유로 실기 노드를 고쳤다**(그 전 09-07\~09-10 에는 순회 순서 등 실기 노드 수정 이력이 있다, `PLANNER_CHANGES.md`). 실행기 창 상수 하나(`_MOVE_TARGET_WRAP_WINDOW_DEG`, J6 ±225, 세부 자세 유도도 같은 창)만 바꿨고, 스윙 가드·운용 한계는 원본 그대로다.
    **확인** — 런 로그의 MoveJoint 순서를 두 창으로 오프라인 재생해 종전 창은 로그와 값까지 일치(회전 2), 새 창은 창 밖 정차 0·회전 0(`log/m3/offline_checks/j6_wrap_window_replay_20260916.txt`). 09-16 03:46 확인 런(`log/m3/20260916T034608-d6e265e9`)에서 실행기 J6 재작성 0건, 스플라인 실소요 최대 1.46 s, 결과 8 파지 / 배치 거부 4 — 원복 뒤 예상과 같다.
    **09-17 정정** — 커밋 34d190d 메시지의 "실기 원본 순회(NW 누락 3분면)"는 틀렸다. 원 팀 기록은 4셀 순회를 실기 검증했고(민1·민2), 최종 시연은 sw 시작 순서에 수확은 sw 한 곳이었다. 실기 로봇에서 같은 회전이 나는지는 확인하지 않았다 (`portfolio/H_scope_decisions.md` §11). 면접 핵심 답변:

    > "원 팀 코드는 시연용으로 sw 부터 돌았고, 그 순서에선 손목이 창 안에 머물고 한 분면만 수확했습니다. 시뮬에서 순서를 원 팀 기록대로 nw 부터 되돌리고 분할을 더하자, 실행기와 플래너가 J6 한계를 다르게 보는 모순이 동작으로 나왔습니다."

## 5. 진행 상황 (Phase별)

| Phase | 내용 | 상태 |
|---|---|---|
| 0 | USD 씬·로봇 애셋 구축 (URDF 빌드, 레이어 분리) | ✅ 완료 |
| 1 | 시뮬 브릿지 계층 이식 (fake_vision, sim_executor_bridge, Script Editor 브릿지) | ✅ 완료 |
| 2 | cuRobo 설정 이식 + 하드코딩 경로 정리 (18개 파일) | ✅ 완료 |
| 3 | 소프트웨어 스택 구축·검증 (ROS 2 Humble, colcon 5패키지 빌드, PyTorch+CUDA+cuRobo, IK 실검증) | ✅ 완료 |
| 4 | **전체 파이프라인 통합 테스트** — scan/start 트리거로 스캔→탐지→계획→접근→파지→복귀 END-TO-END | ✅ **4차까지 완주** (2026-07-10, 07-16, 08-29 ×2) |
| 5 | 접근 오차 해소 + 파지 판정 + **배치(place) 완주** | ✅ 2026-09-07 — 익은 딸기 6개 시도 → **4개 파지·이송·release 완주** (`log/m2_5/`). 단 툴 길이 수정 전이라 화면상 관통 잔존 |
| 5.5 | **노드 시퀀스 ↔ 시뮬 정합** (그리퍼 실구동, open-stem descent, elbow-flip 근본 수정, 순회 순서 nw 시작 복원, 보드 810 복원, 실행 속도 계열) | ✅ 09-10 재완주 (`log/m3/` 런 1~3) |
| 5.7 | **T2~T4b** — 딸기 키네마틱 부착, 정적 덩굴, 배치 슬롯 진행·계란판 메쉬, 익은 8개, 쿼드트리 가지치기·적응 분할, HUD 쿼드트리 패널 | ✅ 09-10\~12 (런 4\~11) |
| 5.8 | **실기 원본 값 복원 + 공식 제원 정합** (스윙 가드·J6 운용 한계 원복, 시뮬 로봇 관절 한계·속도 프로파일) | ✅ 09-14 코드, 09-15 런 12 확인 — 배치 4/8 |
| 5.9 | **T4c 실패를 정직하게 보여주기** (트레이 밖 릴리스 낙하, HUD 낙하 카운트) | ✅ 09-15 (런 13 결함 수정 뒤 런 14 로 완료 기준 충족) |
| 5.10 | **T4d 배치 변경 런** — 서로 다른 배치 5개 완주 + 배치별 기록(비율·신뢰구간 안 함) | ✅ 09-15 — 새 한계: 보드 가장자리 위쪽 과실 도달 포락선(H §10) |
| 6 | 포트폴리오 산출물 | 🔶 B·C·D·E 완료 (`portfolio/`), **A(시연 영상) 남음** — 명세는 `SUBMISSION_PLAN.md` §3 (실물 대조 클립쌍 3종 A-1~A-3 + 전체 수확 A-4 + 배치 변경 런 A-5) |

> **2026-09-06 정책 전환**: 초기에는 "실기 플래너를 수정하지 않는다"는 원칙으로 작업했으나,
> 제출 일정을 우선해 [`PLANNER_POLICY_v2.md`](PLANNER_POLICY_v2.md)로 **플래너 수정을 허용**하도록 바꿨다.
> 대신 모든 수정을 `PLANNER_CHANGES.md`에 한 줄씩 남기고, 원본 스냅샷 대비 diff로 상시 재생성한다.
>
> **2026-09-14 최우선 원칙 추가** (`PLANNER_POLICY_v2.md` §0-1): 실기 노드를 고치는 이유는 "핵심 시퀀스 설계와 맞지 않아서"뿐이다.
> 가드·임계값을 완화해 시뮬을 통과시키지 않고, 시뮬은 어디서도 원본보다 관대하면 안 된다. 실기 노드의 실패는 재현·원인과 함께 한계로 기록한다.

**통합 테스트로 발견·수정한 결함은 09-07 집계 17건**(가상 제어기 6 · USD 오버라이드 5 · 플래너 2 · 설정 4)에
09-08~15 추가 규명·수정(elbow-flip 근본 원인, MoveSpline 단위 회귀, 그리퍼 미구동, 도착 허용오차, 관대한 가드 원복 등)이 더해졌다.
최종 집계는 T6 에서 한다. 미해결은 전부 회피책과 함께 문서화했다. 항목별 목록은
[`portfolio/E_metrics.md`](portfolio/E_metrics.md) §5.

## 6. 남은 작업

> 범위·순서는 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) §4-0 이 결정한다. **산출물 A(시연 영상)까지 하고 제출, 그 이후는 이 리포에서 하지 않는다.**

| 순서 | 작업 | 상태 |
|---|---|---|
| T1 | 09-09 수정본 END-TO-END 재완주 | ✅ 09-10 |
| T2 | 딸기 키네마틱 부착 | ✅ 09-10 |
| T3 | 정적 덩굴 메쉬 12개 | ✅ 09-10 |
| T4 | 배치 슬롯 진행 + 계란판 메쉬 + 익은 8/4 | ✅ 09-11 |
| T4b | 적응 분할 + HUD 쿼드트리 패널 | ✅ 09-12 (화면 육안 일부 남음) |
| **T4c** | 실기 원본 값 복원 뒤 실패를 정직하게 보여주기 — 낙하 물리, HUD `배치 n · 낙하 m`, 런 12·13 | ✅ 09-15 (런 14, 화면 육안은 사용자 확인) |
| **T4d** | 서로 다른 과실 배치 3~5개 완주 + 배치별 기록표 (09-15 개정: 30 런 비율은 버림) | ✅ 09-15 (배치 5개) |
| T5 | 녹화 — 시연 배치 롱샷 + 클로즈업 2회, 배치 변경 런 롱샷 1회(A-5). 실패 장면도 그대로 | ☐ |
| T6 | 수치 갱신·결함 최종 집계·원격 푸시 | ☐ |

**착수 금지 (재론하지 않음, 근거 `portfolio/H_scope_decisions.md` §2)**: 병든 딸기 에셋·솎아내기 분기, 대량 배치 배속 영상, 물리 덩굴·자석 이탈력,
비타겟 과육 blocking 콜라이더, 런타임 FixedJoint, 파지 접촉 물리, M4 전체(J1/J2 swing·검증 리포트 자동화·README 대개편), 시뮬 전용 좌표 신규 하드코딩.

## 7. 정량 요약 (이력서용 숫자)

> 지표마다 출처와 재생성 명령을 붙인 전체 표는 [`portfolio/E_metrics.md`](portfolio/E_metrics.md).

- ROS 2 패키지 **5개** 빌드 — 이식 플래너 1 · 자체 가상 제어기 1 · 데이터 전용 1(신설) · 인터페이스 2
- 실기 플래너 **파이썬 64파일 / 15,975줄**을 Isaac Sim 환경으로 이식 (09-09 재계측)
  - 이식 변경은 원본 스냅샷(`_baseline/`) 대비 diff 로 상시 재생성 가능
    (`scripts/check_planner.sh` → `evidence/planner_diff.txt`), 변경 로그 **145항목**(09-15, `PLANNER_CHANGES.md`)
- 직접 구현한 가상 제어기 계층 — ROS 측 **10파일 / 1,532줄** + Isaac Sim 스크립트 **8파일 / 1,736줄**(HUD 포함)
- 통합 테스트로 결함 **17건(09-07 집계) + 09-08\~09 추가 규명·수정** 발견·수정, 미해결은 회피책과 함께 문서화
- MoveLine IK 구현 FK 검증 오차 **0.003mm**, FK→IK 왕복 오차 **\~1µm**; elbow-flip 근본 수정 후 45mm 직선 진입 **6/6 실패 → 6/6 통과**
- 씬 기하와 플래너 벽 모델의 **131.7mm 불일치 규명·정합** (clamp WARN 소멸, 6개 출처 기계 검사) — "→ 0.0mm" 는 쓰지 않는다
- END-TO-END 시퀀스 완주: 09-07 까지 5회 + **09-10\~15 `log/m3/` 런 14회**(전부 끝까지 진행, 런마다 판정표)
- 최신 성적(09-15 런 14, 실기 원본 값 복원 뒤, 익은 딸기 8개): 파지·분리 8, **트레이 배치 4**, 낙하 4. 가드를 넓혀 두었던 런 10 은 배치 7 — 넓힌 값을 스스로 되돌린 결과를 그대로 공개
- 도착 허용오차 조임으로 팔 오차 **10.0\~24.3mm → 1.9\~5.3mm**, 쿼드트리 가지치기로 런 **364.9 → 240.6초**
- 서로 다른 과실 배치 5개 무인 완주(09-15): 합계 파지 판정 32 / 배치 19 / 낙하 13, 방어선 발동 0 — 비율로는 쓰지 않는다
- USD 애셋·씬 파일 **20개**(09-15), 씬의 딸기 prim **12개**(익은 8 / 안 익은 4 — 09-11 전환, 런 1\~9 는 6/6)
  - ⚠️ 로그의 `pick candidate 24건 / 72건`은 **스캔 프레임별 누적 검출 건수**이지 딸기 개수가 아니다.
    "딸기 24개"로 쓰면 씬을 보여주는 순간 무너진다
- 프로젝트 문서 **16종** (`docs/` 8 + 루트 8)

## 8. 어필 포인트 정리 (직무별)

- **시뮬레이션/Digital Twin 엔지니어**: USD 레이어 컴포지션 실무 적용(순정 애셋을 고치지 않고 오버라이드 레이어로만 수정), PhysX articulation 디버깅, 드라이브 게인 튜닝
- **로보틱스 SW 엔지니어**: ROS 2 노드·인터페이스 설계, 실기-시뮬 추상화 경계 설계(HIL 유사 구조), fail-closed 안전 설계 이해, 두산 로봇 API
- **모션 플래닝**: cuRobo(GPU 가속 플래닝) 소스 빌드·운영, IK 해 선택 전략(최근접 해 + 점프 가드), FK/IK 검증 방법론
- **공통**: 타인이 작성한 대규모 코드(플래너 64파일·15,975줄)를 읽어 인터페이스 계약을 파악하고 다른 실행 환경으로 이식하는 리버스 엔지니어링 역량, 재현 가능한 절차 문서화 습관
- **검증 태도**: 검증 대상 코드를 넓혀 통과시킨 자기 이력을 찾아 되돌리고, 떨어진 수치(배치 8개 중 4~6개)를 그대로 공개. 실기 코드의 실패는 오프라인 재현·원인 위치와 함께 한계로 기록 — "고치면 된다"는 주장은 하지 않음. §0-1 이후 설계 불일치로 실기 노드를 고친 한 번(09-16 J6 등가각 창, 상수 하나)은 두 부품의 모순이라는 근거에 오프라인 재생·확인 런을 붙였고, 원 팀 시연 순서를 결함으로 돌리지 않음
- **변경 관리**: 이식 과정의 모든 변경을 원본 스냅샷 대비 diff로 상시 재생성하고 로그로 남기는 체계 운영 (`PLANNER_POLICY_v2.md` / `PLANNER_CHANGES.md`) — 무엇을 왜 바꿨는지 언제든 설명 가능
