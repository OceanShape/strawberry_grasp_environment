# 프로젝트 목표 및 작업 지침 (AI 에이전트용 브리핑)

> 이 문서는 USD 환경 구성(`strawberry_harvest/`)까지 완료된 현 시점에서, 이후 개발의 최종 목표와
> 리포지토리 내 작업 방식을 정의한다. 코드 작업 전 반드시 이 문서 전체를 읽을 것.

> **⚠️ 제출 범위 (2026-09-10)** — 이 문서는 프로젝트의 **원안·장기 목표**다.
> 이번 포트폴리오 제출에서 **무엇을 하고 무엇을 하지 않는지**는 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md)가 결정하며,
> 충돌하면 그쪽이 우선한다. 제출본이 주장하는 것은 하나다:
> *"실기 제어 스택을 하드웨어 없이 구동하고, 실기와 시뮬의 불일치를 찾아 정합했다."*
> 이 문장을 강화하지 않는 작업(§1 의 검증 파이프라인 자동화, §1-2 의 병든 딸기 분기, §4 6단계 등)은 **제출 후**로 미룬다.

---

## 1. 최종 목표 (한 문장)

**팀에서 작성한 두산 로봇 모션 플래너 노드를 Isaac Sim 환경으로 이식해 구동하고,
그 모션을 자동으로 검증하는 파이프라인을 구축한다.**

이 프로젝트의 검증 대상은 **플래너의 모션·통신·시퀀스**다.
파지(grasp) 물리의 사실성은 검증 대상이 **아니다**.

### 배경

- 실제 시스템: 비전 노드(YOLO) → 모션 플래너 노드 → 두산 제어기(dsr_msgs2 서비스) → 실물 로봇 e0509
- 시뮬레이션에서는 양 끝단을 대체한다:
  - **입력단**: 비전 연산을 생략하고, Isaac Sim 내 딸기 좌표를 추출해 실제 비전 노드와 **완벽히 동일한
    형태의 토픽**을 발행 (비전 모킹)
  - **출력단**: 플래너가 호출하는 두산 제어기 서비스를 수신하는 **가상 제어기 서버**를 만들어
    시뮬레이션 로봇팔을 구동
- 가운데의 모션 플래너 노드는 절대 수정하지 않는다.
  → **2026-09-06 [`PLANNER_POLICY_v2.md`](PLANNER_POLICY_v2.md)로 폐기.** 플래너 수정은 허용하되 `PLANNER_CHANGES.md`에
  한 줄씩 남기고, **실기 기본 동작은 파라미터 기본값으로 보존**한다(시뮬 전용 값은 `ee_to_tcp_offset_m`처럼 파라미터로 분리).

### 데이터 흐름

```
[Isaac Sim: 딸기 prim 좌표] ──→ [비전 모킹 노드] ──(비전과 동일한 토픽)──→ [모션 플래너 (실기 코드 이식 구간)]
                                                                                    │
                                                                    (dsr_msgs2 서비스 호출)
                                                                              ↓
[Isaac Sim: 로봇 articulation] ←──(조인트 타겟)──────────[가상 두산 제어기 서버]
        │                                                                   │
        └──(실행 joint states)──→ [검증 파이프라인] ←──(명령 궤적)──┘
                                                │
                                 궤적 비교 · 리밋 · 충돌 리포트
```

---

## 1-2. 원래 설계했던 수확 시퀀스 (원본 요구사항, 2026-09-09 기록)

> 사용자 구술로 확보한 **프로젝트 원안**. 지금까지 어느 문서에도 없었다.
> 실기 노드가 KPI 발표용으로 축소되면서 이 틀에서 벗어났다는 것이 사용자 판단이며,
> 시뮬과 실기 노드 모두 **이 시퀀스로 되돌리는 것이 장기 목표**다.
>
> **각주 (2026-09-10, `SUBMISSION_PLAN.md` §5)** — 아래 6단계 중 **4·5단계의 병든 딸기 분기는 이번 제출 범위 밖**이다.
> 제출본이 완주를 주장하는 것은 익은 딸기 대상 **수확 핵심 경로**(1·2·3·6)뿐이다. 1·2·6(분면 순회·분면별 스캔 자세)은
> 2026-09-09 에 코드로 복원됐고 실행 검증 대기 중이다. 포트폴리오에는 "병든 과실 폐기 분기는 검증 범위에서 제외 —
> 수확 핵심 경로 아님" 한 줄로만 적고, **"전체 시퀀스 재현"이라고 쓰지 않는다**.

1. home scan position 에서 **1차 스캔**
2. 수확할 딸기가 있어 보이는 쿼드트리 세부 영역을 **2차 스캔** (이때 **세부영역별 scan position 으로 이동**)
3. 세부 영역에서 확인된 **익은 딸기를 모두** 파지/배치
4. **병든 딸기(흰색)** 는 파지만 하고 그리퍼를 열어 떨어뜨림 (제거) — *제출 범위 밖*
5. 다음 영역 2차 스캔 → 파지/배치 + 병든 딸기 제거 **반복** — *병든 딸기 제거 부분은 제출 범위 밖*
6. **4개 영역 전부** 끝나면 종료

### 프로젝트 마지막 단계 영상에서 관찰된 실제 동작 (원안과의 차이)

1. home scan position → **sw 세부영역** scan position 으로 이동
2. 익은 딸기 수확 (파지/배치)
3. 같은 영역의 병든 딸기를 파지한 채 떨어뜨려 제거
4. **나머지 세 영역은 scan position 으로 이동만** 하고 수확하지 않음
5. home scan position 복귀 후 종료

즉 **한 영역만 수확**하고 나머지는 순회만 했다. 원안의 3·5·6 이 무력화된 상태다.

## 2. 현재 상태

- `strawberry_harvest/`의 USD 환경 구성은 완료 (씬/애셋/물리 레이어 분리, 로봇 임포트 완료).
  상세는 `usd_structure.md`, `urdf_setup.md` 참조.
- 지금부터 만드는 것은 전부 **이 USD 세계를 구동하는 Python / ROS 2 코드 레이어**다.

---

## 3. 코드 배치 규칙

1. **새 코드는 `strawberry_harvest/`(USD 저장소) 바깥에, 별도 ROS 2 패키지로 만든다.**
   패키지명 제안: `sim_bridge`. 구성:
   ```
   sim_bridge/
   ├── sim_launcher.py        # SimulationApp 생성, main_scene.usd 로드, 물리 스텝 루프 (메인 스레드)
   ├── vision_mock.py         # 딸기 포즈 추출 → 비전 토픽 발행
   ├── virtual_controller.py  # dsr_msgs2 서비스 서버 (가상 두산 제어기)
   ├── validation/            # 궤적 기록, 오차 플롯, 리밋/충돌 체크, 리포트 생성
   └── config/                # 토픽명, 프레임명, QoS, 게인 등 설정
   ```
2. **USD 파일 수정 최소화 원칙은 코드에도 적용된다.**
   - `assets/robot/doosan_e0509_rh_p12_rn/` (URDF 임포트 원본): 수정 절대 금지
   - 런타임에 씬에 추가하는 것(디버그 마커 등)은 USD 파일에 굽지 말고 세션 레이어 또는 코드 스폰으로 처리
   - 물리 파라미터 튜닝이 필요하면 기존 규칙대로 해당 sublayer에서만 수정
3. 실행 방식은 **Isaac Sim 스탠드얼론 Python** (`python.sh sim_launcher.py`)을 사용한다.
   GUI OmniGraph 기반 워크플로우는 사용하지 않는다.

---

## 4. 작업 단계 (반드시 이 순서, 각 단계 완료 기준 통과 후 다음 진행)

### 0단계 — 씬 스모크 테스트
- `main_scene.usd`를 Play 했을 때: 로봇이 중력에 처지지 않고 초기 포즈 유지, 딸기가 줄기 joint에
  매달려 유지, 팔·테이블·소품 간 관통 없음.
- 딸기 줄기 FixedJoint는 **2026-09-07 `breakForce=20 / breakTorque=5`로 상향**됐다
  (초기 2/1은 과실 자중의 10.2배뿐이라 로봇이 스치기만 해도 끊겨 날아갔다).
  더 올리거나 break를 비활성화하려면 `scenes/layers/physics_layer.usd`에서 조정한다.
- ✅ 완료 기준: Play 상태에서 씬이 수 분간 안정적으로 유지.

### 1단계 — 스탠드얼론 Python으로 로봇 구동
- `sim_launcher.py`: SimulationApp 생성 → `main_scene.usd` 로드 → World reset → 로봇 Articulation
  핸들 획득 → 하드코딩한 조인트 목표로 팔 이동.
- 로봇의 전체 DOF 이름 목록을 출력해 기록할 것 (팔 6축 + 그리퍼).
- ⚠️ RH-P12-RN 그리퍼의 URDF `<mimic>` 조인트는 Isaac Sim 임포터가 제대로 처리하지 못했을 수 있다.
  그리퍼 DOF가 여러 개로 분리되어 있으면 코드에서 동기 제어할 것.
- payload(`lab_environment.usd`)가 headless 실행에서도 로드되는지 확인.
- ✅ 완료 기준: 코드에서 지정한 조인트 각도로 팔이 부드럽게 이동.

### 2단계 — rclpy 연결
- `sim_launcher.py`와 같은 프로세스에서 rclpy 초기화.
- `/joint_states`와 `/clock` 발행. 플래너 측 `use_sim_time` 정합 확인.
- ✅ 완료 기준: 별도 터미널에서 `ros2 topic echo /joint_states`로 시뮬레이션 조인트 값 확인.

### 3단계 — 비전 모킹 노드
- 딸기 prim의 월드 포즈를 읽어, 실제 비전 노드와 **동일한 토픽명 / 메시지 타입 / 좌표 프레임 / QoS**로
  발행한다. 실제 비전 노드의 발행 코드를 먼저 분석해 스펙을 확정할 것.
- 프레임 변환에는 `configs/camera_calibration_to_isaac.py`의 규약(OpenCV ↔ Isaac 좌표계)을 재활용.
  카메라 optical frame 규약(REP-103, z-forward) 주의.
- ✅ 완료 기준: 플래너 노드가 실기 비전과 같은 인터페이스 계약으로 타겟 딸기를 정상 인식.

### 4단계 — 가상 두산 제어기 서버 (최대 난관)
- 플래너 코드를 grep 해 **실제로 호출하는 dsr_msgs2 서비스만** 목록화하고, 그것만 구현한다.
  (예상: MoveJoint, MoveLine, 그리퍼 관련 일부. 전체 API 구현 금지.)
- `move_joint`부터 구현. 궤적은 현재 조인트 상태 → 목표까지 사다리꼴 속도 프로파일(또는 선형 보간).
- **스레딩 패턴 (핵심)**:
  - 메인 스레드: 물리 스텝 루프(`world.step()`)를 절대 블로킹하지 않는다.
  - ROS executor(MultiThreadedExecutor)는 별도 스레드에서 spin.
  - 서비스 콜백은 목표를 큐에 넣기만 하고, 메인 루프가 매 스텝 궤적을 진행시키며,
    모션 완료 시 이벤트로 콜백을 깨워 응답을 반환한다 (두산 서비스의 블로킹 의미론 재현).
- **단위 변환 필수**: 두산 API는 deg / mm, ROS·Isaac Sim은 rad / m. 변환 유틸을 한 곳에 두고 재사용.
- `move_line`은 IK가 필요하다. 직접 구현하지 말고 Isaac Sim 내장 모션 제너레이션(Lula IK)을 사용.
- ✅ 완료 기준: 플래너의 전체 pick 시퀀스가 시뮬레이션에서 끝까지 실행.

### 5단계 — Kinematic attach 파지
- 접촉 물리로 실제 파지하지 않는다. 그리퍼가 닫히는 시점에 딸기를 그리퍼에 붙이고, 놓는 시점에 해제한다.
- **구현 방식은 2026-09-10 에 확정** ([`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) T2): 런타임 **FixedJoint 생성은 하지 않는다**
  (조우가 콜라이더와 겹친 순간에 조인트를 만들면 떨림·튐이 생긴다). 대신 CONTACT 시점의 `T_tcp→berry` 상대 트랜스폼을
  캡처해 매 프레임 `world = T_tcp_world · T_rel` 로 **kinematic 추종**한다. TCP 중심으로 스냅하지 않는다 —
  접근이 5mm 빗나갔으면 딸기도 5mm 빗나간 채 매달려야 실기와 같다.
- 부착 중에는 해당 딸기 콜라이더를 비활성화하고, release 시 그 자리에 정지시킨다.
- 이는 의도된 설계 단순화다 (검증 대상은 모션이지 파지 물리가 아님). 문서/리포트에 명시할 것.
- ✅ 완료 기준: 파지 후 과실이 그리퍼를 따라 이동·배치되는 화면 확보. **1일 타임박스**, 초과 시 HUD 판정 표시로 대체.

### 6단계 — 검증 파이프라인 (제출 후 — `SUBMISSION_PLAN.md` §5 M4 항목)
- `validation/`에 구현:
  - (a) 제어기 서버가 받은 **명령 궤적**과 시뮬레이션의 **실행 joint states**를 동기 기록
  - (b) 궤적 추종 오차 플롯 자동 생성 (조인트별 시계열)
  - (c) 조인트 리밋·속도 리밋 위반 체크
  - (d) PhysX contact report 기반 의도치 않은 충돌 감지 (그리퍼-딸기 접촉은 화이트리스트)
  - (e) 실행 1회당 "통과/실패 + 사유" 리포트 자동 생성 (md 또는 html)
- ✅ 완료 기준: 플래너 실행 한 번에 리포트 1부가 자동 생성.

---

## 5. 공통 주의사항

- **QoS**: 모킹 토픽의 reliability/durability가 실제 노드와 다르면 구독이 조용히 실패한다.
  실제 노드의 QoS 설정을 그대로 복사할 것.
- **sim time**: `/clock` 발행 + 모든 노드 `use_sim_time=true` 정합. 안 맞으면 플래너의
  타임아웃/대기 로직이 오작동한다.
- **단위**: 전 프로젝트 meters, Z-up (USD 체크리스트와 동일). 두산 API 경계에서만 deg/mm 변환.
- **드라이브 튜닝**: 팔이 떨리거나 오버슈트하면 articulation drive의 stiffness/damping(= PD 게인)을
  조정한다. 원본 애셋이 아니라 오버라이드 레이어에서 수정.
- 접촉 기반 파지 물리, RL, 렌더링 품질 개선, USD 구조 리팩토링은 **전부 범위 외**. 제안하지 말 것.

---

## 6. 완성의 정의 (Definition of Done)

1. 팀의 모션 플래너 노드가 시뮬레이션에서 pick-and-place 전체 시퀀스를 수행한다.
   이식 과정에서 가한 변경은 [`PLANNER_CHANGES.md`](PLANNER_CHANGES.md)에 기록하며,
   작업 원칙은 [`PLANNER_POLICY_v2.md`](PLANNER_POLICY_v2.md)를 따른다.
2. 실행마다 궤적 비교 플롯과 통과/실패 리포트가 자동 생성된다. — *제출 후(M4). 이번 제출 범위 밖*
3. 전체 실행이 명령어 한두 줄로 재현된다 (launch 파일 또는 셸 스크립트).
4. README에 아키텍처 다이어그램, 실행 방법, 설계 결정(kinematic attach 단순화 포함)이 문서화된다.
