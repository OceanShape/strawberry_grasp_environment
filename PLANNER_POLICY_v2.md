# 플래너 작업 원칙 v2

**제정일:** 2026-09-06 · **표현 가이드 델타 반영:** 2026-09-10 (§5.4)
**상태:** `PLANNER_CHANGE_POLICY.md`(v1, 2026-09-05)를 **전면 대체**함
**적용 대상:** 이 저장소에서 작업하는 모든 에이전트 및 작업자

> **범위·작업 순서는 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md)(2026-09-10)가 결정한다.**
> 이 문서는 플래너 수정 원칙과 **표현 규칙**만 담당한다. 충돌하면 SUBMISSION_PLAN 의 범위 결정이 우선한다.

---

## 0. 배경 — 왜 원칙이 바뀌었나

v1은 "실기 플래너를 시뮬레이션 전용 코드 추가 없이 구동한다"는 **주장을 보호**하기 위한 규칙 체계였다.
그 주장을 지키려면 원본 스냅샷 관리, 수정 대장 작성, 모든 변경의 FIX/PARAM/SIM 분류가 필요했고,
이는 상당한 관리 비용을 발생시킨다.

포트폴리오 제출 일정이 임박한 현재, **우선순위가 바뀌었다.**
이제 필요한 것은 "무수정"이라는 주장이 아니라, **정상적으로 동작하는 딸기 수확 시퀀스 영상**이다.

주장을 내려놓으면 관리 비용도 함께 사라진다. v2는 그 교환을 명시적으로 받아들인다.

---

## 1. 이번 스프린트의 단일 목표

> **딸기를 탐지하고, 접근하고, 파지하고, 지정 위치에 배치하는 수확 핵심 경로가
> 끊김·오류 없이 완주하는 Isaac Sim 영상 1편을 확보한다.**

이 목표에 기여하지 않는 모든 작업은 **이번 스프린트 범위 밖**이다.
판단이 애매할 때는 "이 작업이 영상 완성을 앞당기는가?"만 묻는다. 아니면 하지 않는다.

> **2026-09-10 보강** — 영상의 명세(클립 4종)와 종료 조건은 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) §0·§3 이 확정했다.
> 새 작업을 넣을지는 SUBMISSION_PLAN §2 의 **sim2real 게이트**("제어 스택의 입력 또는 출력이 달라지는가")로 판정한다.
> 밖으로 나가는 문서에서는 "전체 시퀀스"가 아니라 **"수확 핵심 경로"**라고 쓴다 (§5.4).

---

## 2. 폐기하는 규칙 (v1에서 삭제)

다음은 **더 이상 적용하지 않는다.**

- ❌ FIX / PARAM / SIM 변경 분류 의무
- ❌ "SIM 성격의 변경은 플래너에서 금지, 가상 제어기 쪽에서 해결" 제약
- ❌ `PLANNER_CHANGES.md` 수정 대장의 상세 기입 의무
- ❌ 원본 동작 보존 의무

`_baseline/` 스냅샷 디렉터리는 **삭제하지 말고 그대로 둔다.** 유지 비용이 0이고,
나중에 "무엇을 어떻게 바꿨는지" 설명할 때 근거가 된다. 다만 동기화 의무는 없다.

---

## 3. 새 규칙

### 3.1 플래너 노드 수정 — 전면 허용

실기 제어 노드 코드는 **목표 달성에 필요한 만큼 자유롭게 수정한다.** 사전 승인 불필요.

허용되는 것 (전부):

- 시연용으로 고정되어 있던 시퀀스를 실제 인지 결과 소비 경로로 교체
- 웨이포인트·오프셋·접근 자세·타이밍 하드코딩
- 씬 좌표에 맞춘 상수 조정
- 안전 검사 임계값 완화 (시뮬 환경에 맞게)
- 실패하는 분기 우회 또는 제거
- 노드 분할·병합, 파일 구조 변경

### 3.2 우선순위

충돌 시 위쪽이 이긴다.

1. **동작 정합성** — 시퀀스가 오류 없이 끝까지 간다
2. **속도** — 가장 빨리 되는 방법을 택한다
3. **구조적 우아함** — 마지막. 지금은 신경 쓰지 않는다

하드코딩은 이번 스프린트에서 **기술 부채가 아니라 정당한 도구**다.
"나중에 일반화하기 좋게" 같은 이유로 시간을 쓰지 않는다.

### 3.3 잔여 이슈 처리 방침

기존에 알려진 문제들(목표 앞 132mm 정지, 파지 판정 빈손, 둘째 타겟 궤적 안전검사 탈락)은
**근본 원인 규명 없이 결과만 맞춰도 된다.**
오프셋을 상수로 보정하든, 안전검사를 우회하든, 파지 판정을 강제 성공시키든 무방하다.
단, 그렇게 처리한 항목은 §4에 한 줄 남긴다.

---

## 4. 경량 수정 로그 (의무)

`PLANNER_CHANGES.md`에 **한 줄씩만** 남긴다. 분류·근거·상세 불필요.

```
- [파일:함수] 무엇을 어떻게 바꿨는지 한 문장
```

예:
```
- [harvest_planner.cpp:approach()] 접근 정지 오프셋을 132mm 보정 상수로 하드코딩
- [grasp_node.cpp:verify()] 파지 판정 실패 분기 우회, 항상 성공 반환
```

**이건 은폐 방지용이 아니라 본인 기억용이다.**
면접에서 "이 부분은 어떻게 구현하셨어요?"를 받았을 때
자기가 뭘 했는지 정확히 말할 수 있어야 한다. 작성에 드는 시간은 건당 10초다.

---

## 5. 문서·포트폴리오 표기 규칙 ★

코드는 자유롭게 고치되, **밖으로 나가는 문서는 고친 사실과 어긋나면 안 된다.**
이력서·경력기술서·포트폴리오·면접 답변 전부 해당한다.

### 5.1 사용 금지 표현

플래너를 수정한 이상 아래는 **사실과 다르므로 쓰지 않는다.**

- ❌ "실기 플래너 코드 0줄 수정"
- ❌ "무수정 구동"
- ❌ "시뮬레이션 전용 분기 0줄"
- ❌ "원본 노드를 그대로 사용"

### 5.2 대체 표현 (사실이며, 약하지 않음)

- ✅ **"실기용 모션 플래너를 Isaac Sim 환경으로 이식"**
  → *이식(port)* 은 정확한 기술 용어다. 작업했다는 뜻을 담으면서 무수정을 주장하지 않는다.
- ✅ "가상 dsr_msgs2 서비스 서버를 구현해 실기 제어 스택을 하드웨어 없이 구동"
  → 제어기 구현은 100% 본인 작업이다. 수정 여부와 무관하게 온전한 성과다.
- ✅ "실기 제어 스택과 시뮬레이터 사이의 인터페이스 계층 설계 및 구현"
- ✅ "실기 플래너를 시뮬레이션에 올리는 과정에서 인지 결과를 소비하지 않는 고정 시퀀스 구조를 발견하고, 실제 인지 경로를 사용하도록 재작성"
  → 발견 + 해결. 시뮬레이터가 진단 도구로 기능했다는 서사가 된다.

### 5.4 2026-09-10 추가 — 범위 확정에 따른 델타 (`SUBMISSION_PLAN.md` §7)

§5.1 의 금지 목록은 그대로 두고 다음을 더한다.

| 표현 | 판정 | 대체 |
|---|---|---|
| **"관통 없이"** | 조건부 — **T1 재완주 성공 후에만** 사용 | 그 전에는 "관통 3원인 규명·수정(기하 검증 완료, 실행 검증 대기)" |
| **"병든 과실 선별 / 솎아내기"** | ❌ 사용하지 않는다 | 시뮬 범위 밖. 잔여 이슈에 **범위 결정**으로만 기재("병든 과실 폐기 분기는 검증 범위에서 제외 — 수확 핵심 경로 아님"). 팀원 관련 경위는 어떤 문서에도 쓰지 않는다 |
| **"전체 시퀀스 재현"** | ❌ 사용하지 않는다 | 원안 대비 폐기 분기가 빠졌으므로 **"수확 핵심 경로 완주"** |
| **"접근 오차 131.7 → 0.0mm"** | ❌ 사용하지 않는다 | 보드 기준값이 09-09 에 810mm 로 복원돼 0.0 의 근거가 사라졌다. **"131.7mm 불일치 규명 및 정합(clamp WARN 소멸)"** |
| 딸기 부착(T2) | 조건부 — T2 완료 후 | "파지 후 과실이 그리퍼를 따라 이동·배치되는 것까지 시뮬레이션". **"파지 성공"·"수확 성공"은 여전히 금지** |

### 5.3 면접 대비

"이식 과정에서 어떤 부분을 수정해야 했나요?" 는 **높은 확률로 들어온다.**
§4 로그를 보고 2~3개를 골라 답할 수 있게 준비해 둔다.
이 질문은 위험한 질문이 아니라 **준비하면 점수를 따는 질문**이다.

반대로 "무수정"이라고 적으면 "어떻게 무수정이 가능했죠?" 를 부르는데,
이건 준비할 수 없는 질문이다. 그래서 §5.1이 있다.

---

## 6. 완료 판정

> **2026-09-10 부터 종료 조건은 [`SUBMISSION_PLAN.md`](SUBMISSION_PLAN.md) §0 이 기준이다** —
> 09-09 수정본 END-TO-END 재완주 로그 1건 + 시뮬 영상 1편 + 실물 대조 클립쌍 3종 + 산출물 수치 갱신 + 원격 푸시.
> 아래는 v2 제정 당시의 판정으로, 이력으로 남긴다.

- [x] 스캔 → 탐지 → 계획 → 접근 → 파지 → 배치 → 복귀 전 구간 무오류 완주 — 2026-09-07 런 (`log/m2_5/`, 툴 길이 수정 전)
- [x] 최소 2개 타겟 연속 처리 — 같은 런에서 4개
- [ ] 영상 녹화본 확보 — **카메라 2대**(롱샷 / 클로즈업), 명세는 SUBMISSION_PLAN §3
- [x] `PLANNER_CHANGES.md` 한 줄 로그 최신화 — 101건 (2026-09-09)
- [x] 포트폴리오/이력서/경력기술서에서 §5.1 금지 표현 전수 검색 후 제거 — 2026-09-07, §5.4 델타는 2026-09-10 재검색

---

## 7. 이 원칙의 유효 기간

이번 포트폴리오 제출 시점까지. 이후 프로젝트를 정식으로 다듬을 때는
§3.1에서 허용한 하드코딩을 걷어내고 v1의 인터페이스 분리 원칙으로 되돌아가는 것을 권장한다.
`_baseline/`을 남겨두는 이유가 이것이다.
