# 제출 계획 — 산출물 A(시연 영상)까지로 범위 확정

> **기준일**: 2026-09-10
> **용도**: 코드 세션(CLI) 컨텍스트. 무엇을 하고 **무엇을 하지 않는지**를 고정한다.
> **관계**: 수치·근거의 1차 출처는 `PROGRESS_REPORT.md`(09-09), 표현 규칙은 `PLANNER_POLICY_v2.md`.
> 이 문서는 그 위에 **범위와 순서**만 얹는다. 충돌하면 이 문서의 범위 결정이 우선한다.
> **갱신 규칙**: 각 작업의 `[ ]`를 `[x]`로 바꾸고 결과 한 줄을 그 자리에 추가한다. 새 작업을 넣기 전에 [`portfolio/H_scope_decisions.md`](portfolio/H_scope_decisions.md) §2 표(착수 금지)를 먼저 읽는다.
> **2026-09-11 계획 변경**: 작업마다 걸어 두었던 타임박스·폐기 조건·시간 폴백을 전부 없앴다. 각 작업은 완료 기준을 채운 뒤에 넘어간다.

---

## 0. 한 장 요약

- **목표**: 산출물 A(시연 영상) 확보 → 제출. 그 이후는 이 리포에서 하지 않는다.
- **종료 조건**: 09-09 수정본으로 END-TO-END 재완주 로그 1건 + 시뮬 영상 1편 + 실물 대조 클립쌍 3종 + 산출물 수치 갱신 + 원격 푸시.
- **남은 예상**: 집중 4~6일.
- **하지 않기로 확정한 것**: 병든 딸기 솎아내기(원안 C·D), M4 전체, 파지 접촉 물리, 물리 덩굴, 대량 배치 배속 영상. 이유는 [`portfolio/H_scope_decisions.md`](portfolio/H_scope_decisions.md) §2 (구 §5, 2026-09-11 이동).
- **지금 위치 (2026-09-15)**: T1~T4b 끝. 09-14 에 실기 노드의 가드·운용 한계를 전부 원본으로 되돌리고 시뮬 브릿지·로봇 모델을 실기 값에 맞췄다 → **런 12(09-15)에서 확인: 7/8 분리, 4 배치, 3 이송 거부(J6 spline jump), 1 진입 실패** (`log/m3/README.md` 런 12). T4c ①②(낙하 물리·HUD 낙하 카운트) 09-15 구현·헤드리스 검증 — 화면 확인은 런 13. 남은 순서는 §4-0.
- **M 과 T**: `PORTFOLIO_SPRINT.md` 의 M1~M4 는 **이력(동결)**이다. M3 = 이 문서의 T1~T6, M4 = 제출 이후. 현행 작업 번호는 **T 만** 쓴다.

---

## 1. 범위 결정 (확정, 재론 대상 아님)

포트폴리오는 **영상 촬영 단계까지 하고 종료**한다. 이 프로젝트를 더 키우지 않는다.
본인 서사의 핵심은 "기능 검사·디버깅"이므로, 새 기능을 추가하는 작업은 서사에 기여하지 않는다.

제출본이 주장하는 것은 다음 하나다:

> 실기 제어 스택을 하드웨어 없이 구동하고, 실기와 시뮬의 불일치를 찾아 정합했다.

이 문장을 **강화하지 않는 작업은 전부 범위 밖**이다.

---

## 2. 판단 기준 — sim2real 게이트

새 작업을 넣을지 말지는 이 질문 하나로 판정한다.

> **그것이 바뀌면 제어 스택(플래너 2노드)의 입력 또는 출력이 달라지는가?**

- **달라진다** → 정합 대상. 실기 쪽 값을 1차 출처로 삼아 시뮬을 맞춘다. (예: 툴 길이, 보드 y, 파지 판정 기준, 분면 스캔 자세)
- **달라지지 않는다** → 시각 표현이거나 범위 밖. 물리로 만들지 말고 정적 메쉬나 로그로 처리한다. (예: 덩굴, 자석 이탈력, 계란판 형상)

**규칙 0 (2026-09-14, 최우선)**: 실기 노드를 고치는 이유는 **"핵심 시퀀스의 설계와 맞지 않아서"뿐**이다. 가드·임계값 완화로 시뮬을 통과시키지 않는다 — 시뮬은 어느 곳에서도 원본보다 관대하면 안 된다. 실기 코드의 실패는 한계로 기록한다 (`PLANNER_POLICY_v2.md` §0-1, `portfolio/H_scope_decisions.md` §9). 09-08·09-10 에 올린 스윙 가드 세 값은 09-14 원복.

부수 규칙 두 가지:

1. **실기 기본 동작은 파라미터 기본값으로 보존한다.** 시뮬 전용 값은 파라미터로 분리한다(`ee_to_tcp_offset_m`, `enable_open_stem_descent` 방식).
2. **좌표·자세는 새로 하드코딩하지 않는다.** 이미 플래너 안에 있는 값을 쓴다. 시뮬에만 있는 좌표를 만드는 순간 sim2real 주장이 깨진다.

---

## 3. 산출물 A 명세 (영상)

| # | 클립 | 실물 소스 | 시뮬 소스 | 우선순위 |
|---|---|---|---|---|
| A-1 | **딸기 분리** 쌍 (5~10초) | 부트캠프 단계별 클립(직선 접근·분리) | 클로즈업 카메라, 45mm 직선 진입 구간 | **필수** |
| A-2 | **영역 이동** 쌍 (5~10초) | 발표 영상에서 컷 | 롱샷, 분면 전환(nw→ne) | 높음 |
| A-3 | **딸기 배치** 쌍 (5~10초) | 발표 영상에서 컷 | 롱샷, release 구간 | **높음** — T2 로 배치가 보이게 됐고, T4-1(슬롯 진행) 없이는 A-4 에 6개 겹침이 찍힌다 |
| A-4 | **시뮬 전체 수확** (30~60초) | — | 롱샷 메인 + 파지 순간 클로즈업 인서트 | **필수** |

- 실물 쪽은 **발표 영상 전체를 기준 삼지 않는다.** 구간만 잘라 쓴다. 발표 영상에는 시뮬에 없는 병든 딸기 분기가 있어 전체 대조는 불리하다. 발표 영상은 부록에 링크만.
- 촬영은 **뷰포트 카메라 2대**(롱샷 고정 / 클로즈업)로 하고, 카메라별로 런을 한 번씩 돌린다. 같은 사건을 두 시점으로 찍어 편집에서 합친다.
- 영상 파일은 리포에 커밋하지 않는다(용량). 링크로 참조하고 로그만 `log/m3/`에 보존.

---

## 4. 작업 순서

### 4-0. 현재 위치와 남은 순서 (2026-09-14 갱신 — 이 표만 보면 된다)

| 순서 | 작업 | 상태 | 한 줄 |
|---|---|---|---|
| T1 | 09-09 수정본 END-TO-END 재완주 | ✅ 09-10 | 런 1~7 |
| T2 | 딸기 키네마틱 부착 | ✅ 09-10 | |
| T3 | 정적 덩굴 메쉬 | ✅ 09-10 | |
| T4 | 배치 슬롯 진행 + 계란판 메쉬 (4차) + 익은 8/4 | ✅ 09-11 | 런 9 검증 |
| T4b | 적응 분할 + HUD 쿼드트리·세부 칸 하이라이트 | ✅ 09-12 구현·런 10 실행 / 🔶 화면 확인은 다음 런(씬 재로드) | 런 10 판정표 칸은 미기입 |
| **T4c** | **실기 원본 값 복원 뒤 실패를 정직하게 보여주기** — ① 트레이 밖 릴리스는 공중 동결 대신 **떨어지게**(브릿지; 보드 앞 릴리스 지점은 테이블 밖이라 **바닥 콜라이더 신설**) ② HUD 완료 줄에 **배치 n · 낙하 m**(프로브가 실행기의 release-and-continue 경계에서 셈) ③ 런 12: 09-14 변경 전부 실행 확인 + 씬 재로드 ④ 런 13: ①②·바닥 화면 확인 | 🔶 ①②③ 09-15 완료(①은 헤드리스 검증), ④ 런 13 대기 | 시뮬 코드만. 실행기·플래너 무수정 |
| **T4d** | **무작위 배치 N 회 → 성공률·이송 거부율** — 생성기(`docs/random_layout_audit.md` A1~A7 정리), 씬 재로드 자동화, 런당 지표 기록, N 은 아래 | ☐ | 종전 T7(레이아웃 1회 변경)을 대체 |
| T5 | 녹화 (롱샷 + 클로즈업, 런 2회) | ☐ | T4c 뒤. 실패 장면은 자막 없이 그대로 |
| T6 | 수치 갱신 · 문서 정합 · 원격 푸시 | ☐ | |
| ~~T7~~ | 레이아웃 변경 1회 완주 | ✖ T4d 로 흡수 | |

**T4d 의 N — 어느 정도면 자료가 되나.** 단위는 **런(배치 1개)**이다. 한 런의 과실 8개는 수확 순서·칸 배정으로 서로 묶여 있어 독립 표본이 아니다.
지표는 세 가지를 따로 센다: 파지 성공률(파지 판정 통과 / 익은 과실), **이송 거부율**(이송 계획 거부 / 분리 성공 — 원인 하나), **배치 성공률**(컵에 놓임 / 익은 과실 — 최종). 적응 분할은 별도로 **분할 발생 수(분면 단위)·세부 자세 수락/거부 수·부모 퇴화 수**를 기록한다 — G §6-3 4번의 "임계 3은 배치 맞춤"을 이 비율로 대체한다.
과실 단위로 보면 95% 신뢰구간 폭이 ±10% 되려면 약 100개(런 12회), ±6% 는 240개(30회), ±5% 는 384개(48회)다. 런 단위 상관을 감안해 **최소 30 런**을 자료 기준으로 잡고, 먼저 **5 런 파일럿**으로 생성기·재로드 자동화·기록이 도는지 본다.
런당 약 6~7분(09-14 속도 정합 뒤)이라 30 런은 3.5 시간이다. 오프라인 도달성 도구는 후퇴 자세가 있어야 해서 런을 대신하지 못한다.


### [x] T1 — 09-09 수정본 END-TO-END 재완주  🔒 블로커 — **2026-09-10 종료**

영상에 찍힐 대상이자 "관통 없이"의 근거. 이게 없으면 나머지가 전부 무의미하다.

**Step 0 — 로그부터 읽는다 (이분 탐색보다 먼저).** 09-09 17:23 런에서 확인할 세 가지:
1. `JOINT_COMMAND_REJECTED` 가 찍혔는가 → 관절 한계 차단 방어선이 정상 명령을 막고 있는 경우.
2. 스플라인 실행시간이 얼마로 계산됐는가 → 관절속도 기반 하한이 90초를 넘겼다면 타임아웃은 증상일 뿐이다.
3. 명령값과 측정값이 각각 무엇이었는가 → 팔이 아예 안 움직였는지, 엉뚱한 곳으로 갔는지 구분.

**첫 의심 가설**: 관절속도 기반 스플라인 하한 계산에 deg/rad가 다시 섞였을 가능성. 09-09에 MoveSpline 57.3배 회귀를 잡았는데, 하한 계산은 그와 별개 경로다.

**Step 0 결과 (09-10 확인) — 원인은 플래너가 아니라 Isaac 브릿지 스크립트다.**

- 1) `JOINT_COMMAND_REJECTED` **없음.** 관절 한계 방어선은 이 실패와 무관하다.
- 2) **스플라인 경로가 아니다.** 이 런의 유일한 팔 명령은 YAML 스캔 자세로 가는 **직행 MoveJoint** 2건뿐이다(`~/.ros/log/python3_58401_*.log`). MoveSpline 은 한 번도 호출되지 않았으므로 "관절속도 기반 하한" 첫 의심 가설은 이 실패에 적용되지 않는다.
- 3) **팔이 아예 안 움직였다.** 명령 `[144.09 22.9 -1.0 121.48 -75.31 108.68]`, 측정은 오버뷰 자세 `[88.0 -94.9 129.9 175.9 -31.3 93.4]` 에 고정. 잔차 130.9deg 는 J3 의 전체 이동량(129.89 -> -1.0)과 정확히 같다. 중단 후 오버뷰 복귀 MoveJoint 에는 타임아웃이 없다 — 이미 그 자리에 있었기 때문이다.

**근인**: Isaac 물리 스텝 콜백이 첫 관절 명령에서 예외로 죽었다.
`kit_20260909_171505.log` 에 `robot.apply_action(...)` -> `articulation_controller.apply_action` -> `self._articulation_view.get_applied_actions()` 에서
`AttributeError: 'NoneType' object has no attribute 'get_applied_actions'` 가 **17:23:30 부터 17:23:51 까지 2,486회** 반복된다.
17:23:30 은 `START_ACCEPTED`(30.360) / `MoveJoint`(30.364) 와 같은 초다.

세 가지가 이것 하나로 설명된다:
- 예외가 콜백을 중단시켜 그 뒤의 `/dsr01/joint_states` 발행과 1Hz 딸기 좌표 발행이 같이 멈춘다 → 플래너 JSONL 의 `scene_positions_received` 가 17:23:29.5 를 끝으로 98초간 끊긴다.
- `node.target_action = None` 이 `apply_action` **뒤**에 있어 예외가 나면 초기화되지 않는다 → 매 스텝 재시도(2,486회).
- 관절 상태가 얼어붙으니 브릿지의 8초 도착 대기와 scan_executor 의 90초 도착 확인이 둘 다 실패한다. 90초 타임아웃은 증상이다.

**왜 09-09 가드를 통과했나 (유력 설명, 로그로 직접 확인되진 않음)**: `on_physics_step` 의 가드는 `robot.handles_initialized` 만 본다.
`Executing file:` 기록상 브릿지 스크립트는 17:23:22 에 **Play 중인 상태에서** 재실행됐다(17:23:17 onStop -> 재생 -> 17:23:22 재실행, 직후 좌표 발행 재개).
이때 Isaac 이 prim view 를 자동 초기화해 `handles_initialized` 는 True 가 되지만, `ArticulationController._articulation_view` 는 `robot.initialize()` 안에서만 연결된다.
가드가 통과하면서 `initialize()` 를 건너뛰어 컨트롤러 쪽 view 만 None 으로 남았다. `[bridge] articulation (re)initialized` 도 `[bridge] DOF map:` 도 재실행 이후 한 줄도 찍히지 않은 것과 맞는다(노드는 `builtins.my_ros_node` 로 재사용되어 DOF 캐시가 살아 있었다).

**조치 (09-10 적용 완료)**: Step 1 이분 탐색(A/B/C)은 **보류**한다. 플래너 회귀가 아니므로 플래너는 한 줄도 건드리지 않았다.
`strawberry_harvest/scripts/isaac_sim_script_editor_bridge.py` 에만 세 가지를 넣었다:
- (a) `apply_action` 을 try/except 로 감싸고, 실패 시 `robot.initialize()` 재시도 + `target_action` 을 **유지**해 다음 물리 스텝에 재시도.
- (b) 준비 검사 `_apply_ready()` 가 `handles_initialized` 에 더해 `ArticulationController._articulation_view` 까지 본다. 속성 이름이 없는 버전이면 True 를 돌려 (a) 에 맡긴다 — 명령이 영영 안 나가는 쪽이 더 나쁘다.
- (c) 상태 발행을 `_publish_state()` 로 분리해, 명령 적용이 실패해도 `/dsr01/joint_states` 와 딸기 좌표 발행은 계속 돈다. 재발 시 로그에서 "팔이 안 움직였다" 와 "브릿지가 죽었다" 가 구분된다.
- 덤: 같은 예외 로그를 2초에 한 번으로 제한(종전 21초에 2,486줄), 복구 시 `apply_action 복구됨` 한 줄.

**남은 것**: 재완주 1회. 실행은 **터미널 3개** — ① Isaac Sim ② `bash scripts/run_nodes.sh` ③ 트리거 (2026-09-10 변경, 종전 T1~T5 터미널 번호는 폐기. 여기 T1~T7 은 아래 작업 번호이지 터미널이 아니다). 남은 노드 점검·파라미터·기동 로그 대조는 스크립트가 한다 — planner `-p ee_to_tcp_offset_m:=0.236 -p enable_open_stem_descent:=true`, 브릿지엔 `tool_tcp_offset_m` 지정 금지가 이미 반영돼 있다. 절차는 [`docs/run_guide.md`](docs/run_guide.md). `log/m3/` 는 09-10에 만들어 뒀다 (`README.md` 에 실행 전 점검·보존 대상·판정표). 런이 끝나면 `~/.ros/log/` 원본과 JSONL, **Isaac Kit 로그**를 그리로 복사한다 — 이번 원인은 Kit 로그에만 있었다.

**부수 발견 (수치 인용 금지)**: `ARM_ARRIVAL_TIMEOUT` 이 함께 찍는 손끝오차 값은 신뢰할 수 없다.
`_wait_for_arm_arrival` 은 `ik_solver.kinematics.get_state()` 를 두 번 부르고 `.clone()` 없이 두 결과를 뺀다.
같은 파일 `move_line_cb` 는 같은 호출에 `.clone()` / `.copy()` 를 붙인다 — 버퍼가 재사용된다는 뜻이다. 이 값은 대외 산출물에 쓰지 않는다.

**Step 1 — 가설이 안 맞으면 이분 탐색.** 줄 수가 아니라 **기능 묶음** 단위로 되돌린다.

| 묶음 | 내용 | 첫 스캔 이동 관련성 |
|---|---|---|
| **A** | 원안 수확 시퀀스 복원 (분면 순회, 실기 v12 티칭 스캔 자세, 분면 이동 계획 경유) | 높음 — 목표 자세 자체가 바뀜 |
| **B** | 실행 속도 계열 (`sim_speed_scale`, 스플라인 하한, 도착 대기, 게인 10배) | 높음 — 도착 판정 자체가 바뀜 |
| **C** | MoveSpline deg/rad 수정 + 관절 한계 초과 차단 | 중간 |
| — | HUD, 파지 판정 조우 물림, 보드 810 정합, 자기-장애물 버그 | 무관 — 되돌리지 말 것 |

A·B·C 세 묶음이면 2~3런 안에 잡힌다. `_baseline/` 스냅샷과 `PLANNER_CHANGES.md` 항목별 로그로 중간 상태를 만든다.

**완료 기준**: 익은 딸기 대상 스캔→탐지→계획→접근→파지→이송·배치→복귀 완주 1회, `log/m3/`에 보존, `clamped` WARN 없음, 화면상 관통 없음.

**09-10 02:02 런 결과 — 로그 기준 4/4 충족, 육안 항목 1개 남음.** (`log/m3/README.md` 에 상세)
run_id `20260910T020201-fc808f75`. `TRAVERSAL_SCAN_STARTED (4/4 quadrants)` → `SCAN_COMPLETE`,
`PICK COMPLETE` 6건(전부 `DETACH_SUCCESS_UNVERIFIED`), `GRASP_JUDGE` 6건 전부 `CONTACT`,
`clamped` 0건, `EXEC_TIMEOUT`·`ARM_ARRIVAL_TIMEOUT`·`JOINT_COMMAND_REJECTED` 0건,
Kit 로그 `get_applied_actions` 예외 0건, `scene_positions_received` 413건/413.9초 최대간격 1.0초.
→ 09-09 17:23 의 실패 모드(브릿지 콜백 사망)는 재현되지 않았다.
**남은 것**: 화면상 관통 여부는 로그로 판정할 수 없다. 뷰포트/영상 육안 확인 후 체크박스를 닫는다.

**09-10 범위 변경 (사용자 결정) — 쿼드트리 가지치기 복원, T1 재완주 1회 추가.** 실기 최종본의 4분면 전수 순회는 발표용 축소본이라 원안 1·2단계(overview 1차 스캔 → 익은 과실 있는 분면만 순회)를 `overview_prescan` 파라미터(기본 false, 시뮬 true)로 복원했다. 함께 넣은 것: 비인접 분면 MoveJoint 폴백의 overview 경유, 서브셀 2차 분할 중심선 교정, `scan_dwell_sec:=3.0`. 항목별 근거는 `PLANNER_CHANGES.md` 09-10, 면접 설명은 `portfolio/G_quadtree_interview.md`.
**완료 기준 추가**: 로그에 `OVERVIEW_SCAN` · `TRAVERSAL_PRUNED skip=['root/se']` · 3분면만 `AT_SCAN_POSE` · `PICK COMPLETE` 6건 · `clamped` 0건. 적응 분할(G §6-2)은 T2 뒤 별도 → **09-11 T4b 로 편성**(T 번호가 없어 T2~T4 를 지나며 빠져 있었다).

**09-10 10:13 런 결과 — 9/9 충족, 육안 항목 1개 남음.** (`log/m3/README.md` §런 2 에 상세)
run_id `20260910T101326-43213d77`. `OVERVIEW_SCAN nw:2 ne:1 se:0 sw:3` → `TRAVERSAL_PRUNED skip=['root/se']`,
**se 미방문**(`AT_SCAN_POSE` 가 nw·ne·sw 만), `TRANSIT_VIA_OVERVIEW root/ne -> root/sw` 1건,
2차 분할이 부모 분면 중심선으로 실제 분산(`root/sw/se:1 nw:1 ne:1`),
`PICK COMPLETE` 6건(익은 딸기 6개 전부, 전부 `DETACH_SUCCESS_UNVERIFIED`), `GRASP_JUDGE` 6/6 `CONTACT`,
`clamped`·`ARM_ARRIVAL_TIMEOUT`·`JOINT_COMMAND_REJECTED`·`EXEC_TIMEOUT` 0건.
**전체 364.9 → 240.6초** (dwell 84 → 18초, se 분면 1회분 13초 절감).
Kit 로그에 09-09 와 같은 부류의 브릿지 초기화 실패가 났으나 **09-10 수정이 30회 재시도 후 자동 복구**했고
복구는 트리거보다 14초 앞이다 — 수정의 실증 사례.
**남은 것**: 화면상 관통·HUD 표시 육안 확인 (T5 녹화 때 같이 닫는다).

**[x] 09-10 T1 종료 — 육안 항목 닫힘.** 사용자 확인(런 1~3 뷰포트): 파지 순간 손끝이 타겟 딸기의 윗줄기에서
정지하고 보드로 들어가지 않았으며, 분면 이동 중 보드를 스친 적이 없다. 이로써 T1 완료 기준 전부 충족 —
**"관통 없이" 표현 사용 가능** (§7). 09-10 추가분(가지치기·스텝 상한·`plan_scan_transit=false`)까지 포함한
런 3(`log/m3/20260910T110524-e89f4f91/`)이 최종 상태다. 총 240.6 → 208.5초.

---

### [x] T2 — M2.7 Stage3: 딸기 키네마틱 부착 — **2026-09-10 완료 (반나절)**

**설계**:
- 대상은 딸기 **루트 prim**(줄기+과육 한 덩어리). 줄기만 따로 다루지 않는다.
- CONTACT 판정 시점의 `T_tcp→berry` **상대 트랜스폼을 캡처해 유지**한다. 매 프레임 `world = T_tcp_world · T_rel`.
- **TCP 중심으로 스냅하지 않는다.** 접근이 5mm 빗나갔으면 딸기도 5mm 빗나간 채 매달려야 실기와 같다. 이 오차가 화면에 보이는 것이 검증 루프의 증거다.
- release 시 해제하고 그 자리에 정지. 부착 중에는 해당 딸기 콜라이더 비활성.
- **런타임 FixedJoint 생성 금지** — 근거 `portfolio/H_scope_decisions.md` §2.

**구현 위치**: 부착·해제는 Isaac 스크립트 쪽(`strawberry_harvest/scripts/`, 이미 딸기 좌표를 발행하는 브릿지 존재). CONTACT/release 상태는 `sim_executor_bridge` 가 판정하므로 ROS 토픽으로 내보내 Isaac 스크립트가 구독한다. 새 판정 로직을 Isaac 쪽에 다시 만들지 않는다.

**09-10 구현 완료, 실행 검증 대기.** 브릿지 `/sim/grasp_event`(ATTACH 좌표 / RELEASE) + Isaac 스크립트 부착·추종·해제
(`PLANNER_CHANGES.md` 09-10 항목 2건). 설계대로 그리퍼 밑동 기준 상대 트랜스폼 캡처, TCP 스냅 없음, 세션 레이어만 수정,
런타임 FixedJoint 없음. **추가한 필수 조건**: 부착·해제된 딸기를 `/isaac_sim/strawberries` 발행에서 제외 — 계란판 위치가
분면 격자상 se 에 들어가 배치한 딸기가 새 타겟으로 재탐지되면 가지치기가 깨진다. USD 수학(상대 트랜스폼 왕복, 세션 레이어
물리 속성)은 pxr 오프라인 시험 통과. **12:02 런 결과 — 부착 6/6, 완주, 판정표 9/9** (`log/m3/README.md` §런 4). `[bridge] ATTACH/RELEASE` 6쌍, 대상 일치 0.0mm,
상대 오프셋이 6개 서로 다른 값으로 보존(TCP 스냅 없음 확인), 전부 계란판에 정지, 계란판 좌표 재발행 0건, 그리퍼 자세는
Isaac 5.1 `SingleRigidPrim` 물리 뷰. 부수 결함 1건(익은 과실이 시야에 없으면 빈 배열을 안 보내 좌표 흐름 정지) 발견·수정.
**화면 육안 확인은 사용자 몫** — T5 녹화에서 자연히 닫힌다. 표현 규칙: "파지 후 과실이 그리퍼를 따라 이동·배치되는 것까지 시뮬레이션" 사용 가능, "파지 성공"은 여전히 금지.

---

### [x] T3 — 정적 덩굴 메쉬 — **2026-09-10 완료 (반나절 미만)**

보드에서 각 딸기 줄기 끝까지 얇은 곡선 12개. **물리·콜라이더 없음**.
딸기가 공중에 떠 있는 화면을 고치는 시각 작업이다. T2가 되면 딸기가 덩굴 끝에서 떨어져 나가 "분리"로 읽힌다.
물리 덩굴·자석 이탈력은 하지 않는다(`portfolio/H_scope_decisions.md` §2).

**09-10 구현 완료, 화면 육안 확인 대기.** 항목별 근거는 `PLANNER_CHANGES.md` 09-10 2건.

- **파일 배치는 원안(“레이아웃 레이어에만 추가”)과 다르다.** 메쉬 데이터를 손으로 관리하는
  `layout_layer.usd` 에 넣으면 그 파일이 망가진다. 리포 자체 규약(`main_scene.usd` = 조립,
  `layout_layer.usd` = 배치, 지오메트리는 애셋)을 따라 셋으로 나눴다:
  프로토타입 `assets/props/vine.usd`(200점/191면) → `main_scene.usd` 의 `Scope "vines"` 에서 12번 참조
  → `layout_layer.usd` 에 translate 12건. 딸기 12개가 y·orient·scale 이 모두 같아 **형상이 전부 동일**하므로
  프로토타입 1개면 된다. 덩굴 translate 는 같은 딸기의 translate 와 **같은 값**이다(원점 = 과실 중심).
- **§2 게이트 통과 — 플래너 입출력 불변**: 물리 스키마 0건, 조인트 0건, 프림 이름에 `strawberry` 가
  없어 브릿지의 좌표 발행 필터에 안 걸린다(highlight 오버레이와 같은 규칙). 새 좌표 하드코딩도 없다 —
  형상은 기존 상수(`pick_target_z_bias_m` 35mm, `CRANE_Z_OFFSET_M` 30mm, 보드면 810mm, 딸기 y 782.8mm)에서 유도했다.
- **★ 수직 구간을 과실중심 +25~+65mm 로 잡아 플래너가 겨냥하는 두 점을 덩굴 중심선 위에 올렸다** —
  파지 목표 +35mm 와 open-stem descent 시작 +65mm, 12개 전부 이탈 **0.000mm**.
  열린 조우가 덩굴을 따라 30mm 내려와 덩굴을 물고, 그 뒤 T2 부착으로 과실이 딸려 나간다. 이것이 A-1 클로즈업이다.
  조우 닫힘 간격 0.7mm 라 덩굴(파지점 지름 3.2mm)과 면당 1.3mm 겹치지만 콜라이더가 없어 물리 영향은 없다.
- **검증**: `python3 strawberry_harvest/scripts/scene_tools/verify_vines.py` — Isaac 없이 usd-core 로 씬을 조립해
  5항목(발행 필터 / 물리 0건 / 보드·줄기 접점 / 플래너 두 점 / 이웃 과실 간격) 전부 통과.
  **딸기 배치를 바꾸면 다시 돌린다** — 최소 여유는 `vine_ripe_06` vs `strawberry_ripe_04` 의 **+1.3mm** 로 얇다.
- **남은 것**: 화면 육안 확인. **씬 재로드 필요**(T5 의 09-10 시각 정비와 같이 닫힌다).
  표현 규칙 변화 없음 — "파지 성공"·"수확 성공"은 여전히 금지(§7).

---

### [x] T4 — 배치 슬롯 진행 + 계란판 메쉬 — **2026-09-11 완료 (런 9 검증, 익은 8/4 전환은 런 10)**

**왜 올라갔나 (09-10)**: T2 로 딸기가 실제로 이송되자 **6개가 전부 slot 0 한 자리에 겹쳐 놓이는 것이 화면에 드러났다**
(런 4 해제 위치 (772~777, 87~105, 36~47)mm, 서로 수 mm 차이 = 접근 오차만큼). T2 전에는 딸기가 안 움직여 보이지 않던
문제다. A-4 필수 클립에 그대로 찍히므로 **T5 녹화 전에 1번은 반드시** 한다. 이것은 시뮬 결함이 아니라 **실기 노드 설정의
결함**이다 — `taught_slot_sequence=[0,0,0,0,0,0]` 이면 실기도 여섯 개를 한 칸에 떨어뜨린다. 부트캠프 최종은 과실 1~2개만
배치해 드러나지 않았다. sim2real 불일치 발견 사례로 그대로 쓴다.

**09-10 사실 정정 (종전 문안의 근거가 틀렸다)**:
- 종전 2번에 적은 "09-07 로그 175~181행의 J6 spline jump 358.9~359.5° 거부 = slot≥1 배치 계획 거부" 는 **오독**이다. 그 세 줄은
  딸기 (350,645,800) 을 향한 **파지 접근 계획**의 거부다 (직전 줄 `PICK 딸기 raw=(350,645,800)`, 직후 `ABORT: grasp 전체 실패`).
- 종전 3번 "J6 한계 ±360 vs ±225 대조" 는 **09-08 에 이미 처리됐다** — `harvest_motion_params.OPERATIONAL_JOINT_LIMITS_DEG[5]`
  ±225 → ±360 (URDF 와 동일). 그 뒤로 slot≥1 을 다시 시도한 런은 03:11 런 하나뿐이다. **→ 2026-09-14 C4 로 ±225 원복** (시뮬이 실기보다 관대하면 안 된다. 결과: 런 10 기준 배치 4/8, `docs/e0509_spec_audit.md` §7).
- **실제 slot≥1 근거는 09-10 03:11 런** (`run_logs/20260910_031140/planner.log:270~353`):
  slot 1 (goal 460.2, 55.8, 186.5) **Plan OK**, slot 2 (goal 400.5, 59.2, 187.4) **IK_FAIL ×2**.
  slot 2 는 `is_row2 = slot_index % 3 == 2` — 격자에서 로봇 베이스에 가장 가까운 열(x≈400)이고, 실기 팀도 이 열에
  `row2_place_pitch_tilt_deg`·`row2_release_correction_mm`·`skip_row2_place_slots` 를 따로 둔 것으로 보아 실기에서도 어려웠던 열이다.

**격자 (플래너 상수, 새 좌표 없음)**: `TAUGHT_SLOT{0,1,3}_PLACE_REFERENCE_POSX_MM_DEG` — slot0 (519.95, 52.39), slot1 (460.24, 55.83),
slot3 (511.91, 1.83). 열 피치 ≈ **59.7mm(-x)**, 행 피치 ≈ **50.6mm(-y)**, `TAUGHT_TRAY_SLOT_COUNT=15` = 5행 × 3열, 열 = slot%3, 행 = slot//3.
런 4 의 과실 정지 높이 z 36~47mm.

1. **슬롯 진행** (필수, ~30분): `run_nodes.sh` 의 `taught_slot_sequence:=0,0,0,0,0,0` → **`0,1,3,4,6,7`** (x≈400 열인 2·5·8 제외).
   완료 기준: planner 로그 `TAUGHT_TRAY_SLOT_SEQUENCE: slot 0 -> 1 -> 3 -> 4 -> 6 -> 7`, `Plan OK` 6/6, Isaac `[bridge] RELEASE … frozen at`
   6개가 x 로 ~60mm / y 로 ~50mm 씩 떨어진 서로 다른 자리. `SLOT_SEQUENCE_DONE` 뒤 자동 진행(`next auto slot`) 은 쓰지 않는다 — 열 2 로 간다.
   - [x] **09-10 준비 완료 — 런 대기.** `run_nodes.sh` 를 `0,1,3,4,6,7` 로 바꾸고 기동 로그 대조에 `slot_sequence=[0, 1, 3, 4, 6, 7]` 확인을 넣었다.
     **런 전에 오프라인으로 도달성을 확인했다** (`src/strawberry_motion/scripts/check_tray_slot_reachability.py` 신설 — 플래너와 같은
     MotionGen·충돌월드·plan config 로, 12:02 런의 실제 retreat 종료 관절 6개 + overview 를 시작 자세로):
     **slot 0·1·3·4·6·7 전부 7/7 Plan OK + release IK OK.** 9·10·12·13 도 된다(13 은 5/7). **x≈400 열(2·5·8·11·14)은 7/7 전부 `IK_FAIL`
     — 15° 피치 틸트를 빼도 똑같다.** 즉 `is_row2` 열은 틸트 문제가 아니라 위치 자체가 이 로봇·이 트레이 위치에서 안 닿는 열이고, 실기 팀의
     `row2_*` 보정 파라미터는 원인을 비껴간 처방이었다. 판정표는 `log/m3/README.md` §런 5.
   - [x] **09-10 13:12 런 — 슬롯 진행은 통과, 화면에서 결함 2건.** `SLOT_SEQUENCE 0->1->3->4->6->7`, `_PLACE_BLOCKED` 0건,
     해제 6곳으로 분산(런 4 의 "한 칸에 6개" 해소). 사용자 관찰: **여전히 살짝 닿고, 배치가 살짝 평행사변형.**
     원인 분해는 `log/m3/README.md` §런 5 (A)(B)(C):
     - **(A) 닿음 = 티칭 격자 자체.** 과실은 파지 자세에서 **수직축 기준 82° 돌아 x·y 전폭이 바뀌고**(장축은 수직 유지; 전폭 x 44.5 / y 53.8 / z 62.6mm),
       티칭 행 피치는 51.2mm 다. **산포 0 을 가정해도 행 이웃 간격 +0.3mm** — 완벽히 실행해도 닿는다.
       → **조치: `taught_slot_sequence` `0,1,3,4,6,7` → `0,1,6,7,12,13`** (행 건너뛰기). 최소 간격 +0.3 → **+15.3mm**.
       5행×열2개에서 행 이웃 없이 6개를 놓는 조합은 (0,2,4행)×(0,1열) 뿐이라 slot 13(5/7)은 뺄 수 없고 **맨 뒤**에 뒀다.
     - **(B) 평행사변형 = 실기 티칭 오차.** 격자 두 축 사이각 **84.26°**(직교에서 5.74°). 강체 계란판이 스스로 비직교일 수
       없으므로 slot 0·1·3 수동 티칭 오차이고, 15칸을 세 점으로 만들기에 행마다 누적된다. sim2real 발견 사례로 서술한다.
       **→ 09-11 사용자 결정으로 시뮬에서는 직교화한다** — 단 실기 상수를 고치지 않고 `orthogonalize_taught_grid` 파라미터
       (기본 false) 로 분리해 실기 기본 동작은 보존(§2 부수규칙 1). 서술: "티칭 격자 왜곡을 발견 → 파라미터로 격리 → 시뮬은 보정 격자".
     - **(C) 들쭉날쭉 = 배치 하강의 측방 드리프트.** 계획 above 자세는 정확한데(xy 0.1~2.5mm, 자세 0.05~0.16°),
       과실 정지 위치에서 역산한 그리퍼는 계획에서 **4.3~20.9mm** 어긋났다. 그 구간은 브릿지 MoveLine 이고 검사가
       **스칼라 거리 하나**뿐이라 측방 이탈이 안 보인다. 직교 편차 검사는 `is_row2` 분기에만 있어 **한 번도 실행된 적이 없다**.
       → **조치: `MOVELINE_END_ERR`(along/lateral) 계측 추가. 동작은 불변** — 런 6 에서 실측치를 보고 대응을 정한다.
   - [x] **09-10 13:42 런 6 — 판정표 6/6, (A) 해소.** `SLOT_SEQUENCE 0->1->6->7->12->13`, `_PLACE_BLOCKED` 0건
     (사전 검증 5/7 이던 slot 13 도 성공 — 맨 뒤 배치가 유효), **과실 간 최소 간격 +0.1 → +12.7mm**,
     `GRASP_JUDGE` 6/6 CONTACT, `clamped`·`ARM_ARRIVAL_TIMEOUT`·`JOINT_COMMAND_REJECTED`·`EXEC_*` 0건, 전체 213.8초.
     - **(C) 가설은 반증됐다.** `MOVELINE_END_ERR` 실측 — 배치 하강 6건의 lateral 은 전부 **0.1mm**. 직선은 정확했다.
       팔이 계획 release 자세에서 **10.0~24.3mm** 어긋나는 것은 맞지만, 원인은 MoveLine 이 아니라
       **도착 판정 허용오차 `ARM_ARRIVAL_TOL_DEG = 1.5°`** 다 — 배치 자세 FK 환산으로 J1 13.7 / J2 14.2 / J3 11.4mm,
       **RMS 23.6mm**. 드라이브 정상상태 오차는 0.02° 수준이라, 1.5° 는 **아직 움직이는 팔을 도착으로 인정**하는 값이다.
       실기 Doosan 서비스는 모션이 끝나야 응답하므로 조이는 쪽이 실기에 더 가깝다. **아직 안 고쳤다** — 타이밍이
       시퀀스 전체에 걸려 별도 런이 필요하고, 지금 배치는 이미 안 닿는다. 상세는 `log/m3/README.md` §런 6 (C정정).
   - [x] **09-10 14:06 런 7 — 도착 허용오차 `ARM_ARRIVAL_TOL_DEG` 1.5 → 0.3deg (커밋 `fix(bridge)`), 판정표 8/8.**
     **팔 오차 10.0~24.3mm → 1.9~5.3mm** (계통 11.0→2.5, 산포 11.0→2.1mm), 배치 하강 along −3.6~−4.3 → −1.0~−1.5mm,
     `MOVELINE_SHORT` 6→0건, `ARM_ARRIVAL_STALLED`·`TIMEOUT` 0건, 전체 213.8 → 222.5초(+8.7초).
     파지 lateral 은 개선 없음(2.7~5.3mm) — 그 값은 도착 잔차가 아니라 cuRobo 접근 자세가 지배한다.
   - [x] **T4-1 종료.** 남은 배치 편차 두 가지는 **둘 다 실기 동작이라 유지**한다:
     ① **(B) 티칭 격자 84.26°** (slot 0·1·3 수동 티칭 오차) ② **분면별 접근 기울기** — sw 는 10.1°, nw/ne 는 0.1~0.3°
     로 접근해 과실이 매달리는 깊이가 8.7mm 다르고, 트레이에서 그 차이가 world x 로 나타나 분면 경계를 걸치는
     열 쌍(slot 6↔7)만 간격 +6.0mm 다(나머지 +16~17mm). 수확 순서가 얕은 3 + 깊은 3 이라 한 쌍은 구조적으로
     걸친다. 상세는 `log/m3/README.md` §런 7.
2. ~~슬롯 3·4·6·7 중 `IK_FAIL` 이 나는 것이 있으면 → 되는 슬롯만 남긴 시퀀스로 (예: `0,1,3,4,0,1`).
   전부 실패하면 그때 `row2_*` 보정 파라미터를 본다.~~
   → **[x] 해당 없음 (미발동, 09-10).** 발동 조건인 배치 경로 `IK_FAIL` 이 런 5·6·7 **전부 0건**이고
   `_PLACE_BLOCKED` 도 0건이다. 09-10 오프라인 검사(`check_tray_slot_reachability.py`)가 예측한 대로였다 —
   실패하는 열 2·5·8·11·14 는 애초에 시퀀스에 없다. `row2_*` 보정 파라미터도 볼 일이 없었다.
   ⚠️ 시퀀스를 `0,1,3,4,6,7` → `0,1,6,7,12,13` 으로 바꾼 것은 이 항목이 아니라 **(A) 과실 접촉** 때문이다
   (§런 5 (A)). 트리거가 달랐으므로 이 항목은 미발동으로 닫는다.
3. [x] **계란판 시각 메쉬** — **2026-09-11 완료 (2차).** 물리·콜라이더 없음(§2 게이트 — 플래너 입출력 불변).
   - **1차는 같은 날 폐기.** 컵 격자를 티칭 격자 그대로(사이각 84.26°, z 기울기) 만들고 점유 컵을 실측 과실에 맞췄더니
     기울고·평행사변형이고·컵이 제각각인 판이 됐다. 사용자 지적: 계란판을 쓰는 이유가 "가로세로 균일한 컨테이너" 인데 그러면
     이유가 사라진다. **맞는 지적** — 격자 왜곡은 로봇 티칭 오차이지 컨테이너의 성질이 아니고, 컨테이너에 구우면 오차가 숨는다.
   - **2차 = 강체 실물답게: 수평·직사각·컵 15개 동일.** 피치만 플래너 상수의 **크기**(59.8 × 51.2mm), 각도는 쓰지 않는다.
     컵 = 타원 그릇 4링(림 rx26/ry23 @ z16 → 바닥 rx13/ry12 @ z1, 깊이 15mm), 스커트 상판 3mm 아래. 판 윗면 z16 은 가장 낮게
     놓이는 과실(중심 z≈24)이 림에 걸리지 않는 상한이다.
   - **배치 = 티칭 ee 격자 15점 + 평균 ee→과실 변위(런 7)에 강체 최소자승(Kabsch)**: translate (0.7676, 0.0879, 0), **yaw −7.24°**.
     왜곡을 양쪽 축이 나눠 가져 컵 중심 잔차 최대 5.3mm. 대안은 `egg_carton_geom.YAW_OVERRIDE_DEG` 한 줄 —
     0°(베이스 축 정렬) 이면 잔차 16.7mm(행 0·4 과실이 x 로 ±16mm, 능선 위), −3.30°(티칭 열 축) 이면 10.3mm.
   - **과실 위치는 건드리지 않았다.** 런 7 착지: 컵 중심 치우침 ≤ 5.8mm, 6개 전부 자기 셀 안, 림/벽 겹침 최대 7.6mm(slot 12 —
     행 4 는 중심 z 24 라 판이 넓은 부분을 지난다). 이 겹침이 **티칭 격자 왜곡·분면별 매달린 깊이의 눈에 보이는 크기**다.
   - 파일: `assets/props/egg_carton.usd`(2,188점/984면) ← `scene_tools/gen_egg_carton_asset.py` + `egg_carton_geom.py`(단일 출처) /
     `main_scene.usd` 참조 / `layout_layer.usd` translate+orient. 검증 `verify_egg_carton.py`: 물리 0건·발행 필터·배치=fit·
     수평·직교·컵 동일·과실 6개 자기 셀 안.
   - **2026-09-11 사용자 결정 (씬 재로드 후): 계란판 yaw 0° + 배치 좌표도 거기에 맞춘다.** 배치 좌표는 플래너 출력이므로
     실기 티칭 상수는 덮어쓰지 않고 §2 부수규칙 1 대로 **파라미터로 분리**했다 — `orthogonalize_taught_grid`(기본 **false** = 실기
     그대로; `run_nodes.sh` 가 true). true 면 `tray_place_policy.taught_grid_slot_offset_m` 이 같은 세 점의 **피치 크기만** 쓰고
     축을 −x/−y, z 를 수평으로 둔다(slot 0 은 그대로). 결과 격자: 59.8 × 51.2mm, 사이각 90.00°, z 편차 0.
     계란판은 그 격자와 합동이라 translate = 직교화 격자의 slot 0 ee + 평균 ee→과실 변위 = (0.7711, 0.0915, 0), yaw 0.
     판 윗면은 z 22 로 올렸다(격자가 수평이라 과실 중심 z≈36.5 → 컵 깊이 21mm). 예상 착지(런 7 매달림 변위 적용): 컵 중심
     치우침 ≤5.9mm, 6개 전부 자기 셀, 림/벽 겹침 ≤0.5mm — 남는 치우침은 분면별 매달린 깊이 차(±4mm)뿐이다.
     **씬 재로드 + 노드 재기동 필요.** 실제 착지는 런 8 에서 확인한다 (`log/m3/README.md` §런 8).
   - **2026-09-11 3차 (사용자 제공 실기 영상 정보 반영).** 실기 계란판은 15구(3×5)였고, 실기 런은 과실 3개를 **인접 칸**
     (slot 0·1·3 — 티칭 기준점 세 칸)에 넣었는데 서로 닿지 않았다. 즉 컨테이너의 **깊이와 구멍 지름**이 이웃 과실을 갈라 놓는다.
     - **컵 깊이 21 → 56mm (림 z 22 → 57), 구멍 rx27/ry25(피치가 허용하는 최대, 능선 y 1.2mm).** 시뮬 과실은 정지 자세에서
       y 전폭 53.8mm 로 행 피치 51.2 보다 넓어, 이웃 행 과실의 **적도 대역(중심 −1~+13mm)이 겹치는 것 자체는 어떤 형상으로도
       막을 수 없다**(물리 없음, 과실은 놓인 자리에 고정). 림을 그 대역보다 위(과실 중심 +20.5~23mm)에 두면 겹침은 컵 벽 안에
       숨고(림 아래 15mm 이상) 밖에서는 어깨와 줄기 6.5~9mm 만 보인다 — 실기 영상과 같은 화면. 과실은 키네마틱이라 바닥에
       기대지 않으므로 깊이는 자유 — 바닥은 과실 밑끝 바로 아래(z 1) 그대로, 림만 올렸다. 예상 착지 6개: 림 여유 ≥ +3.3mm(밖에서
       보이는 유일한 접촉 기준), 전부 자기 셀.
     - **수평 중점을 테이블 중심축(y=0)에 정렬** (2차는 y −10.8mm 옆). 계란판은 배치 격자에 합동으로 따라가므로 격자를 옮겨야
       하고, §2 부수규칙 1 대로 플래너 파라미터 **`taught_grid_shift_y_m`**(기본 **0.0** = 실기 그대로; `run_nodes.sh` 가 +0.0108)
       로 분리했다 — 격자 전체(slot 0 포함)를 world y 로 평행이동. 값은 `egg_carton_geom.GRID_SHIFT_Y_M`(= 2·행피치 − slot 0 ee y
       − 평균 매달림 변위 y) 이 단일 출처고 생성기가 출력, 검증기가 run_nodes.sh 인자·기동 로그 대조·현재 실측 기준 ideal(1mm 안)을
       대조한다. 결과 translate (0.7711, **0.1024**, 0), yaw 0, 컵 격자 중점 world (711.3, −0.04) mm. 배치 release 좌표는 y 로 +10.8mm
       움직인다 — 오프라인 도달성 재검사는 `check_tray_slot_reachability.py --orthogonalize --shift-y-m 0.0108`.
     - 검증 `verify_egg_carton.py` 3차: 게이트 5개(입출력 불변 / 배치=fit / 규칙성 / 착지·림 여유·꼭지 노출 / 정렬·파라미터 일치) 통과.
       **슬롯 시퀀스는 바꾸지 않았다**(`0,1,6,7,12,13` 유지). 인접 시퀀스 `0,1,3,4,6,7` 로 놓으면 예상상 행 이웃이 림 아래에서
       2.3~6.7mm 겹치지만 **림 위 간격은 +11.6mm 이상**이라 화면에서는 갈라져 보인다 — 실기처럼 인접 배치로 돌릴지는 사용자 결정.
     **씬 재로드 + 노드 재기동 필요.** 런 8 판정표에 정렬 항목 추가.
   - **런 8 (09-11 02:52, `20260911T025246-941542d8`) — 로그 기준 통과, 화면에서 3차 설계 결함 1건.** `PICK COMPLETE` 6, `_PLACE_BLOCKED`·
     `clamped`·타임아웃·`MOVELINE_SHORT` 0건, 213.5초. 배치 목표 z 전부 185.6(직교화 확인), 착지 6개는 런 7 기반 예측과 x ≤2.6 / y ≤1.5mm 차.
     **사용자 관찰: 옆의 빈 컵으로 들여다보면 과실 적도가 컵 벽을 뚫고 나와 보인다.** 3차의 "림 아래라 안 보인다" 는 틀렸다 — 벽은 양면
     렌더라 빈 컵 안쪽에서 보인다. 상세는 `log/m3/README.md` §런 8.
   - **2026-09-11 4차 (사용자 요구 4건).** ① 구멍이 과실의 수평 최대 지름을 담을 것 ② 컵 바닥은 놓인 과실 밑끝에 딱 맞는 높이·형태
     ③ 계란판 밑변이 테이블 상판에 닿을 것 ④ 시퀀스를 바꿔서라도 **인접 칸** 배치.
     - ①은 피치를 바꾸지 않고는 불가능하다: 과실 y 전폭 53.8 + 착지 산포 ±3.8 + 여유 > 실기 행 피치 51.2. 실기 컵에는 실기 모형 과실이
       인접 3칸에 들어갔으므로 이것은 **과실 애셋 치수의 불일치**다(실기 셀 조건이 아니라 애셋). 실기 상수는 그대로 두고 시뮬 전용 파라미터
       **`taught_grid_pitch_override_m`**(기본 **0.0** = 실기 피치; `run_nodes.sh` 가 0.068) 로 배치 격자를 **정사각 68mm** 로 바꿨다
       (직교화 포함). §2 부수규칙 2("시뮬 전용 좌표 신규 금지")에서 한 발 물러난 것이고 **사용자 결정**이다 — 서술: "실기 티칭 격자
       59.8×51.2 는 시뮬 과실 애셋을 인접 배치할 수 없어 시뮬 격자를 68mm 로 분리(파라미터, 실기 기본값 보존)".
     - 컵 안쪽은 **과실 프로파일에서 유도**: 높이별 반폭(hx, hy) + 그 높이에 걸리는 과실 6개의 착지 치우침 + 여유 1.5mm, 위로 갈수록
       좁아지지 않게(오버행 금지), 링 16단. 림 rx28.5/ry32.2(구멍 57.0 × 64.4mm ≥ 과실 44.6 × 53.8), 능선 x 11.0 / y 3.6mm.
       **바닥 = 가장 낮게 놓인 과실 밑끝(런 8 실측 z 0.7mm)**, 림 z 57 그대로(어깨·줄기 6.3~8.8mm 노출). 밑변 z 0 = 테이블 상판, 밑면 없음.
     - **시퀀스 `0,1,6,7,12,13` → `0,1,3,4,6,7`** (열 0·1 × 행 0~2; 열 2·5·8 은 여전히 `IK_FAIL`). T4-1 의 행 건너뛰기는 4차로 폐기.
       착지 예측은 런 8 매달림 변위를 **수확 순서**로 재배정(변위는 그리퍼–과실 상대자세의 성질이라 슬롯 무관). 예상: 벽 여유 최소 +0.6mm
       (뚫림 없음), 림 여유 ≥7.2mm, 밑끝−바닥 +0.1~+2.6mm, 인접 과실 간격 ≥ +9.4mm(림 위 ≥ +26mm).
     - 정렬: 격자 평행이동 `taught_grid_shift_y_m` 0.0108 → **0.0452** (판이 길어져 중점을 y=0 에 두려면 더 옮겨야 한다).
       translate (0.7705, **0.1360**, 0), 컵 격자 중점 world (702.5, 0.00) mm.
     - 오프라인 도달성 (`check_tray_slot_reachability.py --pitch-m 0.068 --shift-y-m 0.0452`, 런 8 retreat 자세 7개): slot 0·1·4·6·7
       7/7, **slot 3 은 6/7** — `retreat#1` 시작에서만 `swing J6`. 수확 순서상 slot 3 은 3번째 과실의 retreat 뒤이므로 해당 시작이 아니다.
       단, 플래너는 swing 가드에 걸리면 **재시도 없이** `plan → None` → `TAUGHT_TRAY_SLOT3_PLACE_BLOCKED`(`curobo_planning_adapter.plan`)
       이므로, 런 9 에서 이것이 찍히면 시퀀스를 `0,1,4,3,6,7`(slot 3 을 4번째 과실이 받음 — retreat#4 시작은 통과)로 바꾼다.
     - 검증 `verify_egg_carton.py` 4차 게이트 통과(컵 동일성 비교를 최근접점 방식으로 고침 — 정렬 비교는 %.5f 반올림에 오판).
       파일: `egg_carton.usd` 7,944점/3,863면.
     - **런 9 (09-11 03:42, `20260911T034247-b358aa9c`) — 로그 기준 6/6, 인접 배치에서 배치 오류 0건.** `PICK COMPLETE` 6, `_PLACE_BLOCKED`·
       `joint_swing`·`clamped`·타임아웃 0, 217.2초. 사전 검사의 slot 3 우려는 발동하지 않았다. 착지 6개 → `FRUIT_REST_M`·`RUN_SLOT_SEQUENCE`·
       `RUN_GRID` 갱신 → 애셋 재생성(림 rx 28.5 → 31.1, 바닥 z 0.6, translate (0.7710, 0.1366)) → 검증 통과(벽 여유 최소 +0.7, 림 여유 ≥7.4,
       밑끝−바닥 +0.1~+2.2, 중점 y +0.6mm). **주의**: ripe_05 의 x 변위가 슬롯이 바뀌며 +3.9 → +7.4mm — 런 8 기반 애셋에서는 slot 3 쪽 벽을
       적도에서 ~1.7mm 넘었다(점유 컵 사이라 빈 컵에서 안 보임). 재생성 애셋은 **씬 재로드 시** 반영. 화면 항목(옆 빈 컵·판 접촉·바닥 접촉)은
       사용자 확인. 상세 `log/m3/README.md` §런 9.
---

5. **[ ] 익은 딸기 6 → 8 (안 익은 6 → 4) — 2026-09-11 사용자 결정, 런 10 에서 검증.**
   - **왜**: 4차 계란판으로 인접 배치가 되면서 병목이 트레이 도달성(열 0·1 × 5행 = 최대 10칸)으로 옮겨갔고, T4b 적응 분할은 분면당
     후보 3개가 있어야 화면에 보이는데 sw 만 해당했다. 6개는 기능을 구색만 갖춘 수준이라는 사용자 판단.
   - **어떻게**: 위치는 바꾸지 않고 `unripe_01`(nw, x −380 z 720) → **`ripe_07`**, `unripe_03`(ne, x 280 z 700) → **`ripe_08`** 로 변형·이름만
     전환(같은 애셋 `ripeness` 변형; 브릿지 발행 필터는 이름 기준). `main_scene`·`layout_layer`·`physics_layer`(조인트 `stem_ripe_07/08`)·덩굴
     이름. **총 12개 유지** → "실기 셀 조건 12개" 서술 불변. 분면별 익은 수 **nw 3 / ne 2 / sw 3 / se 0** → se 가지치기 유지, 분할 nw·sw 2회.
   - 시퀀스 `0,1,3,4,6,7` → **`0,1,3,4,6,7,9,10`** (slot 9·10 은 사전 검사 7/7). 계란판 도구: 발행 대상 8, 컵 봉투·검증을 **8칸 × 실측 편차 6개**
     후보로(수확 순서가 nw3→ne2→sw3 으로 바뀌어 과실→칸 매핑을 미리 알 수 없다). 컵 형상·translate 는 봉투가 같아 불변.
   - **미검증**: ripe_07 의 x −380 은 지금까지 집은 범위(−250~+200) 바깥. 파지 도달성은 런 10 에서 확인. 실패하면 대신 se 의 하나를 전환
     (가지치기 시연은 잃는다). 안 익은 것을 늘리거나 13번째 과실을 놓는 것은 하지 않는다(첫 판단 유지: 검증 없는 장식 / 좌표 중복·문서 전면 갱신).
   - 표현: "딸기 12개(익은 8/안 익은 4)". 런 1~9 의 수치는 6/6 기준이라 문서에 시점을 함께 적는다.

### [x] T4b — 적응 분할 (G §6-2) — **2026-09-11 구현, 2026-09-12 런 10 실행** (화면 확인·판정표 기입은 T4c 런 12 에서)

**왜 지금**: 09-10 범위 변경 때 T1 완료 기준 끝에 "T2 뒤 별도"라고만 적고 T 번호를 주지 않아 T2~T4 를 지나며
빠졌다. 면접 자료 `portfolio/G_quadtree_interview.md` §6-3 2번이 완료형("후보 밀도 규칙으로 런타임에 올렸고")으로 쓰여 있어
구현 없이는 사실이 아니다. **딸기는 늘리지 않는다** — 현재 배치(nw 2 / ne 1 / se 0 / sw 3)가 한 런에서 가지치기(se)·잎(nw·ne)·
분할(sw)·2단 가지치기(sw/sw 빈 칸)를 전부 보여준다. 근거는 런 9 로그 `SUBCELL_SCAN_ORDER root/sw root/sw/sw:0 se:1 nw:1 ne:1`.
쿼드트리의 전제는 분포의 **불균일**이지 개수가 아니다(H §2 "대량 배치" 항목과 같은 논리).

**§2 게이트**: 스캔 순서·자세(플래너 출력)가 달라지므로 정합 대상. 실기에는 런타임 판정이 없었으므로(G §5, 오프라인 YAML)
실기 1차 출처는 "판정 없음" = **파라미터 기본 0(끔)**. 세부 자세는 새 좌표 하드코딩이 아니라 부모 자세에서 **계산**한다(부수 규칙 2).

**규칙 (G §6-2 B 안)**:
1. 분면 근거리 스캔 → `_deduplicate_poses` 후보 수가 `subdivide_min_candidates`(기본 0 = 끔, `run_nodes.sh` 는 3) 이상이면 분할.
   미만이면 지금 동작 그대로(잎). 임계 3은 이 배치에서 세 경우가 다 나오도록 고른 시뮬 파라미터라고 문서에 적는다.
2. `_group_poses_by_subcell` 결과 중 **후보가 있는 칸만** 방문(빈 칸 = 2단 가지치기, `SUBCELL_EMPTY` 그대로). 순서는 현재 그룹 순서.
3. 세부 자세 = 부모 관절의 FK(`curobo_kinematics_adapter.CuroboKinematicsAdapter.ee_pose`) → **x·z 만 세부 칸 중심으로 평행이동**
   (y·자세 동일 — 실기 NW 세부 자세 4개가 y=433 한 평면인 성질과 같다, G §5) → **09-14 정정: 그 평면(433)은 분면 자세(317~339)보다 보드에 약 100mm 가깝다. 세부 자세는 y 도 433 으로 옮긴다(`subcell_ee_y_m`, 사다리: lab_plane → parent_y → 부모 자세)** → 부모 관절을 시드로 IK(`mg.ik_solver`,
   `check_tray_slot_reachability.py` 와 같은 호출) → 부모 대비 관절 변화 최대 **60° 초과 또는 IK 실패면 `SUBDIVIDE_REJECTED`**
   로 그 칸은 부모 자세에서 pick(현재 동작으로 퇴화).
4. 세부 자세 이동은 **실기와 같은 MoveJoint**(`_move_to_scan_cell_and_wait` 경로, `self._targets[cell]` 에 유도 자세를 넣어 재사용).
   cuRobo `_plan`+스플라인 경로는 쓰지 않는다 — 한 번도 실행된 적 없는 경로(G §6-1 3)를 녹화 직전에 살리지 않는다.
   대신 이동 쌍(부모→세부, 세부→세부)의 보드 여유를 FK 로 **사전 계산**해 50mm 기준으로 판정표에 넣는다(가지치기 때와 같은 방법).
5. 세부 칸에서 재스캔(dwell 동일) → pick → 다음 칸 → 마지막 칸의 pick 이 끝나면 다음 분면. **깊이 상한 2** — 세부 칸에서는 다시 쪼개지 않는다.
6. `quadrant_filter.quadrant_from_cell_id` 가 3단 이름(`root/sw/nw`)을 부모로 뭉개는 동작을 파라미터로 풀어 세부 칸 좌표만 발행
   (가까이 가면 시야가 좁아지는 것과 같다). 이웃 장애물 등록이 줄어드는 점은 한계로 적는다. → **09-14 구현: 실행기가 스캔 자세에 도착하면 `<cell>=VIEWING` 을 발행하고, 시뮬 비전은 lab_plane 세부 자세에서만 그 세부 칸으로 시야를 좁힌다(`subcell_view_enabled`). parent_y 단계와 부모 자세 pick 은 분면 시야 그대로**
7. HUD AREA 라벨에 세부 칸(`SW/NW` 식).

**손대는 곳**: `scan_executor_node._process_cell_detections`(subgroups 루프가 분기점) · `_move_to_scan_cell_and_wait` ·
`_init_motion_gen`(IK 솔버가 필요하므로 `subdivide_min_candidates>0` 이면 생성 — 지금은 `enable_runtime_curobo_preview` 에서만 만든다;
기동 시간 증가는 기록) · `strawberry_sim_core/quadrant_filter.py` · `scripts/run_nodes.sh`(파라미터 1줄 + 기동 로그 대조).

**로그 문자열**: `SUBDIVIDE root/sw candidates=3 >= 3 cells=[se,nw,ne]` · `SUBDIVIDE_SKIP root/nw candidates=2 < 3` ·
`SUBCELL_POSE root/sw/se dJ_max=..deg` · `SUBDIVIDE_REJECTED root/sw/.. reason=..` · `AT_SCAN_POSE root/sw/se`.

**완료 기준 — 런 10 판정표(`log/m3/README.md`)**: ① `SUBDIVIDE root/sw` 1건 + `SUBDIVIDE_SKIP` nw·ne ② `AT_SCAN_POSE` 가 sw 세부 3칸에
있고 `root/sw/sw` 에는 없음 ③ 세부 이동 전부 관절 변화 ≤60°, 보드 여유 ≥50mm(FK 사전 계산) ④ `PICK COMPLETE` 6, `clamped`·
`ARM_ARRIVAL_TIMEOUT`·`JOINT_COMMAND_REJECTED`·`EXEC_*` 0 ⑤ 배치 착지가 런 9 수준(T4-3 회귀 없음) ⑥ 전체 시간 증가분 기록(근거가 아니라
부산물, G §7) ⑦ 화면 관통·HUD 는 T5 에서. **런 10 은 씬 재로드(4차 계란판 애셋 반영)와 겸한다** — 런 9 의 화면 확인 항목도 그때 닫는다.

**끝나면**: G 머리말 "§6-2 미구현" → 런 10 id 로, G §6-3 2번에 런 10 근거, `portfolio/README.md` G 행, `PLANNER_CHANGES.md` 항목, 이 항목 `[x]`.

**[x] 09-14 보강 (T4c 전 선행, 사용자 결정 "깊이가 깊을수록 보드에 가깝게")** — ① `subcell_pose.LAB_SUBCELL_EE_Y_M` 0.433(실기 깊이 2 티칭 평면, `--lab-fk` 로 FK 확인: 4자세 전부 y 432.9~433.0) ② 유도 사다리 `derive_subcell_joints_tiered`: lab_plane → parent_y → 부모 자세 ③ `VIEWING` 상태 + `fake_vision` 세부 칸 시야(lab_plane 만) ④ 깊이 3 없음(실기 데이터가 깊이 2 까지). 오프라인(`log/m3/offline_checks/subcell_poses_tiered_20260914.txt`): 16칸 중 **lab_plane 9 · parent_y 2(ne/se, sw/ne) · IK 밖 4(nw·ne 위쪽) · 해당없음 1**, 보드 여유 최소 68mm, 카메라-보드 거리 분면 412~472mm → lab_plane 318~319mm. **09-15 확인**: 실기 카메라 로그(`docs/lab_data/realsense_d455_enumerate.txt`)로 계산한 D455 MinZ 는 848×480 328mm / 640×480 297mm / 640×360 248mm(`docs/d455_min_range.md`) — 카메라-과실 약 292mm 는 848×480 이면 밖. 실기 사용 해상도·disparity shift 는 로그에 없어 **미확인**, `fake_vision` 은 거리 모델이 없어 이 축에서 시뮬이 관대함(한계로 기록). 화면·로그 확인은 런 12(09-15 13:11 실행: 16칸 중 lab_plane 5·parent_y 1 실행, `VIEWING` 4건 — `log/m3/README.md` 런 12). 이 보강으로 분할이 시뮬에서 측정 가능한 차이(등록 장애물 수·접근 계획)를 낼 수 있게 됐고, 그 측정은 T4d 에서 한다.

**[x] 09-11 구현 완료 · 오프라인 검증 완료 · 노드 모의 시험 통과 — 런 10 대기(Isaac Sim 기동·씬 재로드는 사용자).**
- 코드: `execution/subcell_pose.py`(신설, 계산부 — rclpy 없이 import, 노드와 검사 스크립트의 단일 출처), `scan_executor_node.py`(파라미터
  `subdivide_min_candidates`·`subdivide_max_joint_delta_deg`, `_init_subdivide_solver`, `_should_subdivide`, `_derive_subcell_target`,
  `_subdivide_and_pick`, `_dwell_collect_detections`, `_subcell_of_pose`), `scripts/check_subcell_scan_poses.py`(신설), `run_nodes.sh`
  (`subdivide_min_candidates:=3` + 대조 1줄), 문서(`docs/parameters.md`, `docs/run_guide.md`, `PLANNER_CHANGES.md` 4건). `colcon build` 완료.
- **규칙 대비 달라진 점 3가지**: ① 규칙 3의 IK 는 MotionGen 이 아니라 **IKSolver 만** 만든다(계획 불필요, 기동 0.4초, 세 번째 무거운 인스턴스 회피).
  ② **규칙 6(비전 필터 세부 칸 좁히기)은 하지 않았다** — 세부 자세 재스캔은 분면 시야 좌표 중 그 칸의 것만 **실행기**가 거른다
  (`_subcell_of_pose`, 그룹화와 같은 함수). fake_vision 무수정, 이웃 장애물 등록은 분면 단위 그대로라 오히려 보수적이고, 잎 분면의 종전
  동작이 바뀌지 않는다. ③ 규칙 7(HUD 세부 칸 라벨)은 **HUD 쿼드트리 패널로 대체해 09-11 구현** (사용자 결정 — 트리 분석이 실행 화면에 실시간으로 찍혀야 한다).
  영역·타겟·배치 줄 자리에 트리: 1차 스캔 `방향(제외)`, 잎은 방향, 분할은 `방향(분할)` + 세부 칸 줄(nw·ne·se·sw 순, 최근 분할 분면만, 순회 끝나면 접힘 — 09-12), 로봇 위치는 주황. 보드 하이라이트도 세부 칸 단위(`whiteboard.usd` 16장 추가, 09-12, 씬 재로드 필요).
  실행기 무수정 — `harvest_probe` 가 메서드 경계에서 받는다(`hud/tree_model.py` 신설, 문서 `hud/README.md` §트리 패널).
  오프라인: 실기 실행기 코드를 cuRobo 만 막고 돌려 프로브 유무로 상태 문자열·이동·픽 순서 동일, 8/4 배치에서 트리 전이 기대대로
  (NW 분할·se 부모 퇴화, NE 잎, SE 제외, SW 분할). HUD 는 가짜 Kit 모듈로 위젯 생성·갱신만 확인 — **Kit 화면 확인은 런 10**(사용자).
- 오프라인 검사(한도 60°): **sw 세부 4칸 전부 수락** — 관절 변화 최대 23.3 / 25.4 / 33.6 / 26.2°(sw/se/nw/ne), 유도 자세 ee 오차 ≤0.1mm,
  MoveJoint 관절공간 직선 보드 여유 10쌍 **최소 183.8mm**(기준 50). se 도 4/4(최소 209.7mm). nw·ne 는 아래 칸 1개씩만 수락 — 위쪽 칸은 같은
  y·방향으로 IK 해가 없고 nw/se 66°·ne/sw 70° 초과. 이 배치에서 nw·ne 는 잎이라 무관하고, 켜져도 `SUBDIVIDE_REJECTED` 로 부모 자세 pick 퇴화.
  **정직하게 적을 것**: 유도 규칙은 아래 분면(se·sw)에서 잘 되고 위 분면(nw·ne)의 위쪽 칸에서는 안 된다 — 같은 높이 평행이동이 팔 길이 밖이다.
- 노드 모의 시험(실제 IK, 이동·pick 모의) 5경로 통과: 분할(se→nw→ne 이동 3·pick 3, sw 빈 칸 2단 가지치기) / 전부 거부(이동 0, 부모 pick 3) /
  혼합(se 만 수락 → nw 거부 시 부모 복귀 1회 → ne 는 부모에서) / 끔(종전 경로 그대로) / 그룹화 = 런 9 와 동일.
- **예상 런 10 로그**: `SUBDIVIDE_IK_READY` → nw·ne `SUBDIVIDE_SKIP` → `SUBDIVIDE root/sw candidates=3 >= 3 cells=['se', 'nw', 'ne']` →
  `SUBCELL_EMPTY root/sw/sw` → `SUBCELL_POSE root/sw/se` → `MOVING_TO root/sw/se` → `AT_SCAN_POSE root/sw/se` → `SUBCELL_SCAN … unique=1` →
  pick → nw → ne → 부모 재스캔(종전 흐름, 과실 없음) → overview. 세부 이동 3회 + dwell 3회로 약 +10~15초.
- **런 10 절차**: Isaac Sim 기동 → 씬 재로드(런 9 뒤 재생성한 4차 계란판 애셋 반영) → 브릿지 Run → Play → `bash scripts/run_nodes.sh`
  (대조에 `SUBDIVIDE_IK_READY min_candidates=3`) → 트리거 → 로그 보존. 판정표 틀은 `log/m3/README.md` §런 10. 통과하면 G 머리말·§5 표·
  `portfolio/README.md` 를 "런 10 검증" 으로 고치고 이 항목을 닫는다. 실패하면 원인을 고쳐 런 10 을 다시 돌린다 — 끄고(`subdivide_min_candidates:=0`) 넘어가지 않는다.

**시작 순서 (다음 세션)**: ① `ps -ef | grep` 남은 노드 확인 ② 이 항목 + G §5·§6-2 읽기 ③ 코드 확인 —
`_process_cell_detections`(~1470행)·`_move_to_scan_cell_and_wait`(~1373행)·`_init_motion_gen`(~401행)·`check_tray_slot_reachability.py` 의
`mg.ik_solver` 호출 ④ 세부 칸 중심 좌표 = `_group_poses_by_subcell` 이 쓰는 부모 분면 경계 중심선에서 계산(새 상수 금지) ⑤ 구현 → 오프라인 FK
여유 검사 → 런 10.

### [~] T4c — 실기 원본 값 복원 뒤 실패를 정직하게 보여주기 — **2026-09-15 ①②③ 완료, ④ 런 13 대기**

**왜**: 런 10~12 에서 이송 계획이 거부된 과실은 `hold_on_place_failure=false` 로 그 자리에서 놓이는데, 브릿지가 릴리스 순간 과실을 공중에
고정해(T2 표현) 화면에서는 "들고 있는 것"처럼 보였다. 실기 원본 값으로 되돌린 뒤 배치가 4/8 이 되는 게 정상이므로, 그 실패가 화면·완료 줄에 그대로
보여야 한다(규칙 0 — 시뮬이 원본보다 관대해 보이면 안 된다).

- **① 낙하 물리** (`isaac_sim_script_editor_bridge.py`): RELEASE 위치가 계란판 세계 상자(x·y ±30mm, 상판 위 300mm 안)면 종전대로 동결(계란판엔
  콜라이더가 없어 동적 과실은 뚫고 떨어진다), **밖이면** kinematic 해제·콜라이더 복구·속도 0 → 중력. Kit 로그 `RELEASE … PLACED in tray, frozen at` /
  `RELEASE … DROPPED outside tray at … -> falls  dropped=n`. **바닥**: 보드 앞 릴리스 지점(y≈0.74)은 테이블 상판(y≤0.30) 밖이라 받을 면이 없었다 →
  `lab_environment.usd` 에 `floor`(시뮬 소품, 작업대 높이 0.75m 가정, 상판 z=0 기준 −0.75) + `physics_layer.usd` 콜라이더. **씬 재로드 필요**.
  헤드리스 Isaac 검증(09-15, 스크래치 `test_drop_headless*.py`): kinematic→dynamic 런타임 전환은 되지만 **부착 중 세션 레이어에 쓴 xform 이 PhysX 의
  루트 레이어 쓰기를 가려** 화면에선 계속 공중에 떠 보였다(변형 B: 매 스텝 PhysX 자세를 USD 에 복사 → omni.physx 가 텔레포트로 보고 속도를 매번 리셋, 0.16m/s 로
  가라앉음). 채택: 낙하 시 세션 레이어 xform 의견을 **제거** → 튐 없이 PhysX 쓰기가 드러나고 −0.732m(바닥 위)에 정지. 손끝 파츠엔 콜라이더가 없어 조우 안에서
  콜라이더를 켜도 튕김 없음.
- **② HUD 낙하 카운트**: `status_bus.result.dropped`(사양 3절 필드 추가, 소유 planner) — `harvest_probe` 가 실행기의
  `_release_and_continue_after_place_failure` 경계에서 +1(`PLACE_GATE_BLOCKED_*` 는 과실이 없으므로 제외 = failed). `bus_merge` 전달, 완료 블록 둘째 줄
  `배치 n · 낙하 m`(라벨 PNG `final_placed`·`final_dropped`, 낙하는 빨강). 실행기·플래너 무수정. 단위 검사: 라벨 3종 호출에 dropped 2.
- **③ 런 12** (09-15 13:11, 사용자 실행): 판정표 `log/m3/README.md` 런 12 — 7/8 분리 · 4 배치 · 3 이송 거부(전부 J6 spline jump 356>270, J3 swing 0) ·
  ripe_08 진입 실패(브릿지 MoveLine IK 실패, 09-14 로봇 한계 뒤 첫 차이 — 원인은 T4d 전 확인) · 깊이 2 단계 lab_plane 4/parent_y 1 · `VIEWING` 4 · 방어선 0 · 277초.
- **④ 런 13**: 씬 재로드(바닥) → 브릿지·HUD Run → 실행. 볼 것: 이송 거부 과실이 **떨어져 바닥에 멈춤**(Kit `DROPPED … dropped=n`), 완료 줄
  `배치 4 · 낙하 3` 꼴, HUD 트리·세부 칸 하이라이트(런 12 화면 항목 10), 떨어지는 과실이 아래 과실을 건드리는지(있으면 기록).
- **D455 (09-15)**: 실기 카메라 로그로 MinZ 계산 → `docs/d455_min_range.md`. 실기 설정 미확인, 시뮬 관대 축으로 기록.

---

### [ ] T5 — 녹화

롱샷 런 1회 + 클로즈업 런 1회. 절차는 `portfolio/README.md`.

- [x] 09-10 촬영 전 씬 시각 정비 — ① 분면 꼭지점 봉 9개(`cell_markers.usd`) 제거(시야 방해) ② 보드를 흰 종이 2×2 격자 텍스처로 복귀 ③ 작업 영역을 보드 위 옅은 주황 오버레이로 표시(`whiteboard.usd` `highlight/*`, HUD 가 켜고 끔; 홈 = 전체, 분면 = 그 칸). 전부 시각 전용 — §2 게이트상 플래너 입출력 불변. **씬 재로드 필요.**
클로즈업은 별도 내용이 아니라 **같은 사건의 다른 카메라**다: 그리퍼·타겟에 붙어 접근 정밀도, 줄기가 물리는 순간, HUD 판정을 담는다. 이 클로즈업이 그대로 A-1의 시뮬 쪽이 된다.

---

### [ ] T6 — 수치 갱신 · 문서 정합 · 원격 푸시

- `portfolio/C_before_after.svg`, `portfolio/E_metrics.md`: 672/0.0mm 계열 수치 → `PROGRESS_REPORT.md` §5 값으로. **"접근 오차 131.7 → 0.0mm" 표현 삭제**, "131.7mm 불일치 규명 및 정합(clamp WARN 소멸)"로 교체.
- 파일·줄 수, 결함 건수, END-TO-END 완주 횟수(재완주 반영) 갱신.
- `PROJECT_SUMMARY.md`, `README.md`: 09-07 잔존 상태(672mm, 1,117줄 등) 정정.
  - [x] 09-10 선행 처리 — 위 두 항목 중 **재완주와 무관한 부분**(0.0mm 표현 삭제, 672/1,117줄/61파일/21건 계열을 `PROGRESS_REPORT.md` §5 값으로, C 패널 ① 문구·PNG 재생성)은 끝냈다. **남은 것**: END-TO-END 완주 횟수·결함 건수 최종 집계(T1 이후), 원격 푸시.
- **원격 푸시** (`OceanShape/strawberry_grasp_environment`, 마지막 커밋 07-07, 미커밋 75+ 파일):
  덩어리로 나눠 커밋한다 — ① 플래너 이식(`src/strawberry_motion` + `_baseline/` + `PLANNER_CHANGES.md`) ② `strawberry_sim_core` ③ Isaac 스크립트·USD ④ `docs/`·`portfolio/` ⑤ 로그.
  푸시 전 `.gitignore`로 대용량 바이너리·영상·중간 산출물 제외 확인.

---

### ~~T7~~ — (선택) 레이아웃 변경 1회 완주 — **2026-09-14 T4d(무작위 배치 N 회)로 흡수**

딸기 12개의 **위치만 바꾼 두 번째 레이아웃**으로 완주 1회. 레이아웃 레이어만 교체하면 되므로 싸다.
"하드코딩이 아니다"를 보이는 방법은 **많이가 아니라 다르게**다. 실기도 매번 배치가 달랐다는 사실과 일치한다.

---

## 6. 갱신 대상 기존 문서

| 문서 | 무엇을 |
|---|---|
| `PORTFOLIO_SPRINT.md` | M2.7 Stage3를 T2 명세로 교체, M3 종료 조건을 §3 영상 명세로 확정, 원안 C·D를 범위 밖으로 명시 |
| `PROJECT_GOAL.md` | 원안 시퀀스 중 병든 딸기 분기가 이번 제출 범위 밖임을 각주로 |
| `portfolio/README.md` | 카메라 2대 촬영 절차, 클립쌍 4종 체크리스트 |
| `portfolio/E_metrics.md`, `C_before_after.svg` | T6의 수치 갱신 |
| `PROJECT_SUMMARY.md`, `README.md` | 09-07 잔존 수치 정정 |
| `PLANNER_POLICY_v2.md` | 표현 가이드 델타(§7) 반영 |

---

## 7. 표현 가이드 델타

기존 금지 목록은 그대로 유지하고 다음을 더한다.

- **"관통 없이"** — T1 재완주 성공 후에만 사용.
- **"병든 과실 선별"** — 사용하지 않는다. 시뮬 범위 밖. 잔여 이슈에 범위 결정으로만 기재하고, 팀원 관련 경위는 어떤 문서에도 쓰지 않는다.
- **"전체 시퀀스 재현"** — 원안 대비 폐기 분기가 빠졌으므로 쓰지 않는다. "수확 핵심 경로 완주"로.
- **딸기 부착** — T2가 되면 "파지 후 과실이 그리퍼를 따라 이동·배치되는 것까지 시뮬레이션". **"파지 성공"·"수확 성공"은 여전히 금지.**
